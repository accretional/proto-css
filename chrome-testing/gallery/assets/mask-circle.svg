<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="100" height="100">
  <!-- Opaque white disc on transparent ground — the second mask layer for
       mask-composite (add / subtract / intersect / exclude with the star). -->
  <circle cx="50" cy="50" r="40" fill="#ffffff"/>
</svg>
