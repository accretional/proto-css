# The per-value screenshots (PNG) and animations (GIF) are committed — they are
# the showcase. The intermediate temporal frame folders (frame-*.png, ~230 MB)
# only feed GIF encoding and are regenerable, so they stay out of git, as do the
# automation textprotos and the MDN fetch cache.
screenshots/**/frame-*.png
generated/shots.textproto
generated/shots-*.textproto
generated/mdn-cache/

# verification scratch (contact sheets, verdicts)
_verify/
