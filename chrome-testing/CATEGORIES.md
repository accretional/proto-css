# The CSS Codex — per-property categorisation

Multimodal verification (judged by eye, no byte comparison) of every value of **513** properties. Each property is in exactly one category.

**Goal state:** every property in **1**, **3** (perfect) or **5** (chromerpc cannot capture). Categories 2, 4, 6, 7, 8 are work items.

| Cat | Meaning | Count |
|---:|---|---:|
| 1 | Perfect — every value adequately, uniquely & correctly demonstrated | 128 |
| 2 | UI polish — all values correct & distinct, but the demo could be clearer (fixable) | 0 |
| 3 | Perfect (inherent) — most values unique; a few render identically by spec/engine (unfixable, correct) | 365 |
| 4 | UI weak — some values collide only because the demo is too weak (fixable) | 0 |
| 5 | chromerpc limit — cannot be captured headless (cursor sprite, interaction) (inherent) | 20 |
| 6 | Pipeline-fixable — not captured now, but the screenshot automation could be customised (fixable) | 0 |
| 7 | Broken / other — a value renders wrong or the demo is broken (fixable) | 0 |
| 8 | Grammar fix — collisions from spec-invalid generated values (fix lang/*.ebnf) | 0 |

**513/513** properties are already in the goal state (100%). Work remaining: {}


## Category 1 — Perfect — every value adequately, uniquely & correctly demonstrated  (128)

- **`anchor-name`** — Both values are correct and visually distinct. With 'none' (index 0) only the 'chip' element renders with no tethered anchor box, since the anchor has no name to reference. With '--my-var' (index 1) t
- **`anchor-scope`** — All three values render distinctly and correctly. The demo uses two anchor names (--my-var red, --other blue), each with an inner anchor (a/b, inside the dashed scope box) and an outer anchor (A/B, to
- **`animation-composition`** — The temporal demo runs a transform animation on top of a base transform and samples the same moment for each value. At that moment the box is clearly distinct per value: replace[0] shows the smallest,
- **`animation-delay`** — All three delays (0.3s, 1s, 200ms) are valid <time> values and the 3-frame filmstrip captures distinct box positions: the shorter the delay the further the box has travelled at a fixed capture time (2
- **`animation-direction`** — normal, reverse, alternate, alternate-reverse are all valid and render with clearly different box positions/travel direction across the filmstrip frames (each value samples the box at a distinct posit
- **`animation-duration`** — All 3 values are correctly and distinctly demonstrated in the temporal filmstrip. [0] auto renders fully static across all 4 frames (box stays at x~320); this is spec-correct because for a standard ti
- **`animation-fill-mode`** — none, forwards, backwards, both are all valid and render distinctly across the filmstrip: forwards and both hold the end-keyframe position at the final frame (box parked to the right), backwards holds
- **`animation-iteration-count`** — Excellent temporal demo. A red box travels horizontally and a counter badge shows completed iterations ('0×', '1×', '2×'). The box's resting X-position scales with fractional progress: 0.25 ends ~quar
- **`animation-name`** — Each animation-name references a different @keyframes rule and produces a visibly distinct effect on the sample box: none -> box static at start (no animation, correct); my-ident -> small rightward tr
- **`animation-play-state`** — running shows clear motion across the filmstrip (box at ~x293 in frame-00 moving to ~x362 in frame-02), while paused freezes the box at a single position (~x248) with no change between frames. The two
- **`animation-timing-function`** — All 10 easing functions are correct and visually distinct. At the same sampled timestamp the box sits at a different x position per function, reflecting the easing: e.g. at frame-01 ease-in x~270 (slo
- **`appearance`** — Excellent purpose-built demo: each value is paired with its semantically matching native control (checkbox, radio, search input, textarea, select/menulist, multi-select listbox, meter, progress, butto
- **`backface-visibility`** — Both values are correct and visually distinct. The demo uses a card rotated in a perspective scene so its back face is toward the viewer. With 'visible' (index 0) the back face is rendered (the red/bl
- **`background-attachment`** — Temporal scroll filmstrip of a scrollable container with a diagonal red houndstooth background. As the content scrolls from rows 1-3 to rows 6-8, the three values produce visibly distinct background-p
- **`background-blend-mode`** — All 16 blend modes render distinctly and correctly on the photo-over-gradient sample. normal shows the plain photo; multiply darkens with the red/blue gradient; screen/lighten/color-dodge brighten; di
- **`background-origin`** — All 3 values render correctly and are clearly distinct. content-box confines the gradient to the inner content area with a wide gap to the dashed border; padding-box extends the gradient out to the pa
- **`border-block-end-width`** — Effect panel shows a box with a colored border on its block-end (bottom) edge. The five px values (24/48/8/16/64) render visibly increasing bottom-border thickness and are all distinct. The thin/mediu
- **`border-block-start-width`** — Effect panel shows a box with a colored border on its block-start (top) edge. The five px values render visibly increasing top-border thickness and are all distinct. thin/medium/thick are valid <line-
- **`border-block-width`** — Shorthand applies the width to BOTH the block-start (top) and block-end (bottom) edges; the effect panel correctly shows two colored horizontal borders (top + bottom) on the box. The five px values sc
- **`border-bottom-width`** — Clean, unambiguous demo: a beige box whose bottom border thickness is set per value. 24px, 48px, 8px, 16px, 64px all show distinctly different bottom-border thicknesses, and the line-width keywords th
- **`border-collapse`** — Both values are correct and clearly distinct on the 2x2 table demo. separate (0) shows spaced cells each with their own border plus a rounded outer table border (border-spacing visible between cells).
- **`border-image-outset`** — Every value is correct and distinct. The striped diagonal border-image is pushed outward from the border edge by exactly the given amount: 8px sits tight to the box, 24px leaves a clear gap, 48px and 
- **`border-image-repeat`** — All three keywords render correctly and distinctly. stretch (0) shows the diagonal stripe slices stretched along each edge (visibly distorted/stretched mid-edge). repeat (1) tiles the slice at native 
- **`border-inline-end-width`** — The box shows a border on the inline-end (right, LTR) edge whose thickness scales with each value. The five px values (8/16/24/48/64px) are clearly different inline-end widths, and the keyword values 
- **`border-inline-start-width`** — The box shows a border on the inline-start (left, LTR) edge whose thickness scales with each value. The five px values (8/16/24/48/64px) are clearly distinct, and thin/medium/thick render as distinct 
- **`border-inline-width`** — The box shows borders on BOTH inline edges (start and end) whose thickness scales with each value, correctly reflecting the shorthand. The five px values (8/16/24/48/64px) are clearly distinct and the
- **`border-left-width`** — The box shows a border on its left edge whose thickness scales with each value. The five px values (8/16/24/48/64px) are clearly distinct left-border widths, and thin/medium/thick render as distinct h
- **`border-right-width`** — All 8 values render a beige box whose RIGHT border thickness varies correctly. 8px/16px/24px/48px/64px increase visibly; thin/medium/thick resolve to 1px/3px/5px and are mutually distinct and distinct
- **`border-spacing`** — All five px values render a table whose inter-cell spacing visibly increases monotonically (8px < 16px < 24px < 48px < 64px). Every value is correct, distinct and clearly demonstrated.
- **`border-top-width`** — Effect panel shows a box with a colored border on its top edge. The five px values render visibly increasing top-border thickness and are all distinct. thin/medium/thick are valid <line-width> keyword
- **`border-width`** — All 8 values render correctly and distinctly. The px values show a clear progression of border thickness (8px thin, 16px medium, 24px moderate, 48px very thick, 64px nearly fills the box). The line-wi
- **`box-decoration-break`** — Textbook-perfect demo on a wrapped inline highlight box. slice (00): rounded corners + border appear only at the very start and very end of the whole inline box; interior line breaks have FLAT square 
- **`box-direction`** — Both values are correct and clearly distinct. The legacy -webkit-box demo reorders the four numbered cells: 'normal' renders 1 2 3 4 (left to right) and 'reverse' renders 4 3 2 1. The reordering is un
- **`box-flex`** — The demo varies box-flex on the first flex child while the other two children stay flexible, so item 1 receives a proportional share of the free space. The five values produce monotonically different 
- **`box-ordinal-group`** — Demo is a legacy -webkit-box flex row of cells labelled 1-5 plus a red starred target whose ordinal group is the value under test. The red cell lands in a unique, correct slot for every value: group 1
- **`box-pack`** — All four values are correct and visually distinct. start packs the boxes to the left edge, center centers them, end packs them to the right edge, and justify spreads them with space between. Each cell
- **`box-shadow`** — All 50 values are inset box-shadows of the same red (#c5483c) varying only by offset-x (24px/48px), offset-y (8/16/24/48/64px) and blur (0/8/16/24/48/64px). Every value is valid CSS and renders a corr
- **`box-sizing`** — Both values are correct and clearly distinct. content-box (idx 0) draws the border/padding OUTSIDE the declared content width, so the box's total footprint is larger and the red border ring sits furth
- **`caption-side`** — Both values render correctly and distinctly. For 'top' the caption label sits above the table; for 'bottom' it sits below the table. The active value is also highlighted in the toggle legend on the ri
- **`clip`** — Both values render correctly and distinctly. [0] rect(8px, 56px, 56px, 8px) shows the absolutely-positioned image clipped to a 48x48px top-left rectangle; [1] auto shows the full unclipped image. clip
- **`clip-rule`** — Both values are correct and clearly distinct. 'nonzero' fills the whole self-intersecting star (solid star). 'evenodd' leaves the central self-overlap region unfilled, producing the classic hollow pen
- **`column-height`** — column-height caps the height of each column in a multi-column block; content overflows into additional column rows when capped. The 6 values render a clean monotonic progression: auto = tallest singl
- **`column-rule-width`** — All 8 values render correctly and distinctly. The effect panel shows multi-column text with a red column-rule between columns whose thickness grows monotonically with the value: 8px (thin) < 16px < 24
- **`column-span`** — Both values render correctly and distinctly. 'none' keeps the red heading inside a single column with text flowing in multi-column layout around it; 'all' makes the heading span the full width across 
- **`container-name`** — All three values are correct and uniquely distinct. The probe card has A=name my-ident and B=name tag-a rows. 0 none -> all grey (no named query matches, correct). 1 my-ident -> A row highlighted red 
- **`direction`** — Both values are correct and visually distinct. ltr renders the specimen text left-aligned/flowing from the left edge; rtl renders the same text right-aligned/flowing from the right edge, with the lege
- **`empty-cells`** — Both documented values are correctly and distinctly demonstrated. The demo uses a 2x2 table where the bottom-left cell is empty. With 'show' (00) the empty cell renders its border and red-tinted backg
- **`field-sizing`** — Both values are correct and clearly distinct in full-res. With fixed (00) the native input keeps its default fixed width (~170px, matching the 'native button' above). With content (01) the input shrin
- **`fill-rule`** — Both values render correctly and distinctly on the self-intersecting star path: nonzero fills the whole star solid, while evenodd leaves the inner pentagon hollow. Clearly different and each matches t
- **`filter`** — The demo applies filter directly to a sample photograph. Every value renders correctly and uniquely: none[0] sharp; blur(24px)[1] heavily blurred; brightness(0.4)[2] darkened; contrast(0.4)[3] flatten
- **`flex-direction`** — All four values are correct and visually unique. row shows items 1-4 left-to-right; row-reverse shows them 4-3-2-1 (numbers reveal the reversed order); column stacks them 1-4 vertically top-to-bottom;
- **`flex-grow`** — All 5 values (1, 2, 0.5, 0.75, 0.25) are valid <number> growth factors. The highlighted star item grows proportionally and visibly more for higher factors (widest at 2, narrowest at 0.25), each value 
- **`flex-shrink`** — All 5 values (1, 2, 0.5, 0.75, 0.25) are valid <number> shrink factors. The star item is widest at shrink 0.25 and progressively narrower toward shrink 2 (it gives up the most space). Per-step differe
- **`flex-wrap`** — All three values are correct and visually distinct in a 6-item flex container. nowrap(0): items 1-6 stay on a single line and overflow the container horizontally. wrap(1): items wrap onto two lines in
- **`font-optical-sizing`** — Both values render correctly and distinctly. 'auto' and 'none' differ visibly: with 'none' the 'Hamburgefonstiv' sample renders noticeably wider/heavier (the active opsz axis is frozen at its default 
- **`font-synthesis-small-caps`** — auto (idx0) renders 'HAMBURGEFONSTIV' in synthesized small-caps, while none (idx1) renders normal-case 'Hamburgefonstiv' with small-caps synthesis suppressed. The two values are clearly distinct and e
- **`font-synthesis-weight`** — auto renders the sample with noticeably heavier (synthesized-bold) strokes while none renders thinner real-weight strokes; both values are correct and visually distinct.
- **`font-variant-position`** — All three values are correct and clearly distinct. normal shows baseline 'H2SO4 E = mc2'; sub lowers and shrinks the digits into subscripts (H2SO4 with lowered 2/4, mc2 lowered); super raises and shri
- **`grid-auto-columns`** — Demo shapes a grid whose implicit (auto) columns are sized by the value; the effect panel clearly widens/narrows the auto-tracks. Fixed lengths 8/16/24/48/64px form a clear progression; percentages 15
- **`grid-auto-rows`** — Identical structure to grid-auto-columns but in the row (height) direction; the implicit rows visibly change height per value. Fixed lengths 8/16/24/48/64px give a clear short-to-tall progression; per
- **`grid-template-areas`** — All three values render distinctly and correctly. 'none' produces a plain auto-flow grid with no named areas (the area-named items auto-place). '"Specimen"' defines a single named area; the item assig
- **`hyphenate-character`** — All 3 values render correctly and distinctly. The long demo word auto-hyphenates and the chosen hyphenate-character string appears at every line-end break. [0] auto -> default '-' hyphen ('supercalifr
- **`hyphens`** — All three values are correct and clearly distinct. none[0] applies NO hyphenation so the over-long words (supercalifragilisticexpialidocious / antidisestablishmentarianism) stay whole and overflow. ma
- **`isolation`** — Both documented values are demonstrated correctly and are clearly visually distinct. The demo places a difference-blend-mode patch over a rainbow gradient backdrop. With isolation:auto (index 0) the p
- **`letter-spacing`** — All 20 values render correctly and distinctly. 'normal' is tight with no tracking. The <length> values form a clear monotonic progression (8px < 16px < 24px < 48px < 64px) of inter-letter spacing. The
- **`line-height-step`** — Each value snaps line boxes to a multiple of the step, so the wrapped paragraph's line spacing grows monotonically with the step size. [2] 8px and [3] 16px are tight, [0] 24px medium, [1] 48px and [4]
- **`list-style-position`** — Both values render correctly and are visually distinct. 'inside' places the bullet markers within the content flow so the marker+text block is indented and the bullets sit further right; 'outside' han
- **`marker`** — The demo draws a red zigzag polyline with vertex markers. 'none' (0) shows the bare polyline with no markers at the vertices; url(assets/photo.jpg) (1) places a blue circle marker at every vertex of t
- **`marker-end`** — Two values, both correct and distinct. [0] none renders a bare red polyline with no marker. [1] url(assets/photo.jpg) places a blue marker dot at the END vertex (right terminus) of the polyline. The p
- **`marker-mid`** — Two values, both correct and distinct. [0] none renders a bare polyline. [1] url(assets/photo.jpg) places blue marker dots at the MIDDLE (interior) vertices of the polyline, correctly demonstrating ma
- **`marker-start`** — Two values, both correct and distinct. [0] none renders a bare polyline. [1] url(assets/photo.jpg) places a blue marker dot at the START vertex (left terminus) of the polyline, correctly demonstrating
- **`mask-border-outset`** — Every value is correct and uniquely demonstrated. The checkerboard mask-border is pushed outward by a clearly different distance for each value: the px values form a visible progression (8px < 16px < 
- **`mask-border-repeat`** — All four values render correctly and distinctly. stretch (0) stretches the slice with no edge tiling, repeat (1) tiles the edge slices producing visible mid-edge repeats, round (2) scales tiles to fit
- **`mask-border-slice`** — The demo applies a checkerboard mask-border source and the slice value visibly governs the border geometry. The fourth (and third) number changes the corresponding edge's slice thickness -- e.g. [1] 1
- **`mask-composite`** — All four compositing operators are correct and clearly distinct. add [0] shows the union of the star and circle masks over the image; subtract [1] removes the second mask from the first; intersect [2]
- **`mask-size`** — Every value is demonstrated correctly at a distinct, value-appropriate mask size. The mask (statue/photo) grows monotonically with the value: 8px is a tiny dot, 16px/24px small, 64px medium, percentag
- **`mask-type`** — Both values render correctly and are clearly distinct. luminance treats the mask by luminance, producing the masked square as a gradient that fades from dark (masked out) to bright red (visible). alph
- **`math-style`** — normal and compact are clearly, correctly distinct. With normal the fraction x^2/y is rendered in display style (taller, larger numerator/denominator with full spacing); with compact the whole formula
- **`object-view-box`** — All five values render correctly and distinctly. [0] none shows the full source image (dog, upright). [1] inset(...round...) reframes/pans the view box with visible rounded corners. [2] rect(8px,56px,
- **`order`** — Single-row flex demo with one highlighted item; each of the 5 order values (2,1,3,5,4) repositions the red item to a clearly distinct slot in the row: order 1 -> first, order 2 -> second, order 3 -> m
- **`outline-offset`** — Each value shows a faint inner control box with the orange outline drawn at the specified offset distance from it. The gap between the outline and the inner box grows monotonically and distinctly acro
- **`outline-style`** — All ten line styles render correctly and distinctly. auto = thin solid focus-ring style; none = no outline; dotted and dashed show their respective patterns; solid = uniform solid; double = two parall
- **`outline-width`** — All 8 values render correctly and distinctly. The outline is drawn outside the box (correct outline behavior) and grows with the value: 8px/16px/24px/48px/64px form a clear thickness progression, 64px
- **`perspective`** — All 6 values render uniquely and correctly. 'none' shows flat text; each px depth (24/48/8/16/64px) produces a visibly distinct degree of 3D foreshortening of the 'perspective' word, with smaller px =
- **`position`** — Every value is correct and uniquely demonstrated via a scroll animation in a scroll container. static: the target box scrolls away with the rows. relative: shifts but scrolls with content. absolute: r
- **`quotes`** — Nested q-element sentence ('A nested quote here end') renders each value distinctly. auto (00): curly double + single English quotes (“nested ‘quote’”). none (01): no marks at all. match-parent (02): 
- **`ruby-align`** — All 4 values render distinctly and correctly. With base text 東京札 + ruby annotation, 'start' left-aligns the annotation per base group (note さつ sits at the left edge of 札 leaving a trailing gap); 'cent
- **`scroll-behavior`** — Both values are correctly and distinctly demonstrated via the temporal filmstrip. 'auto': every frame is already settled at panel 6/7 (instant jump, no animation). 'smooth': frame-00 shows panel 2/3 (
- **`scroll-margin`** — Top-snapping scroll demo (snap-align start). Each of the 5 length values (8px/16px/24px/48px/64px) lands the target panel 4 at a progressively larger offset from the container's top edge: 8px snaps ne
- **`scroll-margin-block`** — Same top-snapping demo as scroll-margin; block axis margin offsets the top snap edge in horizontal-tb. All 5 length values (8px/16px/24px/48px/64px) produce monotonically increasing offsets of panel 4
- **`scroll-margin-block-end`** — block-end in horizontal-tb is the bottom edge; the demo correctly uses bottom-snapping (snap-align end) so the margin has a visible effect. Target panel 4 lands near the container bottom at 8px and is
- **`scroll-margin-block-start`** — block-start in horizontal-tb is the top edge, matching the top-snapping demo. 8px snaps panel 4 flush to the top; larger values (16/24/48/64px) offset it down with panel 3 becoming visible above, in m
- **`scroll-margin-bottom`** — Physical bottom edge; the demo correctly uses bottom-snapping (snap-align end) so the margin is visible. Target panel 4 lands near the container bottom at 8px and shifts progressively upward (panel 5 
- **`scroll-margin-inline`** — Horizontal scroll-snap demo where 'panel 4' snaps to the start edge and scroll-margin-inline offsets the snap position. All 5 length values (8px, 16px, 24px, 48px, 64px) render in a clear, monotonic, 
- **`scroll-margin-inline-end`** — Demo uses end-aligned snapping so the inline-end margin is visible: panel 4 snaps toward the right/end edge and scroll-margin-inline-end offsets that landing. All 5 length values produce a distinct mo
- **`scroll-margin-inline-start`** — Start-aligned horizontal snap demo; scroll-margin-inline-start offsets the snap position from the start (left in LTR) edge. All 5 length values render in a clear, distinct, monotonic progression of th
- **`scroll-margin-left`** — Panel 4 snaps to the left edge and scroll-margin-left offsets the landing position. The 5 length values shift the snapped position monotonically and distinctly (8px = panel 4 leftmost/closest to edge;
- **`scroll-margin-right`** — Demo snaps panel 4 toward the right edge (end-aligned) so scroll-margin-right is visible. All 5 length values offset the right-edge landing distinctly and monotonically (8px = panel 4's right edge nea
- **`scroll-margin-top`** — All 5 length values (8px, 16px, 24px, 48px, 64px) are demonstrated with a scroll-snap container whose target panel (panel 4) snaps to a progressively larger offset from the top edge. The final-frame s
- **`scroll-snap-align`** — Temporal demo: a scrollable panel list with a target panel (▶ panel 4); pressing 'snap' settles the scroll, and each of the 4 values lands the target at a different alignment that is correct and visua
- **`scroll-timeline-name`** — Two values, both correctly and distinctly demonstrated as temporal filmstrips. [0] none: the scroll-driven pill bar stays pinned at the far left and stays red across all frames (no named timeline atta
- **`scrollbar-gutter`** — The three values produce three visibly distinct layouts in the full-res stills. auto: scrollbar overlays, panels extend nearly to the bar. stable: a gutter is reserved on the right (panels stop short 
- **`scrollbar-width`** — All three values are correct and visually distinct in the full-res frames. 'auto' shows a normal-width scrollbar with a wide track channel to the right of the panels. 'thin' renders a narrower scrollb
- **`stroke-linecap`** — Demo draws a dashed M so dash-end caps are visible. butt shows separated dashes with flat, square-flush ends; round shows separated pill/capsule dashes with clearly rounded ends; square shows caps ext
- **`stroke-miterlimit`** — The demo strokes a zigzag SVG path whose peaks have varying angle sharpness, so each miterlimit threshold beveling a different set of joins. value 4 (default): sharpest peak beveled, rest mitered. val
- **`tab-size`** — The sample text contains a literal tab between 'Col' and the rest. Numeric values (0.25, 0.5, 0.75, 1, 2) set the tab stop in character widths and px values (8px, 16px, 24px, 48px, 64px) set it absolu
- **`table-layout`** — Both values are correct and visually distinct. 'auto' sizes columns to content (second column widens to fully contain 'Supercalifragilistic', table grows to fit). 'fixed' forces equal 50/50 columns ig
- **`text-align-last`** — Excellent, well-designed demo. Each value is shown in TWO side-by-side panels -- a left LTR panel and a right RTL panel -- so direction-relative keywords are cleanly separable from physical ones. auto
- **`text-anchor`** — All three SVG text-anchor values are rendered correctly and are clearly distinct against the dashed vertical reference line: start (0) places the text beginning at the line and extending right, middle
- **`text-box-trim`** — All 4 values (none, trim-start, trim-end, trim-both) render as 4 distinct, correct boxes. The 'Codex 文字 / Ag' specimen boxes clearly show: none keeps full leading top and bottom, trim-start removes th
- **`text-decoration-inset`** — Experimental property (<length>{1,2} | auto). All 6 values render as 6 distinct, correct underlines: 8px sits close to the text baseline, and 16px/24px/48px/64px progressively push the underline furth
- **`text-decoration-style`** — All five styles render correctly and are visually distinct: solid (a single continuous line), double (two parallel lines), dotted (a row of dots), dashed (a row of dashes) and wavy (a zigzag). Each is
- **`text-emphasis-style`** — Every style renders a distinct, correct emphasis glyph above the specimen text: [0] dot (small filled dot), [1] circle (larger filled circle), [2] double-circle, [3] triangle (filled triangle), [4] se
- **`text-orientation`** — All 3 values are correct and clearly distinct in the vertical-writing sample. mixed (0): CJK upright while Latin digits '2024'/'12' are rotated 90deg (laid sideways), the spec default. upright (1): ev
- **`text-shadow`** — [0] none shows clean white text with no shadow. The other 49 values each apply a red (#c5483c) shadow with a unique combination of offset-x (8/24/48px), offset-y (8/16/24/48/64px) and blur radius (non
- **`text-underline-offset`** — All 20 values render correctly and distinctly. 'auto' sits the underline tight to the baseline. The <length> values push the underline progressively further below the text (8px < 16px < 24px < 48px < 
- **`text-wrap-mode`** — wrap (0) wraps the long sample text into multiple lines inside the box; nowrap (1) keeps it on a single line that overflows the box. The two values are correct and clearly visually distinct.
- **`transform-style`** — Both values are correct and clearly distinct. In the 'flat' cell the child 3D badge is flattened onto the card's plane (appears as a flat yellow rectangle lying on the card face with the '3D' label re
- **`transition-behavior`** — Temporal (3-frame) demo of a box that toggles a discrete property (display/visibility). normal: at the mid-transition frame the 'Aa' pill has already vanished because the discrete property flips insta
- **`transition-delay`** — All three delay values are correctly and distinctly demonstrated. Full-res frame-02: '1s' shows the box still a sharp dark square at its far-left START position (delay not yet elapsed, transition not 
- **`transition-duration`** — Both duration values are correct and visually distinct. Full-res frame-01: '0.3s' shows the box having COMPLETED its transition (full circle, final position) because the short duration finishes quickl
- **`transition-property`** — Every value correctly and distinctly demonstrates which property interpolates while the box toggles. Mid-flight frames make each effect unmistakable: opacity fades the whole ellipse, transform catches
- **`transition-timing-function`** — All 10 easing values are valid and each produces a distinct, correct interpolation snapshot. The 3-frame filmstrip captures the box at different points along each curve plus a top progress bar: linear
- **`translate`** — Demo stages a tall box inside a 3D scene with a perspective floor-grid. translate's X (fixed 24px), Y (24/48/8/16/64px and 50/80/30/65% etc.) and Z (3rd component) are all visibly demonstrated. Crucia
- **`unicode-bidi`** — All six values are correct and visually distinct. Two direction:rtl spans inside an LTR frame are shown. normal (5 ABC 123 | 123 ABC 9), embed (boundary numbers reordered: 5 | shalom ABC 123), isolate
- **`view-timeline-name`** — Both values are correct and clearly distinct. none means no named timeline is registered, so the subject's animation has no timeline to bind to and the pill stays in its initial state (red, unmoved) a
- **`writing-mode`** — All 5 values are correct and visually distinct. horizontal-tb (0): normal horizontal lines top-to-bottom. vertical-rl (1): vertical lines flowing right-to-left (first line on the right), CJK upright, 
- **`z-index`** — Exemplary demo. A red positioned box sits among a blue staircase of boxes labeled 1-5; the red box's z-index decides how many blue layers it covers. auto = red behind all 5 blue boxes; 1 = red covers 

## Category 3 — Perfect (inherent) — most values unique; a few render identically by spec/engine (unfixable, correct)  (365)

- **`accent-color`** — The demo tints a checkbox, radio button and two range sliders with the accent-color value, and lists the value in a right-hand legend. auto, both hex colors, all standard and relative-color functions 
- **`align-content`** — The demo correctly drives a wrapped flex container and the cross-axis packing is genuinely visible: space-between (rows top/middle/bottom, max gaps), space-around (equal space around each row), space-
- **`align-items`** — The demo is a horizontal flex row with four items of differing intrinsic heights and is well-built and correct: start[20] aligns items to the top, end[21] to the bottom, center[19] to the middle, stre
- **`align-self`** — This is the strongest demo of the three: a single target item (marked *) is left at auto cross-size inside the flex row, so the alignment FAMILIES are all clearly distinct — auto/normal/stretch make t
- **`alignment-baseline`** — SVG alignment-baseline demo: a red 'bg' tspan inside an 'A...x' SVG text run, with a drawn baseline line. ideographic (raised), middle, central, mathematical (lowered to distinct positions) and hangin
- **`all`** — The demo resets the sample 'Specimen' element with each global keyword. initial = default serif/no color; inherit = green (inherits parent green); unset = green (color is an inherited property, so uns
- **`animation-range`** — Demo is a scroll-driven 3-frame filmstrip: a 'subject' pill animates its colour/gradient as the inner panel scrolls, and animation-range controls which slice of the scroll timeline drives the animatio
- **`animation-range-end`** — Real view-timeline / scroll-driven demo: a 'subject' box crosses the scrollable panel and an animated pill's HUE (and slight x-position) encodes the keyframe progress. animation-range-end shifts WHERE
- **`animation-range-start`** — Identical demo design to animation-range-end and behaves correctly: animation-range-start shifts WHERE in the view-progress range the animation BEGINS, so the percentages and the cover/contain named-r
- **`animation-timeline`** — Scroll-driven demo: a pill (animation target) is driven by the selected timeline and the panel scrolls under it. The effect is clearly visible AND several values are distinct in the still: auto -> blu
- **`aspect-ratio`** — The demo is correct: it fixes one axis of the Specimen box and lets aspect-ratio drive the other, so ratio 1/1 renders a square, 1/0.5 (=2:1) renders a wide box, 1/2 (=0.5) renders a tall box, and bar
- **`backdrop-filter`** — The demo applies backdrop-filter to a translucent frosted panel layered over a colourful conic gradient, so the filtered backdrop is visible. blur[1], brightness[2], contrast[3], grayscale[5], hue-rot
- **`background-clip`** — The box-clip values are correct and distinct: content-box (0) clips the gradient to a small inset box clearly separated from the dashed border, padding-box (1) extends the gradient to the inner border
- **`background-color`** — The bulk of values render correctly and distinctly: hex (#c5483c, #2f5fd0), rgb/rgba (incl. the near-black rgb(1,0.5,2,.75) which correctly clamps to almost black, and the lavender relative-color rgb(
- **`background-image`** — 12 of 13 values render correctly and are clearly distinct: url(), image(ltr url()), cross-fade(), element(#alpha) and light-dark() all show the photo; the five gradients (linear, repeating-linear stri
- **`background-position`** — Keyword positions left/center/right/top/bottom and all length/percentage/math values render correctly and distinctly (left flushes the image left, center centers it, 24px/48px shift it progressively f
- **`background-repeat`** — repeat-x (tiles horizontally), repeat-y (tiles vertically), repeat (tiles both axes), round (tiles with rescaling so whole tiles fit) and no-repeat (single image) all render correctly and are clearly 
- **`background-size`** — Every value is valid CSS for background-size and renders correctly. The <length> values scale the background image to the right size (24px = tiny thumbnail, 48px/64px progressively larger), the <perce
- **`baseline-shift`** — The demo renders the inline text 'A bg x' and shifts the middle 'bg' run by the baseline-shift value relative to the baseline guide. Every length (24px/48px/8px/16px/64px) and every percentage (50/80/
- **`baseline-source`** — Demo uses a 2-line inline-block ('first'/'last' red box) sitting in the line 'ab [box] cd'. baseline-source:first lowers the box so the text aligns to the box's FIRST baseline (box hangs lower, 'ab'/'
- **`block-size`** — The demo varies the box's block (vertical) dimension against a dashed percentage-reference container. Lengths (24/48/8/16/64px), percentages (15/30/50/65/80/100%), auto, stretch (fills container), fit
- **`border`** — All standard CSS color values render correctly and distinctly as the border color (hex, rgb/rgba, hsl/hsla, hwb, lab/lch, oklab/oklch, relative from-color, and the named colors 23-49 all show clearly 
- **`border-block`** — Identical value list and behavior to border, applied to both block edges (top+bottom). All standard color values render correctly and distinctly on the block borders. The same six values 17-22 (ictcp,
- **`border-block-color`** — Color-only value list; sets the block-start and block-end border colors (top+bottom). All standard colors render correctly and distinctly on the demo box. The same six Chrome-unimplemented but doc-val
- **`border-block-end`** — Shorthand sets width/style/color on the block-end (bottom) edge only. All standard color values render correctly and distinctly on that single edge. The same six doc-valid but Chrome-unimplemented col
- **`border-block-end-color`** — Color-only; sets the block-end (bottom) border color. High-res contact sheet confirms all standard colors render correctly and distinctly on the bottom edge. The same six doc-valid but Chrome-unimplem
- **`border-block-end-style`** — All 10 border-style keywords are valid and correctly rendered on the block-end (bottom) edge: solid, dotted, dashed, double, groove, ridge, inset and outset each render with their distinct line treatm
- **`border-block-start`** — border-block-start colors the block-start (top, in horizontal-tb) edge with the given color. 44 of 50 values render correctly and distinctly: hex (0,1), legacy/modern rgb & rgba including relative-col
- **`border-block-start-color`** — border-block-start-color sets only the block-start (top) edge color. 44 of 50 values render correctly and distinctly (hex, rgb/rgba incl relative, hsl/hsla incl relative, hwb, lab, lch, oklab, oklch, 
- **`border-block-start-style`** — All 10 border-style keywords are valid and correctly rendered on the block-start (top) edge: solid, dotted, dashed, double, groove, ridge, inset and outset each render with their distinct line treatme
- **`border-block-style`** — All ten line styles render correctly: solid (filled edge), dotted, dashed, double, groove, ridge, inset, outset are each visually distinct on the box. [1] none and [2] hidden both render with no visib
- **`border-bottom`** — border-bottom shorthand (24px solid <color>) colors the bottom edge. 44 of 50 values render correctly and distinctly (hex, rgb/rgba incl relative, hsl/hsla incl relative, hwb, lab, lch, oklab, oklch, 
- **`border-bottom-color`** — border-bottom-color sets only the bottom edge color. 44 of 50 values render correctly and distinctly (hex, rgb/rgba incl relative, hsl/hsla incl relative, hwb, lab, lch, oklab, oklch, plus all 27 name
- **`border-bottom-left-radius`** — The demo correctly rounds the bottom-left corner of the sample box. The px values (8/16/24/48/64) and percentage values (15/30/50/65/80/100%) are all correct and clearly distinct, with 100% producing 
- **`border-bottom-right-radius`** — The demo correctly rounds the bottom-right corner of the sample box (mirror of border-bottom-left-radius). The px values (8/16/24/48/64) and percentage values (15/30/50/65/80/100%) are all correct and
- **`border-bottom-style`** — All values render correctly: solid, dotted, dashed and double are clearly distinct; groove/ridge/inset/outset each show a different 3D bevel (groove dark-top, ridge light-top, inset pressed-in, outset
- **`border-color`** — border-color (single value) colors all four edges. 44 of 50 values render correctly and distinctly — the full box outline takes a different color for hex, rgb/rgba incl relative, hsl/hsla incl relativ
- **`border-end-end-radius`** — All 18 values render correctly: the end-end corner (bottom-right in LTR) rounds, with 24px a small round and 100% a full quarter-circle, and the percentages (15/30/50/65/80/100%) and px values are all
- **`border-end-start-radius`** — All 18 values render correctly: the end-start corner (bottom-left in LTR) rounds, 24px a small round and 100% a full quarter-circle; px and percentage values are all distinct. Identical structure to b
- **`border-image`** — All four repeat modes (stretch/repeat/round/space) are clearly distinct: stretch shows wide stretched stripes, repeat shows many thin tiled stripes, round tiles into whole-tile rows/cols, space tiles 
- **`border-image-slice`** — Strong demo. The fill keyword is clearly demonstrated (fill keeps the middle image region as the box background; no-fill leaves the center empty) and the various slice numbers/percentages produce visi
- **`border-image-source`** — url()/gradient sources render real border-images distinctly; the image()/element()/unprefixed cross-fade() <image> forms are valid CSS that Chrome does not implement, so they produce no border-image (
- **`border-image-width`** — Excellent demo. All 24 values render correctly using a striped border-image. px values 8-64px progressively thicken the border-image region (64px fills the box); percentages 15/30/50/65/80/100% are di
- **`border-inline`** — Shorthand colors both inline borders (left+right). 44 of 50 values render correctly and distinctly: hex, rgb/rgba/hsl/hsla/hwb/lab/lch/oklab/oklch (including relational 'from' forms) and the named col
- **`border-inline-color`** — Colors both inline-side borders. 44 of 50 values render distinct inline-border colors (hex, rgb/rgba/hsl/hsla/hwb/lab/lch/oklab/oklch incl. relational 'from' forms, and named colors). The same six val
- **`border-inline-end`** — Shorthand colors only the inline-end (right in horizontal LTR) border; the single-side rendering is correct and distinct from the -start variant. 44 of 50 values paint a distinct end-side border color
- **`border-inline-end-color`** — Colors only the inline-end (right) border; rendering is correct and distinct from the -start-color variant. 44 of 50 values render distinct end-side border colors (hex, rgb/rgba/hsl/hsla/hwb/lab/lch/o
- **`border-inline-end-style`** — The box has a thick inline-end (right) border whose line-style varies. All visually-meaningful styles render correctly and distinctly: 0 solid, 3 dotted, 4 dashed, 5 double (two parallel lines), 6 gro
- **`border-inline-start`** — Shorthand colors only the inline-start (left in horizontal LTR) border; the single-side rendering is correct and distinct from the -end variant. 44 of 50 values paint a distinct start-side border colo
- **`border-inline-start-color`** — Demo box shows a base reddish-brown border on all sides with the inline-start (left) edge overridden by the tested value. Indices 0-16 (hex, named, rgb/rgba/hsl/hsla/hwb/lab/lch/oklab/oklch incl. rela
- **`border-inline-start-style`** — The demo shows a box whose inline-start (left) edge takes the line-style while the other edges keep a thin default border. Full-res screenshots confirm every style is rendered correctly and distinctly
- **`border-inline-style`** — Shorthand that styles both inline edges (start+end). The demo box shows left+right borders in the chosen style. Full-res screenshots confirm each style renders correctly and distinctly: solid, dotted,
- **`border-left`** — Shorthand demo: box has a thin base border with a thick 24px left border set to the tested value. Indices 0-16 and 23-49 render distinct thick colored left borders correctly (0 -> reddish-brown, 32 ->
- **`border-left-color`** — Identical demo and behavior to border-inline-start-color: base reddish-brown border with the left edge overridden by the tested color. Indices 0-16 and 23-49 render distinct, correct left-edge colors 
- **`border-left-style`** — Every style is rendered correctly on the LEFT edge of the sample box (top/right/bottom stay solid as a base): solid, dotted (visible dots), dashed, double (twin lines), groove/ridge/inset/outset (dist
- **`border-radius`** — All 50 values render correctly on a solid rounded box, and the px/percentage variations of the bottom-left corner (the only corner that changes across the set) are clearly visible and distinct (e.g. [
- **`border-right`** — Shorthand demo mirroring border-left but applied to the right edge: box has a thin base border with a thick 24px right border set to the tested value. Indices 0-16 and 23-49 render distinct thick colo
- **`border-right-color`** — Identical demo and behavior to border-inline-start-color but on the right edge: base reddish-brown border with the right edge overridden by the tested color. Indices 0-16 and 23-49 render distinct, co
- **`border-right-style`** — Every line style renders correctly and distinctly on the right edge: solid, dotted, dashed, double, groove, ridge, inset, outset are all clearly different (verified full-res for dotted vs groove). The
- **`border-start-end-radius`** — Demo rounds the block-start/inline-end corner (top-right in LTR horizontal-tb). All px values (8/16/24/48/64px) show progressively larger corner rounding and the percentage values (15/30/50/65/80/100%
- **`border-start-start-radius`** — Demo rounds the block-start/inline-start corner (top-left in LTR horizontal-tb), confirmed by the 100% cell showing a quarter-ellipse on the top-left corner. px values grow the corner progressively an
- **`border-style`** — solid, dotted, dashed, double, groove, ridge, inset and outset all render visibly distinct border treatments (groove vs ridge show inverted bevels; inset vs outset show inverted shading). The only col
- **`border-top`** — The shorthand applies 24px solid <color> and the TOP border of the sample box reflects each color. All concrete colors render correctly and distinctly: hex (0,1), rgb/rgba including the relative-color
- **`border-top-color`** — Same value list as border-top but isolating the color alone; the sample box's top border takes each color while the other three sides stay the default red. All concrete colors render correctly and dis
- **`border-top-left-radius`** — Demo rounds the top-left corner, confirmed by the 100% cell showing a quarter-ellipse on the top-left. px values grow the corner progressively and percentages curve it increasingly, all correct and di
- **`border-top-right-radius`** — Demo rounds the top-right corner, confirmed by the 100% cell showing a quarter-ellipse on the top-right (mirror of the top-left prop). px values grow the corner progressively and percentages curve it,
- **`border-top-style`** — Every value renders correctly: solid (single line), dotted (dotted top edge), dashed, double (two parallel lines), groove (3D sunken-groove top), ridge (3D raised-ridge top), inset (dark sunken top ed
- **`bottom`** — Demo is correct: a red box is offset up from the bottom edge of a frame by the resolved bottom value, and the offset grows as the value grows. auto sits at the top, pixel/percentage values produce vis
- **`box-align`** — box-align (legacy -webkit-box cross-axis alignment) is correctly demonstrated: 'start' puts items at the top of the container, 'center' vertically centers them, 'end' puts them at the bottom, and 'str
- **`box-flex-group`** — All five values (2,1,3,5,4) render pixel-identically: a single red box on the left plus an empty 'g3' box, with the exact same layout regardless of the group number. box-flex-group only has a visible 
- **`box-lines`** — single and multiple render identically (three boxes in one row, fitting the container). box-lines was a proposed property allowing box children to wrap to multiple lines, but it was dropped from the s
- **`box-orient`** — horizontal lays the three boxes side by side; vertical stacks them top to bottom; inline-axis renders identically to horizontal; block-axis renders identically to vertical. All four are correct. The t
- **`break-after`** — Empirically tested in headless Chrome (the shoot engine): the distinct break values (page/avoid/always) render distinctly in the printed demo; the rest (recto/verso/region/avoid-region/column synonyms
- **`break-before`** — Empirically tested in headless Chrome: distinct break values render distinctly; unimplemented synonyms inherently collapse to auto. Inherent, not demo-fixable.
- **`break-inside`** — Paged-PDF demo with a bottom multicol block holding a tall Section II-star card. Two distinct renders: {auto,avoid-page} = the star card SPLITS across the column boundary (md5 eed41f8); {avoid,avoid-c
- **`caret`** — Every one of the 50 values renders the SAME caret: a thin default-colored vertical bar that blinks (visible in frame-01, absent in frame-00). Neither the caret-color component (#c5483c red, #2f5fd0 bl
- **`caret-color`** — The demo shows a text input ('Codex') with a blinking caret captured as a 2-frame filmstrip (caret off / caret on). Every color value that Chrome implements renders the caret in the correct, distinct 
- **`caret-shape`** — Full-res frame-01 captures the focused-input caret and the shapes are clearly distinct and correct: bar = thin vertical bar, block = filled block over the 'C', underscore = underline beneath the 'C'. 
- **`clear`** — The demo floats a blue box (left) and a green box (right) and applies clear to a red 'cleared bar'. The Chrome-implemented values render correctly and distinctly: inline-start[0]/left[4] clear only th
- **`clip-path`** — All four basic shapes (circle/ellipse/inset round/polygon) render correctly and the geometry-box reference box visibly scales each shape (content-box gives a small circle, padding-box larger, border-b
- **`color`** — The demo colors the sample text 'Codex specimen — the quick brown fox 0123' with the value and lists it in a right-hand legend. Both hex colors, all standard and relative-color functions (rgb/rgba/hsl
- **`color-interpolation`** — The demo uses an SVG linearGradient whose interpolation color space is driven by color-interpolation. [2] linearRGB is clearly distinct and correct: its blue->yellow gradient has a brighter, lavender/
- **`color-interpolation-filters`** — Demo blurs three RGB bars and shows the color-blend space. linearRGB [2] is clearly distinct: its blurred boundaries are brighter and wider than auto/srgb. auto [0] and srgb [1] render pixel-identical
- **`color-scheme`** — The demo correctly tints the native form controls (checkbox, range slider, button, panel) for the chosen scheme, and the two genuinely distinct states render perfectly: the light state shows a white p
- **`column-count`** — Column counts 2 (idx1), 3 (idx2), 5 (idx4) and 4 (idx5) each render as the correct distinct number of newspaper columns in the effect panel. The remaining three values all genuinely resolve to a singl
- **`column-fill`** — auto (idx 0) clearly distinct: fills the first column to the full container height (long left column, short right column, text overflowing). balance (idx 1) and balance-all (idx 2) render identically:
- **`column-gap`** — The length and percentage values are correct and clearly distinct: 8/16/24/48/64px show increasing horizontal gaps, and the large percentages (50%/65%/80%/100%) push items onto additional rows while 1
- **`column-rule`** — The column-rule effect panel draws the gutter rule in the specified color and the valid color forms render distinctly: #c5483c reddish (0), #2f5fd0 blue (1), rgb(50% 80% 30%) green (2), the relative r
- **`column-rule-color`** — Identical analysis to column-rule but isolating the color channel. Pixel sampling of the effect-panel rule confirms valid forms are applied and distinct: #c5483c red (0), #2f5fd0 blue (1), rgb(50% 80%
- **`column-rule-style`** — Chrome implements the new Multicol L2 multi-value list / repeat() syntax for column-rule-style, and the demo renders it correctly and distinctly. The single keywords (0-9: solid/none/hidden/dotted/das
- **`column-width`** — auto (0) shows a single full-width column; the five length values 24px/48px/8px/16px/64px (1-5) all correctly demonstrate column-width — narrower widths yield more, thinner columns and wider widths yi
- **`column-wrap`** — All three values (auto, nowrap, wrap) render an identical single-row three-column layout. column-wrap is an experimental CSS Multi-column Level 2 property that Chrome does not implement, so the engine
- **`columns`** — The columns shorthand is demonstrated correctly. The <'column-count'> part varies the number of columns (2/3/4/5 each render the right distinct count) and the <'column-height'> part after the slash is
- **`contain`** — The effect panel (left) collapses the 'Codex specimen' card whenever the value includes size/inline-size containment, and shows full content otherwise; the right panel highlights the active chip. Corr
- **`contain-intrinsic-block-size`** — The demo correctly sizes a placeholder box's block (height) axis by the intrinsic value: the box grows taller from a thin bar (none/auto none) through 8/16/24/48/64px, clearly visible in the dashed tr
- **`contain-intrinsic-height`** — Same demo and behavior as contain-intrinsic-block-size (height is the block axis here). The placeholder box height scales correctly with the value from a thin bar up to a tall box at 64px; all values 
- **`contain-intrinsic-inline-size`** — The demo correctly sizes the placeholder box's inline (width) axis: the box widens from a narrow sliver (none/8px) up to a wide box at 64px. All 12 values are correct CSS. As with the other contain-in
- **`contain-intrinsic-size`** — The shorthand correctly sizes the placeholder box in BOTH axes: the box grows from a tiny dot (none/auto none) into a larger square as the value increases through 8/16/24/48/64px. All 12 values are co
- **`contain-intrinsic-width`** — The demo correctly sizes the placeholder box's width: from a narrow vertical sliver (auto 8px) up to a wide box at 64px, clearly visible in the dashed track. All 12 values are correct CSS. As with the
- **`container`** — The demo uses a JS indicator panel that lights up which container-query features are active: W (inline-size axis), H (block-size axis), S (scroll-state), and name rows A (my-ident) / B (tag-a) / NA (m
- **`container-type`** — Demo uses a 4-row probe card: H=block-size axis fires (green) only when the value includes full 'size', S=scroll-state fires (amber) only when the value includes scroll-state, A is a negative-control 
- **`content`** — The demo swaps a sample box's content with each value. Two structural facts make most values render identically, and neither is fixable by a better demo or a grammar change. (1) Every value of the for
- **`content-visibility`** — Temporal scroll demo. hidden [2] correctly skips painting BLOCK 2's contents (the colored box shows but its 'BLOCK 2 . hidden' label/text is not rendered). visible [0] and auto [1] both paint the on-s
- **`corner-block-end-shape`** — Demo shapes the block-end (bottom) corners of a box. round[0], scoop[1], bevel[2], notch[3], square[4], squircle[5] all render correctly and distinctly on the bottom edge (rounded / concave scoop / fl
- **`corner-block-start-shape`** — Demo shapes the block-start (top) corners of a box. round[0], scoop[1], bevel[2], notch[3], square[4], squircle[5] all render correctly and distinctly on the top edge. The only collision is round[0] v
- **`corner-bottom-left-shape`** — Demo shapes only the bottom-left corner (other three corners stay default-rounded). round[0], scoop[1], bevel[2], notch[3] (full-res confirms a clean rectangular notch only on the bottom-left), square
- **`corner-bottom-right-shape`** — Demo shapes only the bottom-right corner. round[0], scoop[1], bevel[2], notch[3], square[4], squircle[5] all render correctly and distinctly on that single corner. The only collision is round[0] vs su
- **`corner-bottom-shape`** — Demo shapes both bottom corners (shorthand for bottom-left + bottom-right). round[0], scoop[1], bevel[2], notch[3], square[4], squircle[5] all render correctly and distinctly on the bottom edge. The o
- **`corner-end-end-shape`** — Experimental property (MDN: Experimental, Limited availability). The block-end inline-end corner (bottom-right in LTR) of the pill changes shape. Five values render correctly and distinctly: round (0)
- **`corner-end-start-shape`** — Experimental property. The block-end inline-start corner (bottom-left in LTR) changes shape. Five values render correctly and distinctly: round (0), scoop (1) concave bite, bevel (2) flat chamfer, not
- **`corner-inline-end-shape`** — Experimental shorthand affecting the entire inline-end edge = BOTH inline-end corners (top-right and bottom-right in LTR), so scoop/notch produce a more dramatic two-corner silhouette. Five values ren
- **`corner-inline-start-shape`** — round/scoop/bevel/notch/square/squircle each render a clearly distinct corner geometry on the inline-start corner (rounded arc, concave scoop, flat bevel cut, square notch, sharp square, flattened squ
- **`corner-left-shape`** — Identical demo family to corner-inline-start-shape. round/scoop/bevel/notch/square/squircle each render a distinct, correct corner geometry on the left corner. The sole collision is superellipse(1), w
- **`corner-right-shape`** — Affects the two right corners only. round, scoop, bevel, notch, square and squircle each render a clearly distinct right-corner shape (verified by zooming the top-right corner in full-res). superellip
- **`corner-shape`** — Affects all four corners. round (smooth arc), scoop (4-point concave star), bevel (cut/hexagon), notch (plus-shaped bites), square (sharp), and squircle (flatter, boxier rounding) are all clearly dist
- **`corner-start-end-shape`** — Affects a single logical corner (block-start/inline-end). round, scoop, bevel, notch, square and squircle each render a distinct single-corner shape. superellipse(1) renders identically to round becau
- **`corner-start-start-shape`** — Affects a single logical corner (block-start/inline-start). round, scoop, bevel, notch, square and squircle each render a distinct single-corner shape. superellipse(1) renders identically to round bec
- **`corner-top-left-shape`** — Affects only the top-left corner; verified by tightly cropping that corner in full-res. round, scoop, bevel (diagonal cut), notch (rectangular bite), square (sharp) and squircle (flatter rounding) eac
- **`corner-top-right-shape`** — round/scoop/bevel/notch/square/squircle each render correctly & distinctly on the corner (verified); only the superellipse() function form is unimplemented in this Chrome build and falls back to round
- **`corner-top-shape`** — Both top corners show each keyword unambiguously (round/scoop/bevel/notch/square/squircle distinct); only superellipse() is Chrome-unimplemented and aliases to round. Inherent.
- **`counter-increment`** — The demo prints the running 'my-ident · tag-a' counter pair per row, so each increment value yields a unique numeric tally (my-ident 2 -> 2/4/6/8, my-ident 3 -> 3/6/9/12, tag-a 5 -> 0/5/0/10... etc.) 
- **`counter-reset`** — Sophisticated, correct demo. The left panel shows two counters: tag-a values reset the left dot-list counter, my-ident values reset the right arrow-list counter, so the two counter names are visibly s
- **`counter-set`** — Like counter-increment, the demo prints the running my-ident · tag-a pair, and each set value yields a distinct numeric tally (my-ident 2 sets to 2 then +1/row -> 3/4/5/6, my-ident 3 -> 4/5/6/7, etc.)
- **`cx`** — The demo is an SVG circle whose horizontal center (cx) is set by the value, with a blue stroke marking the position. Small lengths (8/16/24px) place the center near the left edge so the circle is clip
- **`cy`** — Identical demo to cx but the circle's vertical center (cy) is controlled. Small lengths place the center near the top (clipped to bottom half); larger lengths and percentages move it down to the botto
- **`display`** — The effect panel uses a rich specimen (running prose with an inset box, table-ish cells, ruby annotation) and most values render distinctly and correctly: block vs inline placement, flow-root containm
- **`dominant-baseline`** — Text 'Agжкрç' is shown against a fixed dotted reference line; the glyph block shifts vertically per baseline. Distinct: auto/alphabetic (line at glyph baseline), ideographic [3] (line below descenders
- **`fill`** — The demo fills an SVG star with the given paint. The hex colors, legacy/modern rgb/rgba, hsl/hsla, hwb, and the relative-color lab/lch/oklab/oklch functions all render distinct, correct colours (e.g. 
- **`fill-opacity`** — All values are valid and the bright red star on a dark background makes the opacity gradations clearly readable (0.25/15% are faint, 0.5/50% medium, 0.75/0.8 strong, 1/100% solid). The only collisions
- **`flex`** — Every value is correct CSS. The demo highlights the first flex item (flex: <value>) among 4 siblings; its final width tracks the flex-basis. Genuinely distinct values render correctly: none / content 
- **`flex-basis`** — Well-built demo: one target item sized by flex-basis with flex-grow:0, so the basis maps directly to rendered width. content/auto = content width; the px values (8/16/24/48/64) and percentages (15/30/
- **`flex-flow`** — flex-flow is the shorthand for flex-direction + flex-wrap. Every one of the 31 generated values is valid CSS and renders exactly what it should: the demo correctly shows row/row-reverse/column/column-
- **`float`** — The demo floats a red box and wraps prose around it. The Chrome-implemented values render correctly and distinctly: inline-start[2]/left[8] float the box to the left with text wrapping on its right; i
- **`flood-color`** — The demo paints the feFlood rectangle with the given colour. Hex, legacy/modern rgb/rgba, hsl/hsla, hwb and the relative-color lab/lch/oklab/oklch functions all render distinct correct colours, and al
- **`flood-opacity`** — A white flood swatch over a checkerboard backdrop reveals opacity correctly and monotonically: 100% opaque white at 1, increasing checkerboard show-through as the value drops (0.75, 0.65, 0.5, 0.3, 0.
- **`font`** — The font shorthand demo varies two axes: line-height (normal / 1 / 0.5 / 2) and font-family. Both are demonstrated: the four line-height groups are clearly distinct (0.5 = overlapping lines, 2 = wide 
- **`font-feature-settings`** — The demo is excellent: the sample 'Quartz Fjord 1/2 ct st fi 0123' exercises every requested OpenType feature and the test font supports all of them. smcp clearly renders SMALL CAPS, swsh adds a swash
- **`font-kerning`** — Demonstration is correct and the meaningful distinction is clearly visible. 'auto'(0) and 'normal'(1) both apply kerning: 'AVATo Wave Ye.' fits on one line with tight A-V pairs. 'none'(2) disables ker
- **`font-language-override`** — normal, "Specimen", and "Aa" all render 'Hamburgefonstiv' identically. font-language-override only changes glyph forms when the font has OpenType locl substitutions for a specific language system AND 
- **`font-palette`** — The effect IS clearly visible: --my-var renders CODEX in blue versus the orange of every other value, proving the COLR/CPAL palette machinery works in this build. light and dark render identically to 
- **`font-size`** — The demo renders the phrase 'Hamburg 0123' in the left effect panel at the selected font-size, with a legend of all values on the right. Sizing is correct across the board: the absolute-size keyword l
- **`font-size-adjust`** — The demonstration is strong: the ratio steps within ex-height, cap-height and ch-width visibly and correctly rescale the sample (e.g. ex-height 2 is huge, ex-height 0.25 is tiny, cap-height 1 is near-
- **`font-smooth`** — font-smooth is explicitly non-standard and NOT implemented by Chrome (Chrome/WebKit uses -webkit-font-smoothing instead, and only on macOS). All 16 cells render the 'Hamburgefonstiv' specimen identica
- **`font-stretch`** — The percentage ladder (15%/30%/50%/65%/80%/100%) and the keyword ladder (ultra-condensed..ultra-expanded) render at progressively different widths of 'Hamburgefonstiv' and are mostly distinct and corr
- **`font-style`** — Sample is 'Hamburgefonstiv' in a monospace-ish serif. normal (upright), italic, oblique 14deg/-14deg/45deg/-45deg/90deg are correctly slanted by their respective amounts and 45/-45/90 are clearly dist
- **`font-synthesis`** — The synthesis effect is clearly demonstrated: none renders plain 'Hamburgefonstiv', while a value enabling all keywords renders 'HAMBURGEFONSTIV' as synthetic bold + oblique + small-caps. Of the 50 ge
- **`font-synthesis-position`** — auto and none render the 'E = mc2' specimen identically. font-synthesis-position only changes rendering when font-variant-position: sub/super is applied to text AND the active font lacks real subscrip
- **`font-synthesis-style`** — auto (synthesize oblique allowed) and oblique-only (synthesize oblique allowed) render an identical slanted 'HAMBURGEFONSTIV' because the sample font has no real italic face and the differentiating we
- **`font-variant`** — The shorthand visibly works: vs normal the demo text 'H2O 1/2 0123 envelope ICJK' gains a real subscript 2 (sub), a diagonal fraction half-glyph, a slashed zero, and full-width CJK. But the 50 values 
- **`font-variant-alternates`** — Demo text 'Gault flu 13469'. Cropping the text region and hashing shows 3 of 8 values produce real (subtle) alternate glyphs distinct from normal: stylistic(alpha) [2], styleset(alpha) [6], character-
- **`font-variant-caps`** — normal [0], small-caps [1], all-small-caps [2] and unicase [5] are clearly distinct and correct. petite-caps [3] renders identical to small-caps, all-petite-caps [4] renders identical to all-small-cap
- **`font-variant-east-asian`** — Well-constructed CJK demo (system CJK font, sample kanji deliberately chosen to span JIS variants plus Latin/digits and a ruby probe). jis78 (and its full-width/proportional/ruby combos) clearly alter
- **`font-variant-emoji`** — emoji (index 2) is correct and clearly distinct — it forces the colorful emoji presentation (color envelope, purple umbrella, blue plane, red heart, larger glyphs). normal (0), text (1) and unicode (3
- **`font-variant-ligatures`** — Effect is unmistakable in the still: demo text 'Quartz fi ffl ct / st long-s-t tz' shows joined fi/ffl ligatures under normal and clearly separated f+i / f+f+l under none. Hashing the demo-text region
- **`font-variant-numeric`** — Strong demo on the sample '0123456789 1/2 3/4 5/8 1st 2nd 3rd'. lining-nums vs oldstyle-nums (figure baselines), proportional-nums vs tabular-nums (digit spacing), diagonal-fractions (real glyphs), or
- **`font-variation-settings`** — Effect is strongly visible: 'wght' 900 renders very bold and 'wght' 25 hairline-thin; 'wdth' 900 is visibly expanded/wide and 'wdth' 25 is dramatically condensed - all clearly distinct from normal. Ho
- **`font-weight`** — After the generator fix (real 100/300/400/700/900 weights on the Codex Flex variable face) every numeric weight renders distinctly thin→black. The only remaining collisions are the CSS spec-mandated e
- **`font-width`** — The demo uses a width-capable (variable) font on the word 'Hamburgefonstiv' and the width axis renders clearly: 15% is visibly narrow, ultra-expanded is visibly wide, and the intermediate percentages/
- **`gap`** — Well-built flex demo: 4 wrapping boxes plus a 'COMPUTED GAP' readout. The first component is row-gap, the second column-gap. Lengths, percentages (which wrap items), and math functions all render and 
- **`grid`** — The grid shorthand sets grid-template (rows/columns). Distinct track sizes render correctly: none/none-none stack items full-width; explicit single-column templates size items to the given track (24px
- **`grid-area`** — Four-value shorthand (row-start / column-start / row-end / column-end) on the named-line demo grid (my-ident lines 1/3/5, tag-a lines 2/4 on both axes). The highlighted (*) item changes position AND s
- **`grid-auto-flow`** — All seven values render correctly: 'row' is sparse (item 4 pushed down leaving gaps), 'row dense' backfills (1,2,4 / 3,5 / 6,7 / 8,9,10), 'column'/'column dense' flow by columns. The collisions are in
- **`grid-column`** — Well-designed grid demo: a 3-column track grid with one red item whose placement/span visibly changes per value. Numeric end-lines (auto/2..auto/5, auto/-1..-3) and span widths (span 2..5) render dist
- **`grid-column-end`** — Single grid-line property setting the item's column END line on the named-line demo grid (my-ident cols 1/3/5, tag-a cols 2/4). The highlighted (*) item widens or repositions horizontally as the end l
- **`grid-column-gap`** — Excellent, correct demo: the horizontal gap between grid columns scales cleanly with the value — 24px close, 64px wide apart, 50% pushes columns far apart (items become thin), 100% pushes them off-scr
- **`grid-column-start`** — Single grid-line property setting the item's column START line on the named-line demo grid (my-ident cols 1/3/5, tag-a cols 2/4). The highlighted (*) item moves horizontally to the indicated column li
- **`grid-gap`** — grid-gap is the row-gap/column-gap shorthand and is demonstrated correctly on both axes: '24px 64px' shows a small row gap with a wide column gap, '24px normal' shows row gap with touching columns (co
- **`grid-row`** — Two-value shorthand grid-row-start / grid-row-end. The demo container (demos2.jsx line ~510) DEFINES named grid lines: grid-template-rows:[my-ident] 30px [tag-a] 30px [my-ident] 30px [tag-a] 30px [my-
- **`grid-row-end`** — Single grid-line property setting the item's row END line. The demo container defines named row lines (my-ident on lines 1/3/5, tag-a on lines 2/4 -- demos2.jsx ~510), so named-line values resolve to 
- **`grid-row-gap`** — All 20 values render correctly: the row gap in the left effect panel scales with the value (24px tight, 64px wide, 100% widest, normal collapses to 0). Percentages (50/80/30/65/15/100) resolve against
- **`grid-row-start`** — Single grid-line property setting the item's row START line. The demo container defines named row lines (my-ident on lines 1/3/5, tag-a on lines 2/4). Verified in full-res: 2 -> row2, 3 -> row3, -1 ->
- **`grid-template`** — All 50 values render correctly. Every value is none/<columns-spec>, so the shorthand drives the explicit COLUMN track while rows stay implicit: items stack into one column whose width tracks the value
- **`grid-template-columns`** — All 50 values render correctly. The value defines a single explicit column track; items stack into that one column and its width tracks the value (none full-width, 24px narrow, 64px wider, 50%/100% wi
- **`grid-template-rows`** — All 50 values render correctly. The grid uses 3 auto columns so items wrap into rows, and the value sets the explicit ROW track height: 24px gives a very short first row, 64px a taller first row, 100%
- **`hanging-punctuation`** — The demo is well-designed (a «Codex...» quote whose opening guillemet would hang into the left margin and whose trailing period/commas would hang past the right edge). All 27 values are doc-valid comb
- **`height`** — Identical demo and behaviour to block-size (height = block-size in the horizontal writing mode used). The box's vertical size tracks each value correctly: lengths, percentages against the dashed conta
- **`hyphenate-limit-chars`** — The hyphenation effect IS visible in every still: the justified text block breaks words with hyphens (sub-ject, com-puter, beauti-ful). But cropping the text block and hashing shows all 6 values - aut
- **`image-orientation`** — All 18 cells render the identical upright pug photo with no rotation or mirroring. Chrome only actually supports two keywords ('none', 'from-image'), and because the test JPG carries no EXIF orientati
- **`image-rendering`** — All 5 values are valid and correctly demonstrated on an up-scaled raster. Chrome renders two genuine visual groups: smooth/bilinear (auto[0], smooth[1], high-quality[2]) and blocky/nearest-neighbor (p
- **`image-resolution`** — image-resolution has essentially no browser support: Chrome does not implement the property at all, so every declaration is treated as a no-op and the image always renders at its intrinsic resolution.
- **`initial-letter`** — The drop-cap demo correctly renders the vast majority of values: the cap-size axis (1.5/2/2.5/3 lines) and the sink axis (sink 1..5 move the letter from raised-at-top down to deeply-sunk) are both cle
- **`inline-size`** — Best-differentiated of the sizing family: the demo varies the box's inline (horizontal) width, so the intrinsic keywords separate clearly: fit-content/fit-content(24px)/calc-size(auto,2em) give narrow
- **`inset`** — Demo is correct: inset insets the box from all frame edges; as the value grows the box moves down-right toward the center (50% centers it, 100% collapses it off-frame), and auto sits at top-left. Pixe
- **`inset-block`** — Demo is correct: inset-block insets along the block axis (top/bottom), so the box shifts vertically as the value grows; auto sits at top, 100% collapses the box off-frame. Pixel and percentage values 
- **`inset-block-end`** — Demo is correct: the box is offset along the block-end edge and shifts vertically as the value grows; auto sits at top. Pixel and percentage values are all visibly distinct, and var()/env() resolve to
- **`inset-block-start`** — Demo is correct: the box is offset from the block-start edge and shifts vertically (downward) as the value grows; auto sits at top. Pixel and percentage values are all visibly distinct, and var()/env(
- **`inset-inline`** — Demo correctly shows an absolutely-positioned red box offset from the inline edges of a frame; auto sits at the start (left) edge and px/% values push it progressively rightward, matching inset-inline
- **`inset-inline-end`** — Demo correctly anchors the box from the inline-END edge: auto sits at static (left) position, small px offsets keep the box's end edge near the right while large percentages (e.g. 80%) push the box to
- **`inset-inline-start`** — Demo correctly anchors the box from the inline-START (left) edge: auto sits at the left, px/% values push the box progressively rightward, and 80% drives it to the far right - the proper mirror of ins
- **`justify-content`** — Strong demo. The base alignment values are correctly and distinctly shown (normal/start/flex-start/left pack left; center centres; end/flex-end/right pack right; space-between/around/evenly spread). C
- **`justify-items`** — The demo is well designed and the meaningful alignments are correct and distinct: [23] center, [24] start, [25] end clearly position the items, and the deliberately oversized 'wide subject overflows i
- **`justify-self`** — Strongly designed demo: a single highlighted subject is deliberately WIDER than its grid cell, so the safe/unsafe distinction is genuinely captured. 'unsafe center' centers and OVERFLOWS the cell (tex
- **`left`** — Demo correctly offsets the box from the LEFT edge: auto at left, px/% values push it rightward (80% to far right). Every value renders correctly. 100%[11] correctly pushes the box fully off the right 
- **`lighting-color`** — The demo tints an feDiffuse/feSpecular-lit rounded panel with the given light colour. Hex, legacy/modern rgb/rgba, hsl/hsla, hwb and the relative-color lab/lch/oklab/oklch functions all light the pane
- **`line-break`** — The property's effect IS visible: 'strict' wraps the CJK specimen differently (small kana kept with the preceding character, shifting every line). However auto/loose/normal/anywhere all produce identi
- **`line-clamp`** — The line-count axis works perfectly: none shows the full paragraph and 1/2/3/4/5 each clamp the specimen to that many lines with a trailing '...' ellipsis, all clearly distinct. However WITHIN each li
- **`line-height`** — The demo shows a fixed-width paragraph of wrapping text; line-height correctly varies the vertical gap between lines. Graduated values (normal, 0.25, 0.5, 0.75, 1, 2 and 8px..64px, 15%..100%) are all 
- **`list-style`** — list-style is the shorthand <position> || <image> || <type>. Two facts collapse the 50 values into ~8 visual buckets, both inherent rather than fixable by a richer demo: (1) when a valid list-style-im
- **`list-style-image`** — The values that Chrome supports are correctly and distinctly shown: url() and image-set() render the photo as the marker, and the five gradient functions (linear, repeating-linear, radial, conic, repe
- **`list-style-type`** — Most values are uniquely and correctly shown: [3] '"Specimen"' prefixes each item with the literal text 'Specimen', [4] '"Aa"' prefixes 'Aa', [5] 'none' shows no markers, and [2] 'symbols(cyclic "Spec
- **`margin`** — Demo shows a red accent box inside a dashed frame; the margin shorthand pushes the box off all four edges, and the box position/frame gap correctly reflects each margin. Distinct values (px, percentag
- **`margin-block`** — Directional demo: margin-block applies to block-start and block-end, so the box is pushed inward from both top and bottom of the dashed frame (frame taller than box, gap above and below). Renders corr
- **`margin-block-end`** — Directional demo: margin-block-end pushes the box away from the block-end (bottom) edge, so the box sits at the TOP of the dashed frame with the gap below. This is correctly distinguished from margin-
- **`margin-block-start`** — Directional demo: margin-block-start pushes the box away from the block-start (top) edge, so the box sits at the BOTTOM of the dashed frame with the gap above. Correctly distinguished from margin-bloc
- **`margin-bottom`** — Directional demo: margin-bottom pushes the box away from the bottom edge, so the box sits at the TOP of the dashed frame with the gap below. Renders correctly and matches spec. Distinct values render 
- **`margin-inline`** — The demo places the accent box inside a dashed frame and margin-inline pushes it off the inline edges; px values (24/48/8/16/64) and percentages (50/80/30/65/15/100%) all render at clearly distinct, c
- **`margin-inline-end`** — Same template/value set as margin-inline; the accent box is offset by margin on its inline-end side. px and percentage values render at distinct, correct horizontal offsets. The math-function values c
- **`margin-inline-start`** — Same template/value set as margin-inline; margin applied to the inline-start side pushes the box across the frame. px and percentage values render at distinct, correct offsets. The math-function value
- **`margin-left`** — margin-left pushes the box rightward by the given amount; px and percentage values render at distinct, correct offsets, and the direction is correctly opposite to margin-right. The math-function value
- **`margin-right`** — margin-right pushes the box leftward (off the left edge of the frame), correctly opposite in direction to margin-left; px and percentage values render at distinct, correct offsets. The math-function v
- **`margin-top`** — Every value renders correctly. The demo shows a dashed containing frame and pushes the accent box down by the resolved margin-top. Distinct pixel values (8/16/24/48/64px) and percentages (15/30/50/65/
- **`margin-trim`** — margin-trim is unimplemented in Chrome/Blink (WebKit-only); the declaration is ignored so every value renders identically to the default. Doc-valid but Chrome-unimplemented — inherent.
- **`mask`** — The composite-operator axis is distinct (add = full circle, subtract = horizontal band cut out, intersect = only the strip band shows, exclude = band excluded) and the mask-clip box axis is distinct (
- **`mask-border`** — The masked checkerboard border renders correctly and is distinct across two axes: the mask-border-width (second slash value 24px/48px/8px/16px) clearly changes border thickness / fill region (verified
- **`mask-border-mode`** — Chrome renders mask-border-mode:luminance and :alpha identically for the demo source — Blink does not distinguish the two modes (mask-border has partial support), so the values inherently collapse. Do
- **`mask-border-source`** — Demo-fix expert (headless Chrome) confirmed: with a valid mask-border-slice/width set, Blink still paints the source unmasked for url()/image-set()/gradient sources here — mask-border masking is only 
- **`mask-border-width`** — The demo masks a striped/checkerboard mask-border-source over a box; every value renders correctly and the genuinely-different widths (8px/24px/48px/64px, the lower percentages 15%/30%, the unitless m
- **`mask-clip`** — content-box [0], padding-box [1] and border-box [2] are all genuinely distinct: content-box clips the mask to the smallest inset region, padding-box slightly larger, and border-box reveals the full re
- **`mask-image`** — url(photo.jpg), the SVG mask, none, and all six gradient forms (linear, repeating-linear, radial, repeating-radial, conic, repeating-conic) render correctly and are all clearly distinct (diagonal stri
- **`mask-mode`** — luminance [1] is clearly distinct (the image is visibly darkened/dimmed because the mask source's luminance modulates opacity). alpha [0] and match-source [2] render identically (both show the full im
- **`mask-origin`** — content-box (idx 0), padding-box (idx 1) and border-box (idx 2) are clearly distinct -- the mask's origin/sizing box grows from the small content area to padding to the full border box (border-box sho
- **`mask-position`** — The large majority of the 50 values are demonstrated correctly and distinctly: left/center/right/top/bottom each move the mask image to the expected edge, the percentage values (15/30/50/65/80/100%) a
- **`mask-repeat`** — The core values are correct and clearly distinct: [0] repeat-x tiles the mask horizontally into a single wide row, [1] repeat-y tiles it vertically into a single tall column, [4] repeat fills the whol
- **`math-depth`** — The demo pairs a fixed white 'A' with a red 'A' whose font-size is driven by math-depth; the red A shrinks monotonically and clearly as depth increases (auto-add largest, then 1, 2, 3, 4, 5 progressiv
- **`math-shift`** — normal and compact render pixel-for-pixel identically. math-shift only changes the vertical shift applied to superscripts inside an already-compact math layout; its effect is imperceptible (and largel
- **`max-block-size`** — Demo clips a tall multi-line text box in the block (height) axis against a dashed reference of the unconstrained extent. Length values (8/16/24/48/64px) clip to a clear number of lines, percentages (1
- **`max-height`** — Identical demo and identical behavior to max-block-size (same height axis). Length values clip to a clear number of lines, percentages give a smooth gradation, all math functions clip to their length 
- **`max-inline-size`** — Effect panel constrains a multi-word red box; the length/percentage and math values render correctly and distinctly: 24px/8px/abs(24px)/round(nearest,24px,2em) are very narrow single-word columns, 48p
- **`max-width`** — Identical demo and behaviour to max-inline-size. Length/percentage/math values (24px, 8px, 48px, 64px, 50%, 80%, clamp, min, max, round, abs, env, anchor-size fallback) render correctly and distinctly
- **`min-block-size`** — Effect is clearly visible: the red box grows taller as min-block-size increases (8px is a thin bar, 16/24/48/64px progressively taller, percentages 15/30/50/65/80/100% scale against the container, str
- **`min-height`** — Identical demo and behaviour to min-block-size (same property semantics in horizontal writing mode). The effect is clearly visible: the box grows taller with larger px and percentage values, and stret
- **`min-inline-size`** — The demo grows a red element's inline width as min-inline-size increases (horizontal writing mode, so inline-size maps to width). Lengths, percentages, and content keywords are clearly distinct (50% h
- **`min-width`** — The demo grows a red element's width as min-width increases. Lengths, percentages, and content keywords are clearly distinct (64px narrow, 50% half, 100% full width, min-content shrink-wraps wrapped t
- **`mix-blend-mode`** — 17 of 18 blend modes render correctly and visually distinct over the conic-gradient backdrop: normal (plain box), darken, multiply (darkened), color-burn (deep red/black), lighten, screen (washed ligh
- **`object-fit`** — Five of seven values are correct and visually distinct: fill [0] stretches the image to the square, none [1] shows the image at intrinsic size cropped to the box, contain [3] letterboxes the full imag
- **`object-position`** — The physical keywords (left, center, right, top, bottom), lengths (24px/48px/8px/16px/64px), percentages (50/80/30/65/15/100%), and math functions (calc, clamp, min, max, round, abs, var, env) all eva
- **`offset`** — The offset shorthand demo holds the path/distance/rotate constant (normal none 24px auto 45deg) and varies only the offset-anchor <position> after the slash, shifting a red arrow marker. Physical keyw
- **`offset-anchor`** — The demo anchors a red marker box to a fixed point on a dashed arc motion path; offset-anchor shifts WHICH point of the box touches that path point. Every IMPLEMENTED value renders correctly and disti
- **`offset-distance`** — Good working demo: a red arrow rides a dashed arc and offset-distance sets how far along it sits. Percentages are clearly distinct and correct (50% sits at the arc apex, 100% at the far end, 15/30/65/
- **`offset-path`** — Demo-fix expert (rendered in real Chrome) confirmed the remaining collisions are <coord-box> reference-box keywords (content-box/padding-box/border-box/fill-box/stroke-box/view-box) appended to ray()/
- **`offset-position`** — Excellent demo: a red arrow marker is placed at the resolved offset-position inside the panel. Physical keywords (left/center/right/top/bottom and their two-keyword combos), lengths (24/48/8/16/64px),
- **`offset-rotate`** — Manually verified (the wave-3 subagent for this batch failed to emit structured output). The offset-path demo orients the marker correctly for every value: auto faces the direction of travel, reverse 
- **`opacity`** — The opacity demo is excellent: a gradient box sits over a busy radial-gradient backdrop, and the distinct numeric/percentage steps (0.75, 0.25, 80%, 30%, 65%, 15%, 0.5/50%) clearly show progressively 
- **`orphans`** — The fragmentation context (page break + split paragraph) IS visible in the still, but all six values render byte-identically: the page break lands in the same place for 1,2,3,4,5 and the var() fallbac
- **`outline`** — All 50 values are '24px auto <color>', so width (24px) and style (auto) are constant and only the color varies. Every named color (24-49: aliceblue..darkgreen) renders a clearly distinct, correct outl
- **`outline-color`** — Same color set as outline, applied as the standalone outline-color on an element that already has an outline. Every named color and every Chrome-supported color function renders a distinct, correct ou
- **`overflow`** — Demo shows a striped content box inside a container. visible (0) lets the striped content spill past the container box; hidden (1) clips it to the box with no scrollbars; scroll (3) clips and always s
- **`overflow-anchor`** — Scroll-anchoring effect: auto adjusts scroll position to compensate when content is inserted above the viewport during a live scroll; none opts out. The temporal filmstrips exist and show the scrollab
- **`overflow-block`** — Same demo and behavior as overflow. visible (0) spills past the box, hidden (1) clips with no scrollbars, scroll (3) clips and shows scrollbars — all distinct and correct. clip (2) renders identically
- **`overflow-clip-margin`** — Every value renders correctly and the demo is genuinely strong: it animates content that overflows a `overflow: clip` box, and overflow-clip-margin controls how far the (diagonally hatched) overflow m
- **`overflow-inline`** — Same demo and behavior as overflow (no overlay value here). visible (0) spills past the box, hidden (1) clips with no scrollbars, scroll (3) clips and shows scrollbars — all distinct and correct. clip
- **`overflow-wrap`** — normal[0] keeps the over-long Latin word whole (it overflows as a single unit). break-word[1] breaks the over-long word at the box edge (supercalifragilist/icexpialidocious), clearly distinct from nor
- **`padding`** — The demo renders a striped padding band around a solid content box; all 18 values render correctly. Fixed lengths (8px/16px/24px/48px/64px) and percentages (15/30/50/65/80/100%) are clearly distinct a
- **`padding-block`** — Correctly demonstrates block-axis padding (top+bottom in horizontal writing mode): the striped band grows on both vertical sides and boxes get taller with larger values. Fixed lengths and percentages 
- **`padding-block-end`** — Correctly demonstrates block-end padding: the striped band appears BELOW the content box (bottom side in horizontal-tb writing mode) and grows with larger values. Fixed lengths and percentages are dis
- **`padding-block-start`** — Correctly demonstrates block-start padding: the striped band appears ABOVE the content box (top side in horizontal-tb writing mode) and grows with larger values. Fixed lengths and percentages are dist
- **`padding-bottom`** — Correctly demonstrates bottom padding: the striped band appears below the content and grows with larger values. Fixed lengths and percentages are distinct. The math-function values are valid CSS resol
- **`padding-inline`** — Demo shows a striped band (padding) around a solid 'content' block; padding-inline applies on both inline sides. Distinct lengths (8/16/24/48/64px) and percentages (15/30/50/65/80/100%) all render cor
- **`padding-inline-end`** — Demo correctly applies padding on the inline-end side only (striped band on the right, content shifted left). Distinct lengths (8/16/24/48/64px) and percentages (15/30/50/65/80/100%, resolved against 
- **`padding-inline-start`** — Demo correctly applies padding on the inline-start side only (striped band on the left, content shifted right). Distinct lengths (8/16/24/48/64px) and percentages (15/30/50/65/80/100%, resolved agains
- **`padding-left`** — Demo correctly applies padding on the physical left side (striped band on the left, content pushed right). Distinct lengths (8/16/24/48/64px) and percentages (15/30/50/65/80/100%, resolved against wid
- **`padding-right`** — Demo correctly applies padding on the physical right side (striped band on the right, content sits at the left). Distinct lengths (8/16/24/48/64px) and percentages (15/30/50/65/80/100%, resolved again
- **`padding-top`** — The demo renders padding-top as a striped band above a solid 'content' block; band height equals the resolved padding. Literal lengths (24px/48px/8px/16px/64px) and all percentages (50%/80%/30%/65%/15
- **`page`** — Paged demo. auto[0] applies no named page, so Section II flows continuously (two pages) -- correct and distinct. my-ident[1] and tag-a[2] each assign a named page to Section II, which correctly forces
- **`page-break-after`** — The demo correctly emulates paged media: 'auto' (0) flows naturally; 'always' (1) forces a page break after Section II's first block, leaving a large empty gap on the page and pushing remaining conten
- **`page-break-before`** — Correctly demonstrated and property-specific (renders differently from page-break-after): 'auto' (0) flows Section II naturally onto page 1; 'always' (1) forces Section II to START at the top of a fre
- **`page-break-inside`** — Excellently demonstrated. 'avoid' (0) keeps Section II as an unbreakable unit: since it cannot fit on page 1 it is pushed entirely to page 2 (page 1 ends after Section I with empty space). 'auto' (1) 
- **`paint-order`** — Excellent triangle demo (blue fill, red stroke, yellow vertex markers). Distinct compositing classes are clearly visible: fill-over-stroke gives a thin red ring (stroke fill [7], stroke markers fill [
- **`perspective-origin`** — The demo shows a 3D platform whose perspective vanishing point shifts with the origin; physical keywords (left/center/right/top/bottom), lengths and percentages all render distinct, correct skews (lef
- **`place-content`** — Effect is unmistakably visible: the demo is a flex container (with LTR and RTL rows) whose items 1-4 are arranged differently per value - space-between pushes them to the edges, space-around/space-eve
- **`place-items`** — The demo applies place-items to a CSS grid containing a 'wide subject' red item that is wider than its track, plus normal items. The align-items (first token, always normal/stretch here) governs the b
- **`place-self`** — This is pure layout alignment (place-self = align-self + justify-self of a grid item), so a still CAN in principle show the effect, and it partially does: the target item's width/extent within its tra
- **`pointer-events`** — Demo correctly shows the effect via a click-counter proxy and selectable chips, but on an HTML (non-SVG) element the SVG-only values all behave identically to auto/all; the only real distinction (none
- **`position-anchor`** — Demo correctly tethers the chip to its named anchor and the effect is shown, but the chip lands at the same position for every value because there is no overflow/repositioning trigger in the still; no
- **`position-area`** — Demo-fix expert confirmed the optimal self-* writing-mode demo is already implemented; the residual self-y-start/center/end span-forms resolve to the SAME physical area as their non-self counterparts 
- **`position-try`** — position-try only applies a fallback when the anchored element would overflow the containing block; in this non-overflowing still the chip stays at its primary position for all 50 values, so only the 
- **`position-try-fallbacks`** — This is the position-try fallback longhand; fallback positions are only resolved when the anchored element overflows, so in this non-overflowing still the chip never moves across all 50 values. The ef
- **`position-try-order`** — position-try-order only changes which fallback is chosen, and only when multiple fallbacks exist and the element overflows. With no overflow the chip sits at the same spot for normal/most-width/most-h
- **`position-visibility`** — Anchor-positioned chip renders correctly, but position-visibility only HIDES the anchored element when its anchor scrolls out of view / overflows the scroll-clip rect. In an on-screen still where the 
- **`right`** — Demo correctly anchors the box from the RIGHT edge: auto at static (left) position; small px offsets keep the box's right edge near the right; large percentages (80%) drive it to the far left; 100%[11
- **`rotate`** — The demo is a real 3D slab (preserve-3d, top red / bottom blue halves, an up-arrow, a green left edge and a pushed-back face) so every axis/angle reads correctly: bare angles spin in 2D, x/y/diagonal 
- **`row-gap`** — Flex demo: a flex container with two rows of items shows the vertical row-gap clearly. normal (small default gap), the pixel values (24/48/8/16/64px) and the percentages (50/80/30/65/15/100%) all rend
- **`ruby-overhang`** — 'auto' and 'spaces' render pixel-identical. ruby-overhang is not implemented in Blink/Chrome (it was never shipped), so both valid keyword values produce the same layout. Even in an engine that suppor
- **`ruby-position`** — The property's effect is clearly visible in the still: 'over' places the ruby annotation (kanji / kana) above the base 漢字, and 'under' places it below - two plainly distinct renderings. The residual c
- **`rx`** — The demo is an SVG rounded rect whose horizontal corner radius (rx) is set by the value. 8/16/24px give increasingly rounded corners; 48px gives a squircle that still has flat top/bottom edges; auto r
- **`ry`** — Identical demo to rx but the vertical corner radius (ry) is controlled. 8/16/24px give increasing vertical rounding; 48px gives a squircle with flat left/right edges; auto resolves to rx (=0) and show
- **`scale`** — Every value is valid and renders correctly: the 'scale' word grows/shrinks proportionally (0.25/15% tiny, 0.5/50% small, 0.75/65%/80% medium, 2 large, etc). The only collisions are inherent CSS equiva
- **`scroll-initial-target`** — The preview is a single static red box identical for none and nearest. scroll-initial-target sets the initial scroll position of a scroll container when first rendered, which is a paintable static sta
- **`scroll-marker-group`** — Documented syntax is 'none | [ [ before | after ] || [ links | tabs ] ]'. The position keyword renders correctly and distinctly: 'none' shows no marker group; 'before' (and before-links/before-tabs) p
- **`scroll-padding`** — The snap-landing position of the red box visibly shifts across the length and percentage values in the temporal filmstrips, so the property effect is clearly demonstrated. The residual collisions are 
- **`scroll-padding-block`** — The demo scrolls a snap container and the highlighted panel 11 lands offset from the red edge by the scroll-padding value; the effect IS visible in the still (small values like 8px/auto land flush nea
- **`scroll-padding-block-end`** — Same logical-direction scroll-padding demo as scroll-padding-block; the snapped panel's offset from the red edge is visible in the still and varies with magnitude. Collisions are inherent: 24px/calc(2
- **`scroll-padding-block-start`** — The snap-offset effect is visible in the still (panel lands offset from the red start edge proportional to the padding). Distinctness collisions are inherent equivalences: the length-function family a
- **`scroll-padding-bottom`** — Physical-axis variant: the snapped panel 11 lands flush at the bottom for auto/8px and shows a visible gap below it for larger values (64px), so the effect is captured in the still. Overlaps are inher
- **`scroll-padding-inline`** — Temporal filmstrip demo: a horizontal scroll-snap container with numbered panels; pressing 'snap' scrolls so the target panel lands offset from the scrollport's inline-start edge by the scroll-padding
- **`scroll-padding-inline-end`** — Temporal filmstrip demo identical in structure to scroll-padding-inline but applying padding to the inline-END edge: the snap target panel lands offset from the end (right, in LTR) edge of the scrollp
- **`scroll-padding-inline-start`** — Temporal filmstrip demo applying padding to the inline-START edge: the snap target panel lands offset from the start (left, in LTR) edge of the scrollport. Correct directional behavior; the target ('p
- **`scroll-padding-left`** — Temporal filmstrip demo applying padding to the physical LEFT edge: the snap target panel lands offset from the left edge of the scrollport. In LTR this matches scroll-padding-inline-start (left==inli
- **`scroll-padding-right`** — Temporal horizontal scroll-snap demo: after 'snap', the marked panel lands offset from the right edge by scroll-padding-right. The animation frames capture the effect well and most values are distinct
- **`scroll-padding-top`** — Temporal (3-frame filmstrip) scroll-snap demo. The snap target (▶ panel 4) lands offset from the red top edge of the scroll container by the scroll-padding-top value, and this is rendered correctly: a
- **`scroll-snap-type`** — The demo adapts the container to the axis and the effect IS visible in the still: none leaves the panels mid-scroll unsnapped (vertical stack), x switches to a horizontal scroll container with the pan
- **`scroll-target-group`** — Both values (none, auto) render an identical static red box with a generic placeholder. scroll-target-group makes an element a scroll-marker-group container; its effect only manifests with ::scroll-ma
- **`scroll-timeline`** — Temporal capture works: the pill is consumed by an animation-timeline. The two real outcomes are correct CSS. (a) All five 'none *' values (none block/inline/x/y/none) produce NO named timeline regard
- **`scroll-timeline-axis`** — Temporal capture is correct. 'block' and 'y' both drive the pill: it slides right and turns blue as the vertically-scrolling panel advances (block == y in horizontal-tb writing mode, a spec alias). 'i
- **`scrollbar-color`** — The scrollable panel paints a visible custom scrollbar, so the property's surface IS shown in the still. However on macOS Chrome the native overlay scrollbar does not honor scrollbar-color: auto, #c54
- **`shape-image-threshold`** — The fractional/percentage values that fall inside [0,1] are correct and distinct: 0.25, 0.5, 0.75 and 15%, 30%, 50%, 65%, 80% each cut the alpha-fade shape-outside contour at a different radius, so te
- **`shape-margin`** — Excellent demo: a circular shape-outside float with running prose wrapping around it, captured statically. The length/percentage values are clearly distinct — 8px hugs the circle tightly, 24px/48px/64
- **`shape-outside`** — Float-and-wrap demo. The geometric shapes (circle/ellipse/inset/polygon) each wrap the prose to a distinct contour, and the four reference boxes (content/padding/border/margin) differ subtly because t
- **`shape-rendering`** — SVG hint property. Chrome collapses the 4 doc-valid keywords into only 2 distinct renders: {auto, geometricPrecision} render anti-aliased/smooth, and {optimizeSpeed, crispEdges} render with anti-alias
- **`stop-color`** — Demo applies the value as one stop of a gradient swatch; the resulting fill color is what is judged. The hex colors, percentage/number rgb(a), relative-color rgb/rgba/hsl/hsla/hwb/lab/lch/oklab/oklch 
- **`stop-opacity`** — stop-opacity sets the alpha of a gradient stop, demoed as how strongly the red stop blends in. All 11 values are valid CSS and render correctly; the distinct opacity levels (0.75, 0.25, 80%, 30%, 65%,
- **`stroke`** — Demo draws a red star and colors its outline (stroke); the outline color is judged. [0] none correctly shows the star with no outline. The hex, percentage/number rgb(a), relative-color rgb/rgba/hsl/hs
- **`stroke-dasharray`** — The demo strokes a zigzag 'M' path and varies the dash pattern. Large px values are clearly differentiated by dash size (8px = many fine dashes, 24px = medium, 48px = large, none = solid line). Percen
- **`stroke-dashoffset`** — SVG zigzag with a dashed stroke; the offset slides the dash pattern along the path and is clearly visible across the px values (24px vs 64px phase-shift the dashes to obviously different positions). T
- **`stroke-linejoin`** — Demo draws a thick caret (^) corner. miter renders a sharp point, round renders a smoothly rounded apex, bevel renders a flattened/chopped apex — all correct and distinct. arcs renders identically to 
- **`stroke-opacity`** — The red zigzag stroke is rendered at a correct, visibly distinct opacity for the fractional and percentage values (0.5, 0.75, 0.25, 50%, 80%, 30%, 65%, 15%). Three values render identically at full op
- **`stroke-width`** — The vast majority of the 27 values are correct and visibly distinct: the px lengths (8/16/24/48/64px) clearly grow in stroke thickness; the percentages (15/30/50/65/80/100%) resolve against the SVG di
- **`text-align`** — All standard keywords are correct and visually distinct: start/left render left-aligned, end/right right-aligned, center centered, justify stretches inter-word spacing with a flush-left last line. The
- **`text-autospace`** — Demo uses mixed CJK/Latin/numeric text '日本語Test混植API設計、「2024」年版とCodex仕様99番。'. Only [0] normal renders the autospace effect: clear thin spaces inserted at every CJK<->Latin and CJK<->digit boundary (日本
- **`text-box`** — Shorthand for text-box-trim || text-box-edge. The trim keyword (none vs trim-start) is correctly and visibly demonstrated: with trim-start the red box-edge lines pull tight to the cap/ideographic/x-he
- **`text-box-edge`** — All 24 values are valid per the documented <text-edge> syntax ([text|ideographic|ideographic-ink] single, or [text|ideographic|ideographic-ink|cap|ex] [text|ideographic|ideographic-ink|alphabetic] pai
- **`text-combine-upright`** — 'none' (digits stacked one per line) and 'all' (each digit run '2024'/'12' squeezed into one upright square) are correct and clearly distinct. Every 'digits <N>' value, however, is unimplemented in Ch
- **`text-decoration`** — All named colors (23-49) and all valid color forms (hex, legacy percentage rgb/rgba, hsl/hsla, hwb, lab, lch, oklab, oklch, and the relative-color rgb/rgba/hsl/lab... from <color> forms) render distin
- **`text-decoration-color`** — Identical situation to text-decoration. Named colors and all valid color functions (hex, percentage rgb/rgba, hsl/hsla, hwb, lab, lch, oklab, oklch, relative-color rgb/hsl/lab... from <color>) paint d
- **`text-decoration-line`** — underline (line under text), overline (line above text), line-through (strikethrough), spelling-error (red wavy/dotted underline) and grammar-error (faint wavy underline) all render correctly and dist
- **`text-decoration-skip`** — Both none and auto render an identical underline that crosses the glyph descenders the same way. text-decoration-skip is the deprecated predecessor of text-decoration-skip-ink and Chrome does not impl
- **`text-decoration-skip-ink`** — none is clearly distinct: the underline is drawn straight through the descenders of p/Q/y/g with no gaps. auto correctly skips the glyph ink, leaving visible gaps where descenders cross the underline.
- **`text-decoration-thickness`** — All numeric thicknesses are excellent: [2]24px / [3]48px / [4]8px / [5]16px / [6]64px give clearly different bar heights, the percentages [7-12] resolve against font size, and the math functions [13]c
- **`text-emphasis`** — The emphasis 'dot' marks render correctly above the specimen text and take the requested colour for every valid color: [0] is red dots, [32] dot blue is blue dots, [13] lab(...) is orange dots, and al
- **`text-emphasis-color`** — Each value sets the colour of the emphasis marks rendered above 'Specimen Quartz Glyph'. All Chrome-supported colour forms render correctly and distinctly: hex (#c5483c red, #2f5fd0 blue), rgb/rgba (i
- **`text-emphasis-position`** — Outstanding demo: it shows BOTH horizontal text ('Specimen Quartz Glyph 0123') and a vertical-text panel, so over/under is read from the horizontal emphasis dots (above vs below the line) and right/le
- **`text-indent`** — Strong, correct demo across every meaningful axis. Plain <length> indents only the first line; each-line indents ALL forced lines; hanging inverts it (first line flush, the rest indented); the magnitu
- **`text-justify`** — The demo justifies a fixed-measure paragraph and the effect resolves into exactly three correct, visually distinct states: (A) word-spaced justification (auto / inter-word and their no-compress combos
- **`text-overflow`** — [0] clip cuts the text hard at the box edge ('Specimen Qu'); [1] ellipsis shows 'Specime...' with the truncation glyph - both correct and clearly distinct. The remaining four values are valid CSS-Over
- **`text-rendering`** — Sample word 'Affluent Waffle' (with ffl/ffl ligatures) in a serif face. optimizeSpeed [1] is genuinely distinct: Chrome DISABLES ligatures/kerning, so the 'ffl' glyphs separate and the word's spacing 
- **`text-size-adjust`** — The demo applies the real CSS declaration to the specimen text and headless Chrome does render text-size-adjust:<percentage> as a font-size multiplier, so 15% / 30% / 50% / 65% / 80% all produce visib
- **`text-spacing-trim`** — The demo uses ideal Japanese mixed-script text with full-width punctuation (Codex(コード)と / 「2024」年。日本語 / Test、混植テキスト。). Two distinct renders appear: space-first(2) and trim-start(3) trim the leading fu
- **`text-transform`** — uppercase/lowercase/capitalize render distinctly; the compound full-width / full-size-kana / math-auto values are NOT implemented in this Chrome build (verified headless) and fall back to none, so the
- **`text-underline-position`** — Strong demo: a horizontal sample ('Specimen Quartz Glyph 0123') shows underline vertical offset, and a vertical writing-mode panel (rotated 'specimen' / 縦組 / 'Codex' columns) shows which side the unde
- **`text-wrap`** — Every value renders correctly; the only repeated renders are spec-forced or Chrome-unimplemented, so nothing more can be done in a static screenshot. Distinct outcomes observed: (A) auto-style wrap = 
- **`text-wrap-style`** — The effect panel wraps the headline 'The codex balances this ragged headline across its measure' in a fixed-width column. Three of the five values render distinctly and correctly: auto(0) produces the
- **`timeline-scope`** — timeline-scope is a pure name-scoping declaration: it widens the lookup scope of a named scroll/view timeline so elements outside the declaring element's subtree can attach to it. It has no standalone
- **`top`** — The demo offsets a red box from the top of a dashed frame by the computed top value, and the vertical position scales correctly across all length and percentage values. auto sits flush at the frame to
- **`touch-action`** — Every value renders an identical scrollable 'pan me' panel (verified auto vs none full-res; all 34 cells in the contact sheet match). touch-action controls which touch pan/pinch gestures the browser a
- **`transform`** — The demo transforms a 'transform' text element inside a panel that has a perspective context (a 3D floor grid is present and 3D rotations clearly foreshorten the text, confirming perspective is active
- **`transform-box`** — The demo correctly uses a REAL SVG: a <rect> (x34 y22 w52 h40, 14px blue stroke, red fill) carrying transform:rotate(40deg);transform-origin:0 0 inside an SVG with viewBox 0 0 120 84 and a padded/bord
- **`transform-origin`** — The demo tilts a slab with rotateX(50deg) rotateY(-20deg) so the origin's X/Y and (for 3-value origins) Z component change the projection. Keywords, lengths and percentages all shift the slab correctl
- **`transition`** — The shorthand demo (toggle box that moves/reshapes/recolors) correctly captures the two visually-meaningful axes: the easing function (linear / linear(1 80% 30%) / ease / ease-in / ease-out shift the 
- **`user-modify`** — All four values render an identical static 'Codex specimen' box (only the label text changes). user-modify (non-standard -webkit-/-moz- property, MDN: Non-standard + Deprecated) controls whether conte
- **`user-select`** — All five values (auto, text, none, contain, all) render the identical 'Try to select this sentence with your mouse' text in the still. The difference between these values only manifests during a live 
- **`vector-effect`** — [0]=none and [1]=non-scaling-stroke are correctly and visibly distinct: under the SVG viewBox scaling, none gives a thick chevron stroke while non-scaling-stroke gives a clearly thinner one. The other
- **`vertical-align`** — All 27 values render correctly. The demo places a small red inline box next to the serif 'Hg xy' text with a baseline rule, and the box position cleanly distinguishes every keyword: baseline (on basel
- **`view-timeline`** — Every one of the 50 grammar-generated values has the timeline NAME set to 'none' (the form is always 'none <axis> <inset>'). With name = none, no named view-progress timeline is actually established, 
- **`view-timeline-axis`** — The demo binds the subject to a named view-timeline and varies only the axis, and the subject DOES animate (color-coded pill advances along the scroll/view timeline). The four values resolve into two 
- **`view-timeline-inset`** — Here the demo element has a working named view timeline, so the inset genuinely shifts the start/end of the view-progress range and at a fixed scroll position the pill lands at different progress per 
- **`visibility`** — visible[0] shows the box ('one two three' chips over a red panel); hidden[1] hides it (empty effect panel) - both correct and distinct. collapse[3] also renders as an empty panel, identical to hidden:
- **`white-space`** — The sample is the generic wrapping block (a giant unbreakable word, then 'and / longlonglong / wrapping.' on source-broken lines). Only the values Chrome actually implements separate visually: normal(
- **`white-space-collapse`** — The four Chrome-supported keywords render distinctly: collapse(0) collapses spaces and newlines (single run), preserve(2) keeps both spaces and line breaks (multi-line with extra inner spacing and a b
- **`widows`** — The fragmentation/column-split effect IS painted in the still (the 'Section II split paragraph' visibly breaks across the column/page boundary). However full-res comparison shows values 1, 2 and 5 pro
- **`width`** — Strongest demo of the family. width directly sets the box size so almost every value renders distinctly and correctly: 8px is a single-character column, 24px/abs/round narrow, 48px/env wider, the perc
- **`will-change`** — will-change is a performance/optimization hint to the browser and has no visual rendering effect. All five values (auto, scroll-position, contents, my-ident, tag-a) are valid CSS and produce a byte-fo
- **`word-break`** — normal[0], break-all[1], keep-all[2] and break-word[5] are all distinct and correct: break-all breaks the Latin word mid-character at the box edge (supercalif/ragilisticexpialid/ocious), keep-all keep
- **`word-spacing`** — All 19 values render correctly: 'normal' is tight, the px values (8/16/24/48/64px) increase the inter-word gap monotonically and distinctly, and the percentages (15/30/50/65/80/100%) also render with 
- **`word-wrap`** — word-wrap is the legacy alias of overflow-wrap and shows the same behavior. normal[0] keeps the over-long Latin word whole; break-word[1] breaks it at the box edge (supercalifragilist/icexpialidocious
- **`zoom`** — The demo scales a 'Codex specimen - N' card by the zoom factor against a fixed dotted-grid background, so size differences are immediately legible. Every value renders correctly and at the expected sc

## Category 5 — chromerpc limit — cannot be captured headless (cursor sprite, interaction) (inherent)  (20)

- *WORKS* **`caret-animation`** — Only two values: auto (caret blinks) vs manual (caret stays solid, no blink). The editable card and its text caret ARE captured (filmstrip frame-01 shows the red caret bar before 'Codex'; frame-00 cat
- *WORKS* **`cursor`** — The cursor property changes the OS mouse-pointer sprite, which is drawn by the OS/compositor only when a real pointer hovers the element - it is never part of the page's painted surface. Headless Chro
- **`dynamic-range-limit`** — dynamic-range-limit caps the maximum luminance (HDR tone) the element may use. It only produces a visible difference on an HDR-capable display showing HDR content. The headless Chrome capture is an SD
- *WORKS* **`font-family`** — The CSS generic/system font keywords (system-ui, ui-serif, ui-sans-serif, ui-monospace, ui-rounded) resolve to the host OS's configured UI fonts; the headless screenshot environment has no such config
- *WORKS* **`forced-color-adjust`** — Requires forced-colors emulation (CDP Emulation.setEmulatedMedia with features:[{name:'forced-colors',value:'active'}]). chromerpc's SetEmulatedMedia step exposes only the media TYPE (print/screen), n
- *WORKS* **`interactivity`** — interactivity: auto vs inert controls whether an element (and its subtree) is inert - i.e. non-focusable, non-clickable, excluded from hit-testing and the a11y tree. It produces zero visual difference
- **`interest-delay`** — interest-delay is a shorthand for the delay before interest-triggered UI (popovers/tooltips) appears on hover/focus and hides on de-hover. The static effect panel is identical for normal/0.3s/1s/200ms
- **`interest-delay-end`** — interest-delay-end sets the delay before interest-triggered UI hides after the user stops showing interest (de-hover/blur). This is a pure interaction-timing effect: a static screenshot cannot capture
- **`interest-delay-start`** — interest-delay-start sets the delay before interest-triggered UI appears after the user shows interest (hover/focus). This timing-of-a-hover-reveal cannot be captured by a static screenshot. All four 
- **`interpolate-size`** — interpolate-size only toggles WHETHER a transition/animation between a <length-percentage> and an intrinsic keyword size (auto/fit-content/max-content) is allowed. It produces NO difference in any set
- **`overlay`** — Per MDN, overlay is only relevant within transition-property with transition-behavior:allow-discrete, controlling whether a top-layer element (popover/dialog) remains rendered in the top layer during 
- **`overscroll-behavior`** — The effect is scroll-CHAINING / bounce when a scroll continues PAST the inner container's boundary. Capturing it requires scrolling beyond the scroll extent (a wheel/touch fling past the edge); elemen
- **`overscroll-behavior-block`** — Same as overscroll-behavior: block-axis scroll-chaining past the boundary is not reachable — scrollTo clamps at the extent and no overscroll-past gesture exists in the automate vocabulary.
- **`overscroll-behavior-inline`** — Same as overscroll-behavior: inline-axis scroll-chaining past the boundary is not reachable — scrollTo clamps and no overscroll-past gesture exists in the automate vocabulary.
- *WORKS* **`print-color-adjust`** — economy vs exact only diverge in PRINT output (whether the UA may drop backgrounds/adjust colors to save ink). On the on-screen render both values are identical by spec, and the headless capture pipel
- *WORKS* **`resize`** — The resize grabber appears in the corner for resizable values, but the property's actual effect (and the difference between both/horizontal/vertical/block/inline) only manifests during a real mouse-DR
- **`scroll-snap-stop`** — normal and always render PIXEL-IDENTICALLY across all frames -- both land the target panel flush at the top. This is expected: scroll-snap-stop only changes behavior during a fast fling/momentum scrol
- **`speak-as`** — speak-as is a CSS Speech / @counter-style aural descriptor that controls how assistive technology VOCALIZES content (spell-out, digit-by-digit, punctuation handling). It has zero visual rendering — th
- **`view-transition-class`** — view-transition-class only has any effect while a view transition is actively animating (it assigns transitions to a class for grouped *::view-transition-group styling). In every static still the box 
- **`view-transition-name`** — view-transition-name only takes effect during a running view transition (it tags an element so the old/new states are captured and cross-faded). In any static still the box is identical across none/ma
