# The CSS Codex — per-property categorisation

Multimodal verification (judged by eye, no byte comparison) of every value of **513** properties. Each property is in exactly one category.

**Goal state:** every property in **1**, **3** (perfect) or **5** (chromerpc cannot capture). Categories 2, 4, 6, 7, 8 are work items.

| Cat | Meaning | Count |
|---:|---|---:|
| 1 | Perfect — every value adequately, uniquely & correctly demonstrated | 63 |
| 2 | UI polish — all values correct & distinct, but the demo could be clearer (fixable) | 3 |
| 3 | Perfect (inherent) — most values unique; a few render identically by spec/engine (unfixable, correct) | 87 |
| 4 | UI weak — some values collide only because the demo is too weak (fixable) | 101 |
| 5 | chromerpc limit — cannot be captured headless (cursor sprite, interaction) (inherent) | 8 |
| 6 | Pipeline-fixable — not captured now, but the screenshot automation could be customised (fixable) | 47 |
| 7 | Broken / other — a value renders wrong or the demo is broken (fixable) | 13 |
| 8 | Grammar fix — collisions from spec-invalid generated values (fix lang/*.ebnf) | 191 |

**158/513** properties are already in the goal state (30%). Work remaining: {2: 3, 4: 101, 6: 47, 7: 13, 8: 191}


## Category 1 — Perfect — every value adequately, uniquely & correctly demonstrated  (63)

- **`anchor-name`** — Both values are correct and visually distinct. With 'none' (index 0) only the 'chip' element renders with no tethered anchor box, since the anchor has no name to reference. With '--my-var' (index 1) t
- **`animation-delay`** — All three delays (0.3s, 1s, 200ms) are valid <time> values and the 3-frame filmstrip captures distinct box positions: the shorter the delay the further the box has travelled at a fixed capture time (2
- **`animation-direction`** — normal, reverse, alternate, alternate-reverse are all valid and render with clearly different box positions/travel direction across the filmstrip frames (each value samples the box at a distinct posit
- **`animation-fill-mode`** — none, forwards, backwards, both are all valid and render distinctly across the filmstrip: forwards and both hold the end-keyframe position at the final frame (box parked to the right), backwards holds
- **`animation-name`** — Each animation-name references a different @keyframes rule and produces a visibly distinct effect on the sample box: none -> box static at start (no animation, correct); my-ident -> small rightward tr
- **`animation-play-state`** — running shows clear motion across the filmstrip (box at ~x293 in frame-00 moving to ~x362 in frame-02), while paused freezes the box at a single position (~x248) with no change between frames. The two
- **`backface-visibility`** — Both values are correct and visually distinct. The demo uses a card rotated in a perspective scene so its back face is toward the viewer. With 'visible' (index 0) the back face is rendered (the red/bl
- **`background-origin`** — All 3 values render correctly and are clearly distinct. content-box confines the gradient to the inner content area with a wide gap to the dashed border; padding-box extends the gradient out to the pa
- **`border-collapse`** — Both values are correct and clearly distinct on the 2x2 table demo. separate (0) shows spaced cells each with their own border plus a rounded outer table border (border-spacing visible between cells).
- **`border-image-outset`** — Every value is correct and distinct. The striped diagonal border-image is pushed outward from the border edge by exactly the given amount: 8px sits tight to the box, 24px leaves a clear gap, 48px and 
- **`border-image-repeat`** — All three keywords render correctly and distinctly. stretch (0) shows the diagonal stripe slices stretched along each edge (visibly distorted/stretched mid-edge). repeat (1) tiles the slice at native 
- **`border-spacing`** — All five px values render a table whose inter-cell spacing visibly increases monotonically (8px < 16px < 24px < 48px < 64px). Every value is correct, distinct and clearly demonstrated.
- **`box-pack`** — All four values are correct and visually distinct. start packs the boxes to the left edge, center centers them, end packs them to the right edge, and justify spreads them with space between. Each cell
- **`box-shadow`** — All 50 values are inset box-shadows of the same red (#c5483c) varying only by offset-x (24px/48px), offset-y (8/16/24/48/64px) and blur (0/8/16/24/48/64px). Every value is valid CSS and renders a corr
- **`box-sizing`** — Both values are correct and clearly distinct. content-box (idx 0) draws the border/padding OUTSIDE the declared content width, so the box's total footprint is larger and the red border ring sits furth
- **`caption-side`** — Both values render correctly and distinctly. For 'top' the caption label sits above the table; for 'bottom' it sits below the table. The active value is also highlighted in the toggle legend on the ri
- **`clip-rule`** — Both values are correct and clearly distinct. 'nonzero' fills the whole self-intersecting star (solid star). 'evenodd' leaves the central self-overlap region unfilled, producing the classic hollow pen
- **`column-span`** — Both values render correctly and distinctly. 'none' keeps the red heading inside a single column with text flowing in multi-column layout around it; 'all' makes the heading span the full width across 
- **`direction`** — Both values are correct and visually distinct. ltr renders the specimen text left-aligned/flowing from the left edge; rtl renders the same text right-aligned/flowing from the right edge, with the lege
- **`field-sizing`** — Both values are correct and clearly distinct in full-res. With fixed (00) the native input keeps its default fixed width (~170px, matching the 'native button' above). With content (01) the input shrin
- **`fill-rule`** — Both values render correctly and distinctly on the self-intersecting star path: nonzero fills the whole star solid, while evenodd leaves the inner pentagon hollow. Clearly different and each matches t
- **`flex-direction`** — All four values are correct and visually unique. row shows items 1-4 left-to-right; row-reverse shows them 4-3-2-1 (numbers reveal the reversed order); column stacks them 1-4 vertically top-to-bottom;
- **`flex-grow`** — All 5 values (1, 2, 0.5, 0.75, 0.25) are valid <number> growth factors. The highlighted star item grows proportionally and visibly more for higher factors (widest at 2, narrowest at 0.25), each value 
- **`flex-shrink`** — All 5 values (1, 2, 0.5, 0.75, 0.25) are valid <number> shrink factors. The star item is widest at shrink 0.25 and progressively narrower toward shrink 2 (it gives up the most space). Per-step differe
- **`font-synthesis-small-caps`** — auto (idx0) renders 'HAMBURGEFONSTIV' in synthesized small-caps, while none (idx1) renders normal-case 'Hamburgefonstiv' with small-caps synthesis suppressed. The two values are clearly distinct and e
- **`font-synthesis-weight`** — auto renders the sample with noticeably heavier (synthesized-bold) strokes while none renders thinner real-weight strokes; both values are correct and visually distinct.
- **`font-variant-position`** — All three values are correct and clearly distinct. normal shows baseline 'H2SO4 E = mc2'; sub lowers and shrinks the digits into subscripts (H2SO4 with lowered 2/4, mc2 lowered); super raises and shri
- **`hyphenate-character`** — All 3 values render correctly and distinctly. The long demo word auto-hyphenates and the chosen hyphenate-character string appears at every line-end break. [0] auto -> default '-' hyphen ('supercalifr
- **`list-style-position`** — Both values render correctly and are visually distinct. 'inside' places the bullet markers within the content flow so the marker+text block is indented and the bullets sit further right; 'outside' han
- **`mask-composite`** — All four compositing operators are correct and clearly distinct. add [0] shows the union of the star and circle masks over the image; subtract [1] removes the second mask from the first; intersect [2]
- **`mask-type`** — Both values render correctly and are clearly distinct. luminance treats the mask by luminance, producing the masked square as a gradient that fades from dark (masked out) to bright red (visible). alph
- **`math-style`** — normal and compact are clearly, correctly distinct. With normal the fraction x^2/y is rendered in display style (taller, larger numerator/denominator with full spacing); with compact the whole formula
- **`order`** — Single-row flex demo with one highlighted item; each of the 5 order values (2,1,3,5,4) repositions the red item to a clearly distinct slot in the row: order 1 -> first, order 2 -> second, order 3 -> m
- **`outline-offset`** — Each value shows a faint inner control box with the orange outline drawn at the specified offset distance from it. The gap between the outline and the inner box grows monotonically and distinctly acro
- **`outline-style`** — All ten line styles render correctly and distinctly. auto = thin solid focus-ring style; none = no outline; dotted and dashed show their respective patterns; solid = uniform solid; double = two parall
- **`perspective`** — All 6 values render uniquely and correctly. 'none' shows flat text; each px depth (24/48/8/16/64px) produces a visibly distinct degree of 3D foreshortening of the 'perspective' word, with smaller px =
- **`ruby-align`** — All 4 values render distinctly and correctly. With base text 東京札 + ruby annotation, 'start' left-aligns the annotation per base group (note さつ sits at the left edge of 札 leaving a trailing gap); 'cent
- **`scroll-behavior`** — Both values are correctly and distinctly demonstrated via the temporal filmstrip. 'auto': every frame is already settled at panel 6/7 (instant jump, no animation). 'smooth': frame-00 shows panel 2/3 (
- **`scroll-margin`** — Top-snapping scroll demo (snap-align start). Each of the 5 length values (8px/16px/24px/48px/64px) lands the target panel 4 at a progressively larger offset from the container's top edge: 8px snaps ne
- **`scroll-margin-block`** — Same top-snapping demo as scroll-margin; block axis margin offsets the top snap edge in horizontal-tb. All 5 length values (8px/16px/24px/48px/64px) produce monotonically increasing offsets of panel 4
- **`scroll-margin-block-end`** — block-end in horizontal-tb is the bottom edge; the demo correctly uses bottom-snapping (snap-align end) so the margin has a visible effect. Target panel 4 lands near the container bottom at 8px and is
- **`scroll-margin-block-start`** — block-start in horizontal-tb is the top edge, matching the top-snapping demo. 8px snaps panel 4 flush to the top; larger values (16/24/48/64px) offset it down with panel 3 becoming visible above, in m
- **`scroll-margin-bottom`** — Physical bottom edge; the demo correctly uses bottom-snapping (snap-align end) so the margin is visible. Target panel 4 lands near the container bottom at 8px and shifts progressively upward (panel 5 
- **`scroll-margin-inline`** — Horizontal scroll-snap demo where 'panel 4' snaps to the start edge and scroll-margin-inline offsets the snap position. All 5 length values (8px, 16px, 24px, 48px, 64px) render in a clear, monotonic, 
- **`scroll-margin-inline-end`** — Demo uses end-aligned snapping so the inline-end margin is visible: panel 4 snaps toward the right/end edge and scroll-margin-inline-end offsets that landing. All 5 length values produce a distinct mo
- **`scroll-margin-inline-start`** — Start-aligned horizontal snap demo; scroll-margin-inline-start offsets the snap position from the start (left in LTR) edge. All 5 length values render in a clear, distinct, monotonic progression of th
- **`scroll-margin-left`** — Panel 4 snaps to the left edge and scroll-margin-left offsets the landing position. The 5 length values shift the snapped position monotonically and distinctly (8px = panel 4 leftmost/closest to edge;
- **`scroll-margin-right`** — Demo snaps panel 4 toward the right edge (end-aligned) so scroll-margin-right is visible. All 5 length values offset the right-edge landing distinctly and monotonically (8px = panel 4's right edge nea
- **`scroll-margin-top`** — All 5 length values (8px, 16px, 24px, 48px, 64px) are demonstrated with a scroll-snap container whose target panel (panel 4) snaps to a progressively larger offset from the top edge. The final-frame s
- **`scrollbar-width`** — All three values are correct and visually distinct in the full-res frames. 'auto' shows a normal-width scrollbar with a wide track channel to the right of the panels. 'thin' renders a narrower scrollb
- **`stroke-linecap`** — Demo draws a dashed M so dash-end caps are visible. butt shows separated dashes with flat, square-flush ends; round shows separated pill/capsule dashes with clearly rounded ends; square shows caps ext
- **`tab-size`** — The sample text contains a literal tab between 'Col' and the rest. Numeric values (0.25, 0.5, 0.75, 1, 2) set the tab stop in character widths and px values (8px, 16px, 24px, 48px, 64px) set it absolu
- **`table-layout`** — Both values are correct and visually distinct. 'auto' sizes columns to content (second column widens to fully contain 'Supercalifragilistic', table grows to fit). 'fixed' forces equal 50/50 columns ig
- **`text-anchor`** — All three SVG text-anchor values are rendered correctly and are clearly distinct against the dashed vertical reference line: start (0) places the text beginning at the line and extending right, middle
- **`text-decoration-style`** — All five styles render correctly and are visually distinct: solid (a single continuous line), double (two parallel lines), dotted (a row of dots), dashed (a row of dashes) and wavy (a zigzag). Each is
- **`text-emphasis-style`** — Every style renders a distinct, correct emphasis glyph above the specimen text: [0] dot (small filled dot), [1] circle (larger filled circle), [2] double-circle, [3] triangle (filled triangle), [4] se
- **`text-orientation`** — All 3 values are correct and clearly distinct in the vertical-writing sample. mixed (0): CJK upright while Latin digits '2024'/'12' are rotated 90deg (laid sideways), the spec default. upright (1): ev
- **`text-shadow`** — [0] none shows clean white text with no shadow. The other 49 values each apply a red (#c5483c) shadow with a unique combination of offset-x (8/24/48px), offset-y (8/16/24/48/64px) and blur radius (non
- **`text-wrap-mode`** — wrap (0) wraps the long sample text into multiple lines inside the box; nowrap (1) keeps it on a single line that overflows the box. The two values are correct and clearly visually distinct.
- **`transform-style`** — Both values are correct and clearly distinct. In the 'flat' cell the child 3D badge is flattened onto the card's plane (appears as a flat yellow rectangle lying on the card face with the '3D' label re
- **`transition-delay`** — All three delay values are correctly and distinctly demonstrated. Full-res frame-02: '1s' shows the box still a sharp dark square at its far-left START position (delay not yet elapsed, transition not 
- **`transition-duration`** — Both duration values are correct and visually distinct. Full-res frame-01: '0.3s' shows the box having COMPLETED its transition (full circle, final position) because the short duration finishes quickl
- **`writing-mode`** — All 5 values are correct and visually distinct. horizontal-tb (0): normal horizontal lines top-to-bottom. vertical-rl (1): vertical lines flowing right-to-left (first line on the right), CJK upright, 

## Category 2 — UI polish — all values correct & distinct, but the demo could be clearer (fixable)  (3)

- **`animation-composition`**  — _ui_: Stack two animations on the same transform property with a clearly non-zero underlying/base transform (e.g. an inline transform offset plus a keyframed translate), so that replace overrides the base, add sums base+keyframe, and accumulate c
- **`box-flex`**  — _ui_: Widen the flex container (more free space to distribute) and/or shrink the fixed siblings so box 1's share spreads across a larger range, amplifying the width differences between adjacent box-flex values. Optionally annotate each box with i
- **`transition-timing-function`**  — _ui_: Make the easing differences legible in a still: either (a) capture filmstrip frames at fixed animation-progress fractions (sync all value animations to start at capture time so frame N samples the same elapsed fraction for every value), or 

## Category 3 — Perfect (inherent) — most values unique; a few render identically by spec/engine (unfixable, correct)  (87)

- **`align-content`** — The demo (two rows of flex items in a tall container) renders every distribution and positional family correctly and distinctly: normal/stretch fill the height, space-between/around/evenly show correc
- **`align-self`** — This is the strongest demo of the three: a single target item (marked *) is left at auto cross-size inside the flex row, so the alignment FAMILIES are all clearly distinct — auto/normal/stretch make t
- **`alignment-baseline`** — SVG alignment-baseline demo: a red 'bg' tspan inside an 'A...x' SVG text run, with a drawn baseline line. ideographic (raised), middle, central, mathematical (lowered to distinct positions) and hangin
- **`aspect-ratio`** — The demo is correct: it fixes one axis of the Specimen box and lets aspect-ratio drive the other, so ratio 1/1 renders a square, 1/0.5 (=2:1) renders a wide box, 1/2 (=0.5) renders a tall box, and bar
- **`backdrop-filter`** — The demo applies backdrop-filter to a translucent inner panel over an opaque rainbow gradient. The filters with a real visual effect are correct and clearly distinct: [1] blur(24px) softens the backdr
- **`background-repeat`** — repeat-x (tiles horizontally), repeat-y (tiles vertically), repeat (tiles both axes), round (tiles with rescaling so whole tiles fit) and no-repeat (single image) all render correctly and are clearly 
- **`border-block-end-style`** — All 10 border-style keywords are valid and correctly rendered on the block-end (bottom) edge: solid, dotted, dashed, double, groove, ridge, inset and outset each render with their distinct line treatm
- **`border-block-start-style`** — All 10 border-style keywords are valid and correctly rendered on the block-start (top) edge: solid, dotted, dashed, double, groove, ridge, inset and outset each render with their distinct line treatme
- **`border-block-style`** — All ten line styles render correctly: solid (filled edge), dotted, dashed, double, groove, ridge, inset, outset are each visually distinct on the box. [1] none and [2] hidden both render with no visib
- **`border-bottom-style`** — All values render correctly: solid, dotted, dashed and double are clearly distinct; groove/ridge/inset/outset each show a different 3D bevel (groove dark-top, ridge light-top, inset pressed-in, outset
- **`border-image-slice`** — Strong demo. The fill keyword is clearly demonstrated (fill keeps the middle image region as the box background; no-fill leaves the center empty) and the various slice numbers/percentages produce visi
- **`border-inline-end-style`** — The box has a thick inline-end (right) border whose line-style varies. All visually-meaningful styles render correctly and distinctly: 0 solid, 3 dotted, 4 dashed, 5 double (two parallel lines), 6 gro
- **`border-inline-start-style`** — The demo shows a box whose inline-start (left) edge takes the line-style while the other edges keep a thin default border. Full-res screenshots confirm every style is rendered correctly and distinctly
- **`border-inline-style`** — Shorthand that styles both inline edges (start+end). The demo box shows left+right borders in the chosen style. Full-res screenshots confirm each style renders correctly and distinctly: solid, dotted,
- **`border-left-style`** — Every style is rendered correctly on the LEFT edge of the sample box (top/right/bottom stay solid as a base): solid, dotted (visible dots), dashed, double (twin lines), groove/ridge/inset/outset (dist
- **`border-right-style`** — Every line style renders correctly and distinctly on the right edge: solid, dotted, dashed, double, groove, ridge, inset, outset are all clearly different (verified full-res for dotted vs groove). The
- **`border-style`** — solid, dotted, dashed, double, groove, ridge, inset and outset all render visibly distinct border treatments (groove vs ridge show inverted bevels; inset vs outset show inverted shading). The only col
- **`border-top-style`** — Every value renders correctly: solid (single line), dotted (dotted top edge), dashed, double (two parallel lines), groove (3D sunken-groove top), ridge (3D raised-ridge top), inset (dark sunken top ed
- **`box-align`** — box-align (legacy -webkit-box cross-axis alignment) is correctly demonstrated: 'start' puts items at the top of the container, 'center' vertically centers them, 'end' puts them at the bottom, and 'str
- **`box-lines`** — single and multiple render identically (three boxes in one row, fitting the container). box-lines was a proposed property allowing box children to wrap to multiple lines, but it was dropped from the s
- **`box-orient`** — horizontal lays the three boxes side by side; vertical stacks them top to bottom; inline-axis renders identically to horizontal; block-axis renders identically to vertical. All four are correct. The t
- **`caret-shape`** — Full-res frame-01 captures the focused-input caret and the shapes are clearly distinct and correct: bar = thin vertical bar, block = filled block over the 'C', underscore = underline beneath the 'C'. 
- **`color-interpolation`** — The demo uses an SVG linearGradient whose interpolation color space is driven by color-interpolation. [2] linearRGB is clearly distinct and correct: its blue->yellow gradient has a brighter, lavender/
- **`color-scheme`** — The demo correctly tints the native form controls (checkbox, range slider, button, panel) for the chosen scheme, and the two genuinely distinct states render perfectly: the light state shows a white p
- **`column-count`** — Counts 2,3,4,5 each render their distinct number of newspaper columns with rules between them and are correct. auto (0), 1 (3) and var(--my-var, 0.5) (6) all render as a single, full-width column. aut
- **`column-wrap`** — All three values (auto, nowrap, wrap) render an identical single-row three-column layout. column-wrap is an experimental CSS Multi-column Level 2 property that Chrome does not implement, so the engine
- **`contain`** — The effect panel (left) collapses the 'Codex specimen' card whenever the value includes size/inline-size containment, and shows full content otherwise; the right panel highlights the active chip. Corr
- **`contain-intrinsic-block-size`** — The demo correctly sizes a placeholder box's block (height) axis by the intrinsic value: the box grows taller from a thin bar (none/auto none) through 8/16/24/48/64px, clearly visible in the dashed tr
- **`contain-intrinsic-height`** — Same demo and behavior as contain-intrinsic-block-size (height is the block axis here). The placeholder box height scales correctly with the value from a thin bar up to a tall box at 64px; all values 
- **`contain-intrinsic-inline-size`** — The demo correctly sizes the placeholder box's inline (width) axis: the box widens from a narrow sliver (none/8px) up to a wide box at 64px. All 12 values are correct CSS. As with the other contain-in
- **`contain-intrinsic-size`** — The shorthand correctly sizes the placeholder box in BOTH axes: the box grows from a tiny dot (none/auto none) into a larger square as the value increases through 8/16/24/48/64px. All 12 values are co
- **`contain-intrinsic-width`** — The demo correctly sizes the placeholder box's width: from a narrow vertical sliver (auto 8px) up to a wide box at 64px, clearly visible in the dashed track. All 12 values are correct CSS. As with the
- **`corner-block-end-shape`** — Demo shapes the block-end (bottom) corners of a box. round[0], scoop[1], bevel[2], notch[3], square[4], squircle[5] all render correctly and distinctly on the bottom edge (rounded / concave scoop / fl
- **`corner-block-start-shape`** — Demo shapes the block-start (top) corners of a box. round[0], scoop[1], bevel[2], notch[3], square[4], squircle[5] all render correctly and distinctly on the top edge. The only collision is round[0] v
- **`corner-bottom-left-shape`** — Demo shapes only the bottom-left corner (other three corners stay default-rounded). round[0], scoop[1], bevel[2], notch[3] (full-res confirms a clean rectangular notch only on the bottom-left), square
- **`corner-bottom-right-shape`** — Demo shapes only the bottom-right corner. round[0], scoop[1], bevel[2], notch[3], square[4], squircle[5] all render correctly and distinctly on that single corner. The only collision is round[0] vs su
- **`corner-bottom-shape`** — Demo shapes both bottom corners (shorthand for bottom-left + bottom-right). round[0], scoop[1], bevel[2], notch[3], square[4], squircle[5] all render correctly and distinctly on the bottom edge. The o
- **`corner-right-shape`** — Affects the two right corners only. round, scoop, bevel, notch, square and squircle each render a clearly distinct right-corner shape (verified by zooming the top-right corner in full-res). superellip
- **`corner-shape`** — Affects all four corners. round (smooth arc), scoop (4-point concave star), bevel (cut/hexagon), notch (plus-shaped bites), square (sharp), and squircle (flatter, boxier rounding) are all clearly dist
- **`corner-start-end-shape`** — Affects a single logical corner (block-start/inline-end). round, scoop, bevel, notch, square and squircle each render a distinct single-corner shape. superellipse(1) renders identically to round becau
- **`corner-start-start-shape`** — Affects a single logical corner (block-start/inline-start). round, scoop, bevel, notch, square and squircle each render a distinct single-corner shape. superellipse(1) renders identically to round bec
- **`corner-top-left-shape`** — Affects only the top-left corner; verified by tightly cropping that corner in full-res. round, scoop, bevel (diagonal cut), notch (rectangular bite), square (sharp) and squircle (flatter rounding) eac
- **`counter-increment`** — The demo prints the running 'my-ident · tag-a' counter pair per row, so each increment value yields a unique numeric tally (my-ident 2 -> 2/4/6/8, my-ident 3 -> 3/6/9/12, tag-a 5 -> 0/5/0/10... etc.) 
- **`counter-set`** — Like counter-increment, the demo prints the running my-ident · tag-a pair, and each set value yields a distinct numeric tally (my-ident 2 sets to 2 then +1/row -> 3/4/5/6, my-ident 3 -> 4/5/6/7, etc.)
- **`fill-opacity`** — All values are valid and the bright red star on a dark background makes the opacity gradations clearly readable (0.25/15% are faint, 0.5/50% medium, 0.75/0.8 strong, 1/100% solid). The only collisions
- **`font-feature-settings`** — The demo is excellent: the sample 'Quartz Fjord 1/2 ct st fi 0123' exercises every requested OpenType feature and the test font supports all of them. smcp clearly renders SMALL CAPS, swsh adds a swash
- **`font-kerning`** — Demonstration is correct and the meaningful distinction is clearly visible. 'auto'(0) and 'normal'(1) both apply kerning: 'AVATo Wave Ye.' fits on one line with tight A-V pairs. 'none'(2) disables ker
- **`font-size-adjust`** — The demonstration is strong: the ratio steps within ex-height, cap-height and ch-width visibly and correctly rescale the sample (e.g. ex-height 2 is huge, ex-height 0.25 is tiny, cap-height 1 is near-
- **`font-stretch`** — The percentage ladder (15%/30%/50%/65%/80%/100%) and the keyword ladder (ultra-condensed..ultra-expanded) render at progressively different widths of 'Hamburgefonstiv' and are mostly distinct and corr
- **`font-synthesis-style`** — auto (synthesize oblique allowed) and oblique-only (synthesize oblique allowed) render an identical slanted 'HAMBURGEFONSTIV' because the sample font has no real italic face and the differentiating we
- **`font-variant-caps`** — normal [0], small-caps [1], all-small-caps [2] and unicase [5] are clearly distinct and correct. petite-caps [3] renders identical to small-caps, all-petite-caps [4] renders identical to all-small-cap
- **`font-variant-emoji`** — emoji (index 2) is correct and clearly distinct — it forces the colorful emoji presentation (color envelope, purple umbrella, blue plane, red heart, larger glyphs). normal (0), text (1) and unicode (3
- **`grid-column`** — Well-designed grid demo: a 3-column track grid with one red item whose placement/span visibly changes per value. Numeric end-lines (auto/2..auto/5, auto/-1..-3) and span widths (span 2..5) render dist
- **`grid-column-end`** — Clear demo: a 4-column grid with one red item whose end line varies. Numeric end lines (1..5, -1..-3) and span widths (span 1..5) place/size the item distinctly and correctly (verified 1, 2, 5, span).
- **`grid-column-start`** — Mirror of grid-column-end and equally clear: a 4-column grid with one red item whose start line varies. Numeric start lines (1..5, -1..-3) and spans (span 1..5) render distinctly and correctly (verifi
- **`image-rendering`** — All 5 values are valid and correctly demonstrated on an up-scaled raster. Chrome renders two genuine visual groups: smooth/bilinear (auto[0], smooth[1], high-quality[2]) and blocky/nearest-neighbor (p
- **`image-resolution`** — image-resolution has essentially no browser support: Chrome does not implement the property at all, so every declaration is treated as a no-op and the image always renders at its intrinsic resolution.
- **`list-style-image`** — The values that Chrome supports are correctly and distinctly shown: url() and image-set() render the photo as the marker, and the five gradient functions (linear, repeating-linear, radial, conic, repe
- **`list-style-type`** — Most values are uniquely and correctly shown: [3] '"Specimen"' prefixes each item with the literal text 'Specimen', [4] '"Aa"' prefixes 'Aa', [5] 'none' shows no markers, and [2] 'symbols(cyclic "Spec
- **`mask-clip`** — content-box [0], padding-box [1] and border-box [2] are all genuinely distinct: content-box clips the mask to the smallest inset region, padding-box slightly larger, and border-box reveals the full re
- **`mask-mode`** — luminance [1] is clearly distinct (the image is visibly darkened/dimmed because the mask source's luminance modulates opacity). alpha [0] and match-source [2] render identically (both show the full im
- **`math-shift`** — normal and compact render pixel-for-pixel identically. math-shift only changes the vertical shift applied to superscripts inside an already-compact math layout; its effect is imperceptible (and largel
- **`mix-blend-mode`** — 17 of 18 blend modes render correctly and visually distinct over the conic-gradient backdrop: normal (plain box), darken, multiply (darkened), color-burn (deep red/black), lighten, screen (washed ligh
- **`offset-rotate`** — Manually verified (the wave-3 subagent for this batch failed to emit structured output). The offset-path demo orients the marker correctly for every value: auto faces the direction of travel, reverse 
- **`opacity`** — The opacity demo is excellent: a gradient box sits over a busy radial-gradient backdrop, and the distinct numeric/percentage steps (0.75, 0.25, 80%, 30%, 65%, 15%, 0.5/50%) clearly show progressively 
- **`overflow`** — Demo shows a striped content box inside a container. visible (0) lets the striped content spill past the container box; hidden (1) clips it to the box with no scrollbars; scroll (3) clips and always s
- **`overflow-block`** — Same demo and behavior as overflow. visible (0) spills past the box, hidden (1) clips with no scrollbars, scroll (3) clips and shows scrollbars — all distinct and correct. clip (2) renders identically
- **`overflow-inline`** — Same demo and behavior as overflow (no overlay value here). visible (0) spills past the box, hidden (1) clips with no scrollbars, scroll (3) clips and shows scrollbars — all distinct and correct. clip
- **`page`** — Paged demo. auto[0] applies no named page, so Section II flows continuously (two pages) -- correct and distinct. my-ident[1] and tag-a[2] each assign a named page to Section II, which correctly forces
- **`page-break-after`** — The demo correctly emulates paged media: 'auto' (0) flows naturally; 'always' (1) forces a page break after Section II's first block, leaving a large empty gap on the page and pushing remaining conten
- **`page-break-before`** — Correctly demonstrated and property-specific (renders differently from page-break-after): 'auto' (0) flows Section II naturally onto page 1; 'always' (1) forces Section II to START at the top of a fre
- **`page-break-inside`** — Excellently demonstrated. 'avoid' (0) keeps Section II as an unbreakable unit: since it cannot fit on page 1 it is pushed entirely to page 2 (page 1 ends after Section I with empty space). 'auto' (1) 
- **`ruby-overhang`** — 'auto' and 'spaces' render pixel-identical. ruby-overhang is not implemented in Blink/Chrome (it was never shipped), so both valid keyword values produce the same layout. Even in an engine that suppor
- **`scale`** — Every value is valid and renders correctly: the 'scale' word grows/shrinks proportionally (0.25/15% tiny, 0.5/50% small, 0.75/65%/80% medium, 2 large, etc). The only collisions are inherent CSS equiva
- **`stop-opacity`** — stop-opacity sets the alpha of a gradient stop, demoed as how strongly the red stop blends in. All 11 values are valid CSS and render correctly; the distinct opacity levels (0.75, 0.25, 80%, 30%, 65%,
- **`stroke-linejoin`** — Demo draws a thick caret (^) corner. miter renders a sharp point, round renders a smoothly rounded apex, bevel renders a flattened/chopped apex — all correct and distinct. arcs renders identically to 
- **`stroke-opacity`** — The red zigzag stroke is rendered at a correct, visibly distinct opacity for the fractional and percentage values (0.5, 0.75, 0.25, 50%, 80%, 30%, 65%, 15%). Three values render identically at full op
- **`text-decoration-line`** — underline (line under text), overline (line above text), line-through (strikethrough), spelling-error (red wavy/dotted underline) and grammar-error (faint wavy underline) all render correctly and dist
- **`text-decoration-skip`** — Both none and auto render an identical underline that crosses the glyph descenders the same way. text-decoration-skip is the deprecated predecessor of text-decoration-skip-ink and Chrome does not impl
- **`text-decoration-skip-ink`** — none is clearly distinct: the underline is drawn straight through the descenders of p/Q/y/g with no gaps. auto correctly skips the glyph ink, leaving visible gaps where descenders cross the underline.
- **`text-overflow`** — [0] clip cuts the text hard at the box edge ('Specimen Qu'); [1] ellipsis shows 'Specime...' with the truncation glyph - both correct and clearly distinct. The remaining four values are valid CSS-Over
- **`text-spacing-trim`** — The demo uses ideal Japanese mixed-script text with full-width punctuation (Codex(コード)と / 「2024」年。日本語 / Test、混植テキスト。). Two distinct renders appear: space-first(2) and trim-start(3) trim the leading fu
- **`transition-property`** — Only 'all' produces a visible transition: full-res frames show the box smoothly interpolating shape/position/color (square->circle, moving right). 'none', 'my-ident' and 'tag-a' all render identically
- **`vector-effect`** — [0]=none and [1]=non-scaling-stroke are correctly and visibly distinct: under the SVG viewBox scaling, none gives a thick chevron stroke while non-scaling-stroke gives a clearly thinner one. The other
- **`white-space-collapse`** — The four Chrome-supported keywords render distinctly: collapse(0) collapses spaces and newlines (single run), preserve(2) keeps both spaces and line breaks (multi-line with extra inner spacing and a b
- **`will-change`** — will-change is a performance/optimization hint to the browser and has no visual rendering effect. All five values (auto, scroll-position, contents, my-ident, tag-a) are valid CSS and produce a byte-fo
- **`zoom`** — The demo scales a 'Codex specimen - N' card by the zoom factor against a fixed dotted-grid background, so size differences are immediately legible. Every value renders correctly and at the expected sc

## Category 4 — UI weak — some values collide only because the demo is too weak (fixable)  (101)

- **`align-items`**  — _ui_: Remove the explicit/fixed heights from the four flex items (or give at least one item auto cross-size) so that align-items: stretch and normal visibly stretch the items to the full container height, distinguishing them from start. Keep diff
- **`all`**  — _ui_: Rebuild the demo so the cascade keywords actually diverge: nest the specimen inside a named @layer base { } with an author override in an unlayered rule, give the specimen a parent with a clearly different inherited color/font, and exercise
- **`anchor-scope`**  — _ui_: Rebuild the scene so anchor-scope actually matters: place TWO sibling subtrees that each declare an anchor with the SAME anchor-name (e.g. --target), plus a positioned tooltip using position-anchor: --target. Apply anchor-scope on a wrapper
- **`animation-iteration-count`**  — _ui_: Use a single-direction (non round-trip) long linear track of a property that monotonically maps to iteration progress (e.g. translateX from 0 to a large fixed distance, no alternate), and add a numeric run-counter badge or per-iteration col
- **`appearance`**  — _ui_: Render a per-keyword matching element instead of one fixed control set: appearance:meter on a <meter>, progress-bar on a <progress>, textfield/searchfield on a (search) <input>, textarea on a <textarea>, listbox/menulist/menulist-button on 
- **`background-blend-mode`**  — _ui_: Make the top background layer semi-transparent (e.g. add an rgba color or lower the image's effective opacity via a translucent gradient overlay) so additive modes plus-darker and plus-lighter visibly darken/lighten relative to normal. Alte
- **`baseline-source`**  — _ui_: Make the specimen a multi-line inline-block (vertical-align: baseline) placed on the same line as a single-line text run, so changing baseline-source from first to last visibly raises/lowers the multi-line box relative to the adjacent text.
- **`border-bottom-width`**  — _ui_: Enlarge the demonstrated bottom border (e.g. show each keyword as a wide horizontal bar whose height equals the resolved border width, or zoom the bottom edge) so the 1px/3px/5px steps of thin/medium/thick are clearly visible. Separately, d
- **`border-image`**  — _ui_: Increase the border-image-slice from '1 1 1 1' to a meaningful value (e.g. 33% 33% 33% 33% fill, or a px slice matching a patterned 9-slice source) AND give the box a thick border-width, so that stretch vs repeat vs round produce visibly di
- **`border-inline-start-width`**  — _ui_: Use a much larger sample element and a magnified/cropped view of the left edge (or render the four keyword widths as side-by-side swatch bars with a px readout) so the 1px/3px/5px difference between thin/medium/thick is clearly visible. Sep
- **`border-right-width`**  — _ui_: Make the thin/medium/thick difference legible: use a larger sample element or add a zoomed inset of the right edge, OR render the three keyword borders side-by-side at high magnification so 1px vs 3px vs 5px is obvious. Separately, in the <
- **`box-decoration-break`**  — _ui_: Replace the three-box effect element with a single inline (display: inline) element containing enough text to wrap across 3-4 lines, with a visible border, border-radius, padding and a background/box-shadow. Apply box-decoration-break to it
- **`box-direction`**  — _ui_: Rebuild the demo element as a horizontal display:-webkit-box (or -webkit-box-orient:horizontal) container holding 3-4 numbered/colored child boxes (e.g. cells labelled 1,2,3,4). With box-direction:normal they lay out 1,2,3,4 left-to-right; 
- **`box-flex-group`**  — _ui_: Redesign the sample to actually exercise grouping: make 4+ children flexible (box-flex:1) and split them into two box-flex-groups, then set the demonstrated box-flex-group on a subset so the lower-numbered group consumes free space first. W
- **`box-ordinal-group`**  — _ui_: Give each box its own box-ordinal-group and demonstrate by reordering: e.g. five boxes whose ordinal groups are permuted so the tested value lands the box in a unique position. Concretely, set the other boxes to fixed distinct groups (1..5)
- **`break-after`**  — _ui_: Augment the demo with the contexts each value actually breaks in. (1) Add a CSS multicolumn container variant (columns:2) so column/avoid-column[10,11] and always/all visibly break/avoid between columns. (2) Fix always[2]/all[3]: in the pag
- **`break-before`**  — _ui_: Same as break-after: add a CSS multicolumn variant (columns:2) so column/avoid-column visibly break between columns; fix always/all so they produce the page break in the paged variant (verify the declaration is applied and print/paged emula
- **`break-inside`**  — _ui_: Add a CSS multicolumn container variant (columns:2; column-fill:auto) sized so Section II would naturally split across a column boundary; then avoid-column[3] visibly keeps it whole within one column while auto splits it. (Region demos requ
- **`clip-path`**  — _ui_: Give the clipped element substantial and DIFFERING box-model dimensions: e.g. a large margin, a thick visible border, and generous padding (each >= 24px), plus a contrasting background/outline drawn at each reference box. Then circle(44%) c
- **`color-interpolation-filters`**  — _ui_: Replace the gradient swatch with an SVG <filter> that visibly depends on blend color space, e.g. a feGaussianBlur or an feComposite/feBlend over two overlapping saturated shapes (or an feDropShadow), and set color-interpolation-filters on t
- **`column-height`**  — _ui_: Use a much smaller font-size / line-height (e.g. 8px text, 9px line-height) or denser content so that 8px, 16px and 24px produce visibly different fragment heights (1 vs 2 vs 3 lines) instead of all bottoming out at one line. Alternatively 
- **`column-width`**  — _ui_: Widen the effect container and use longer un-hyphenatable words so min-content yields many narrow columns (column = widest word) while max-content yields one wide column and fit-content(24px) clamps to 24px-wide columns. Currently all three
- **`container`**  — _ui_: Add a named container-query child that lights up under '@container my-ident (...)' so the name axis (none vs my-ident vs tag-a) becomes observable; add a width-based @container query and a block-size query so 'size' vs 'inline-size' differ,
- **`container-name`**  — _ui_: Add a second container-query child styled via '@container tag-a (...)' so that the 'tag-a' value lights up its own child, distinguishing it from 'none'. With both my-ident and tag-a queries present, all three values render uniquely.
- **`container-type`**  — _ui_: Add a block-size @container query (fires only for type:size, not inline-size) and a scroll-state @container query, each toggling a distinct child style. Then size, inline-size, the scroll-state combos, and scroll-state-alone each produce a 
- **`counter-reset`**  — _ui_: Demonstrate reversed() counters in a context where they matter: render an <ol> using 'list-item' (or a content counter that auto-numbers in document order) so the reversed counting direction and the reset offset visibly change the printed n
- **`display`**  — _ui_: For the table-internal display values (table-row-group/header-group/footer-group/row/cell/column-group/column/caption) render the sample inside an actual display:table ancestor with sibling rows/cells so each internal role lays out distinct
- **`dominant-baseline`**  — _ui_: Strengthen the demo so the colliding baselines diverge: draw an explicit horizontal guide line for each baseline (text-top/text-bottom/hanging/ideographic/alphabetic) and offset a tall reference glyph against it, and/or include a CJK glyph 
- **`empty-cells`**  — _ui_: Add at least one truly empty cell to the demo table (a <td></td> with no text and no &nbsp;) while keeping border-collapse: separate on the table. Then 'show' will draw that empty cell's border and background and 'hide' will remove them, ma
- **`filter`**  — _ui_: In the value generator for the <filter-function> demo arguments, emit non-identity factors for brightness/contrast/opacity/saturate (e.g. brightness(1.6), contrast(2), opacity(0.4), saturate(3)) so each is visibly distinct from none. For th
- **`flood-opacity`**  — _ui_: Change the flood-color used in the flood-opacity demo to a bright, high-contrast color (e.g. white or a saturated yellow) and place the feFlood rectangle on a light/checkerboard backdrop so the alpha gradations are visible. Optionally overl
- **`font`**  — _ui_: Redesign the font-shorthand sample element: (a) drop the xx-small size or use a per-cell legible size so the font-family token in the shorthand is readable; (b) render a MULTI-LINE paragraph (e.g. 3-4 wrapped lines) in the effect panel so t
- **`font-optical-sizing`**  — _ui_: Switch the sample element to a variable font that exposes an 'opsz' (optical-sizing) axis with strong low-vs-high-opsz contrast (e.g. a display-grade variable font), and render the demo at a large size so the optical adjustment is visible. 
- **`font-palette`**  — _pipeline_: Change the palette-mix value generator so the two color-stops reference DIFFERENT base palettes (e.g. palette-mix(in srgb, light 60%, dark 40%) or palette-mix(in oklab, --my-var 50%, normal 50%)). Mixing two distinct palettes yields a blend
- **`font-synthesis-position`**  — _ui_: Change the sample element to include text with font-variant-position: super (and/or sub), e.g. a chemical formula 'H<sub-styled>2</>O' or 'x' with a superscript '2', using a font that lacks native sub/super glyphs. With 'auto' the browser w
- **`font-variant`**  — _ui_: Use sample content that exercises the differentiating sub-features in the effect panel: include digits/fractions and an explicit sub/super sample (e.g. 'H2O 1/2'), CJK characters to show full-width vs proportional-width, and an emoji to sho
- **`font-variant-alternates`**  — _ui_: Add an @font-feature-values block defining @stylistic/@swash/@ornaments/@annotation/@styleset/@character-variant named sets (e.g. 'alpha') mapped to real feature indices, and use a display font that actually ships those alternate glyph sets
- **`font-variant-east-asian`**  — _ui_: Load a CJK webfont that actually ships the Adobe-Japan1 OpenType glyph variants (jp78/jp83/jp90/jp04/trad/smpl/ruby/pwid), e.g. a 'Source Han' / Noto Serif CJK variant build, and use sample kanji whose glyphs differ between jis forms (e.g. 
- **`font-variant-ligatures`**  — _ui_: Use a font that exposes historical ligatures and contextual alternates, and a sample string that exercises them: e.g. a font with a 'long s' historical ligature (ſt, ſh, ſi) plus calt contextual swashes, and add test words containing those 
- **`font-variant-numeric`**  — _ui_: Switch the sample text to a font that implements afrc (stacked-fractions), onum (oldstyle figures) and ordn (ordinal) — e.g. a webfont like 'Recursive', 'Source Serif 4', or 'IBM Plex' with full numeric OpenType coverage. Then stacked-fract
- **`font-variation-settings`**  — _ui_: Render the sample text in a variable font that exposes all four demonstrated axes (e.g. Roboto Flex, which has wght/wdth/opsz/slnt/GRAD), self-hosted via @font-face with font-variation-settings actually applied. Also clamp generated slnt va
- **`font-weight`**  — _ui_: Make the value generator emit meaningful font-weight numbers on the 100-900 scale (e.g. 100, 300, 400, 600, 900) instead of tiny 1-5 integers. With weights spread across 100-900 (in a font that has those weights or a variable wght axis) the
- **`font-width`**  — _ui_: Render the sample text in a font that actually has a width axis / multiple width faces (e.g. Roboto Flex with its wdth axis, or a family shipping Condensed/Expanded faces) self-hosted via @font-face. Then ultra-condensed through ultra-expan
- **`grid-area`**  — _ui_: In the grid-area template, define the custom line names used by the generator on the grid container, e.g. grid-template-columns: [my-ident] ... [tag-a] ... (and matching named rows), placing 'my-ident' and 'tag-a' at DIFFERENT line position
- **`grid-auto-flow`**  — _ui_: Give a couple of the demo items explicit spans (e.g. grid-column: span 2 on item 2 and a later item) so the default sparse flow leaves visible gaps. Then 'row dense'/'column dense'/'dense' will back-fill those holes and render differently f
- **`grid-row`**  — _ui_: Declare the named lines used by the generated values in the demo's grid-template-rows, e.g. grid-template-rows: [my-ident] 40px [tag-a] 40px [my-ident] 40px [tag-a] 40px; so that 'N my-ident'/'N tag-a' resolve to distinct row positions inst
- **`grid-row-end`**  — _ui_: Add the named row lines (my-ident, tag-a) to the demo's grid-template-rows so named-line end values resolve to real lines and render distinctly, e.g. grid-template-rows: [my-ident] 40px [tag-a] 40px [my-ident] 40px [tag-a] 40px;.
- **`grid-row-start`**  — _ui_: Declare named row lines (my-ident, tag-a) in the demo grid-template-rows so 'N my-ident'/'N tag-a' resolve to distinct start lines instead of all falling into the implicit row below the grid.
- **`grid-template-areas`**  — _ui_: Place a few demo items into the named areas so the template becomes visible. Give one or two items grid-area: Specimen (for value 1) and grid-area: a (for value 2), and have the demo container actually use the live grid-template-areas value
- **`hanging-punctuation`**  — _ui_: Replace the single-line sample with a narrow (e.g. 14em) justified multi-line paragraph whose lines begin and end with hanging punctuation - e.g. start lines with opening quotes and dashes and end them with closing quotes/periods/commas (“Q
- **`hyphenate-limit-chars`**  — _ui_: Use the two/three-value form that actually sets the before/after limits (e.g. 'auto 1 1', 'auto 5 5', 'auto 8 2') and/or a sample built from shorter sub-words (around 4-6 letters) so the chars-before/chars-after thresholds change WHERE the 
- **`hyphens`**  — _ui_: Insert soft hyphens (&shy; / U+00AD) into the long demo word for the 'manual' (and 'auto') sample, e.g. 'long&shy;long&shy;long'. Then 'manual' will break at the soft hyphens (distinct from 'none' which ignores them and from 'auto' which us
- **`isolation`**  — _ui_: Rebuild the demo so isolation is observable: place a colored backdrop shape, then a container holding a child element with mix-blend-mode (e.g. difference or multiply) that overlaps the backdrop. With isolation:auto the child blends with th
- **`justify-items`**  — _ui_: Add an overflow scenario to the demo (e.g. give the sample grid item a min-content width LARGER than its cell, or shrink the cell) so safe vs unsafe genuinely diverge: 'safe center/end' should fall back to start while 'unsafe center/end' ke
- **`justify-self`**  — _ui_: Give the highlighted sample item an intrinsic width LARGER than its grid cell (or shrink the cell) so overflow occurs, making safe vs unsafe diverge: 'safe center/end' fall back to start while 'unsafe center/end' keep centering/ending and o
- **`line-break`**  — _ui_: Replace the sample with text that exercises the loose/normal/strict differences and force breaks at those characters by narrowing the box: include small kana / prolonged sound mark (e.g. 'ぁ', 'ャ', 'ー') and breakable punctuation so that 'loo
- **`line-height-step`**  — _ui_: Replace the single short label with a fixed-width (e.g. 280px) paragraph of several wrapped lines of text at a chosen font-size (e.g. font-size:17px so natural line-height ~22px sits between step multiples), and apply line-height-step to th
- **`list-style`**  — _ui_: Redesign the list-style demo so the trailing counter-style/string component is observable: (a) for cells whose purpose is the <counter-style|string> token, pair it with 'none' image (or no image) so the marker text/symbol shows; (b) demonst
- **`mask`**  — _ui_: Use TWO overlapping mask layers (e.g. a circle gradient layer plus the photo layer) so mask-composite add/subtract/intersect/exclude each produce a visibly different combined mask. Also give the element padding+border so content-box/padding
- **`mask-border-mode`**  — _ui_: Use a larger sample element (~300px) with a mask-border-source whose alpha channel and luminance deliberately diverge (e.g. a radial-gradient from rgba(255,255,255,1) to rgba(0,0,0,0.5) where luminance fades but alpha stays high, vs alpha f
- **`mask-border-outset`**  — _ui_: Use a much larger element (~300px) with a generous mask-border-width (~30-40px), a non-zero mask-border-slice, and add visible margin/space around the element so the outward-extended mask border is not clipped by neighbours. Give the box a 
- **`mask-border-repeat`**  — _ui_: Use a larger element and a mask-border-source whose edge slices contain a clearly repeating shape (e.g. a row of discrete dots/dashes or a repeating-linear-gradient with hard color stops creating distinct tiles). Pick element dimensions tha
- **`mask-border-slice`**  — _ui_: Use a large element (~300px) with a thick mask-border-width (~40px), a solid contrasting background, and a mask-border-source designed as a 3x3 grid where the center tile and the edge tiles have clearly different opacity/luminance (e.g. tra
- **`mask-border-source`**  — _ui_: Configure the surrounding mask-border shorthand on the sample so the source is actually used: set a non-zero mask-border-slice (e.g. 30 fill) and a visible mask-border-width (e.g. 40px) on the same element, with a solid background. Then a g
- **`mask-image`**  — _ui_: Make the mask sources actually carry alpha/luminance variation: add transparent (or low-alpha) stops to the gradients (e.g. linear-gradient(135deg, #000 0%, transparent 70%)), or set mask-mode: luminance on the demo so the colour gradients 
- **`mask-origin`**  — _ui_: Give the masked sample element a clearly visible padding (e.g. padding: 24px) AND a thick border (e.g. border: 16px solid), and use a non-repeating mask image positioned at top-left, so that content-box, padding-box and border-box each reve
- **`object-view-box`**  — _grammar_: Fix the rect() value generator (the <basic-shape-rect>/rect() grammar rule) so the four edge offsets form a valid, non-empty rectangle: ensure the 2nd offset (right) is greater than the 4th (left) and the 3rd (bottom) is greater than the 1s
- **`offset`**  — _ui_: Stop using 'offset-path: none' in this shorthand demo. Build the demonstrated shorthands around a real path (e.g. 'normal ray(45deg) 24px auto 45deg/ <anchor>' or a fixed path() ) so that varying the offset-anchor tail visibly moves the box
- **`offset-path`**  — _ui_: Redesign the demo so coord-box differences can show: (1) draw the actual path outline and animate offset-distance so the path GEOMETRY is visible, and (2) give the sample element a thick padding and border so content-box/padding-box/border-
- **`offset-position`**  — _ui_: Wire offset-position to a path that actually uses it: either set 'offset-path: ray(45deg)' WITHOUT an 'at' clause (the ray then originates from offset-position), or set 'offset-path: none' together with a non-zero offset-distance, so that c
- **`orphans`**  — _pipeline_: Make the split paragraph much taller and place the page break so the bottom of page 1 has exactly ~5 candidate lines; then orphans 2/3/4/5 each force a different number of lines to move to page 2, producing visibly different break points. P
- **`outline-width`**  — _ui_: Drop the non-standard 'hairline' keyword from the outline-width value set (it is not in the CSS spec). If keeping it for documentation, render it on a light/contrasting sample box so its actual computed width is observable, and place thin/m
- **`overflow-clip-margin`**  — _ui_: Give the clipped element a thick visible padding (e.g. 24px) and a thick visible border (e.g. 12px solid) so the content-box, padding-box and border-box reference edges are physically separated. Then content-box/padding-box/border-box at th
- **`overflow-wrap`**  — _ui_: Add a second sample box sized with width:min-content (or display:inline-block with no explicit width) alongside the fixed-width one. 'anywhere' will shrink that box to the widest broken fragment while 'break-word' will keep it as wide as th
- **`paint-order`**  — _ui_: Change the sample element to an OPEN stroked path (e.g. a zig-zag or arc) with large, opaque vertex markers (marker-start/mid/end pointing to a sizeable <marker>) whose markers overlap the thick stroke and the fill. With all three of fill, 
- **`place-content`**  — _ui_: Use a wrapping multi-LINE flex container (or a grid with extra cross-axis space) so align-content actually moves rows, and make the container content OVERFLOW (items wider/more than the box) so safe vs unsafe diverge. Add a second RTL/ortho
- **`place-items`**  — _ui_: Make item content OVERFLOW its grid cell (bigger content than the track) so safe clamps back to start while unsafe keeps overflowing - the only way to show safe vs unsafe. Add an RTL/orthogonal sample cell so left/right and start/self-start
- **`position`**  — _ui_: Redesign the position demo so the effect is visible: give the sample box a non-trivial offset (e.g. top:16px; left:16px) and place a normal in-flow sibling box next to it inside the frame. Then static will sit in flow (ignoring the offset) 
- **`position-area`**  — _ui_: Make the spanned containing-block region visible: render a translucent rectangle filling the resolved position-area grid region (the chip's containing block) so span-top/span-bottom/span-y-start/span-all are visibly different sizes/extents,
- **`ruby-position`**  — _ui_: Use a base string with MULTIPLE consecutive ruby segments (e.g. several kanji+furigana pairs in a row) so 'alternate' visibly places successive annotations on alternating over/under sides, distinguishing it from plain 'over'/'under' and mak
- **`scroll-snap-align`**  — _pipeline_: For the 'none' value, drive the scroll to a deliberately non-aligned offset (e.g. stop mid-panel, ~50px past a panel boundary) so the target rests partway between panels, visibly different from the snapped 'start' position. Either scroll a 
- **`scroll-snap-type`**  — _ui_: Rework the snap demo: use TWO scroll containers (a horizontal one to demonstrate x/inline, a vertical one for y/block/both), scroll to an off-boundary offset and release so the box snaps, and capture the settled frame so each axis lands the
- **`shape-outside`**  — _ui_: Add a real same-origin raster image WITH an alpha channel (a soft-edged PNG mask) to assets/ and point the url()/image()/image-set()/cross-fade() demos at it, paired with shape-image-threshold so the wrap follows the alpha contour rather th
- **`text-align-last`**  — _ui_: Change the specimen text so the LAST wrapped line contains multiple words (e.g. end the text with a phrase like "the quick brown fox" that wraps so the final line has 3-4 words). Then text-align-last: justify will visibly stretch that last 
- **`text-box`**  — _ui_: Wrap the specimen text in an element with a visible top/bottom border (or draw fixed cap-height/ex-height/em guide lines) so the trimmed leading is measurable by eye, and add a CJK+Latin sample line so ideographic/ideographic-ink edges diff
- **`text-combine-upright`**  — _ui_: Use a larger vertical-writing sample that makes tate-chu-yoko obvious, e.g. a year like '2024' or page '12' at large font in a vertical line and render side-by-side states so 'digits 4' visibly squeezes '2024' into a single upright square w
- **`text-emphasis-position`**  — _ui_: Add a second sample panel that uses writing-mode: vertical-rl (or vertical-lr) on the specimen text. In vertical writing the left/right component of text-emphasis-position controls which side of the column the marks sit on, so 'over right' 
- **`text-indent`**  — _ui_: Insert explicit hard line breaks (<br>) into the specimen text inside the indented block, or make the sample two block-level lines, so that 'each-line' re-indents after each forced break. With hard breaks present, 'Npx each-line' will inden
- **`text-rendering`**  — _ui_: Change the sample text to one rich in ligatures and kerned pairs (e.g. 'Affluent Waffle fjord — AV To Wa') set much larger (e.g. 64-96px), and ideally overlay/compare optimizeLegibility against optimizeSpeed side-by-side so the ligature/ker
- **`text-transform`**  — _ui_: Change the sample element to mixed Japanese/Latin text that exercises every sub-feature: include lowercase Latin words (so capitalize visibly differs from none), small kana such as 'ぁぃぅっゃゅょ' (so full-size-kana visibly enlarges them), and ha
- **`text-underline-position`**  — _ui_: Add a second sample run set to writing-mode: vertical-rl (with text-orientation: upright) so that left and right place the underline on opposite sides of the vertical text run, making them distinct from auto and from each other. Optionally 
- **`text-wrap`**  — _ui_: Change the text-wrap sample from the single-long-word string to a multi-word ragged headline (e.g. 'The codex balances this ragged headline across its measure', as text-wrap-style already uses) inside a narrow fixed-width measure. That make
- **`transform`**  — _ui_: Stage a real 3D box (like the rotate / perspective-origin demos already use) inside a container that sets `perspective` on the scene, instead of flat text. Then translateZ(24px) (depth scale), scaleZ(1)/scale3d, and perspective(24px) would 
- **`transform-box`**  — _ui_: Swap the sample element for an actual SVG with a transform applied: give it an explicit viewBox, a wide stroke and a fill, and a transform-origin offset. Then fill-box (geometry bounding box), stroke-box (stroke-inclusive box) and view-box 
- **`transition-behavior`**  — _ui_: Change the demo to also toggle a DISCRETE property on the box, e.g. animate 'display: none <-> block' (or 'visibility: hidden <-> visible') together with opacity, with a long duration. With 'allow-discrete' the box will fade in/out smoothly
- **`translate`**  — _ui_: Add 'perspective: 600px' (and 'transform-style: preserve-3d') to the scene/stage container so translateZ visibly scales the element toward/away from the viewer; this makes the third component distinct across the 5 Z-variants in each family.
- **`unicode-bidi`**  — _ui_: Change the sample to mixed-direction text containing RTL script (Hebrew/Arabic) interleaved with LTR Latin/numbers, and wrap a sub-run in a span with dir='rtl' (or override the element's direction). Then: bidi-override visibly reverses char
- **`word-break`**  — _ui_: Add a second sample line with CJK text (e.g. a Japanese/Chinese sentence with no spaces) to the effect panel so keep-all (keeps CJK runs together, forcing overflow) and auto-phrase (breaks at natural phrase boundaries) render visibly differ
- **`word-spacing`**  — _ui_: Change the word-spacing effect element to a multi-word sentence with many spaces (e.g. 'The quick brown fox jumps over the lazy dog') so word gaps are visible; then 24px/48px/64px and the percentage values will render with clearly different
- **`word-wrap`**  — _ui_: Add (or switch to) a min-content / shrink-to-fit container -- e.g. a box with width:min-content or an inline-block/flex item allowed to shrink -- so that overflow-wrap:anywhere shrinks the box narrower than break-word, making indices 1 and 
- **`z-index`**  — _ui_: Make the numeric z-index values distinguishable by introducing multiple competing layers instead of one. Render a fixed staircase of background boxes at z-index 1, 2, 3, 4 (each offset and labelled) and let the sample box take the demonstra

## Category 5 — chromerpc limit — cannot be captured headless (cursor sprite, interaction) (inherent)  (8)

- **`cursor`** — The cursor property changes the OS mouse-pointer sprite, which is drawn by the OS/compositor only when a real pointer hovers the element - it is never part of the page's painted surface. Headless Chro
- **`dynamic-range-limit`** — dynamic-range-limit caps the maximum luminance (HDR tone) the element may use. It only produces a visible difference on an HDR-capable display showing HDR content. The headless Chrome capture is an SD
- **`interactivity`** — interactivity: auto vs inert controls whether an element (and its subtree) is inert - i.e. non-focusable, non-clickable, excluded from hit-testing and the a11y tree. It produces zero visual difference
- **`interest-delay`** — interest-delay is a shorthand for the delay before interest-triggered UI (popovers/tooltips) appears on hover/focus and hides on de-hover. The static effect panel is identical for normal/0.3s/1s/200ms
- **`interest-delay-end`** — interest-delay-end sets the delay before interest-triggered UI hides after the user stops showing interest (de-hover/blur). This is a pure interaction-timing effect: a static screenshot cannot capture
- **`interest-delay-start`** — interest-delay-start sets the delay before interest-triggered UI appears after the user shows interest (hover/focus). This timing-of-a-hover-reveal cannot be captured by a static screenshot. All four 
- **`scroll-snap-stop`** — normal and always render PIXEL-IDENTICALLY across all frames -- both land the target panel flush at the top. This is expected: scroll-snap-stop only changes behavior during a fast fling/momentum scrol
- **`speak-as`** — speak-as is a CSS Speech / @counter-style aural descriptor that controls how assistive technology VOCALIZES content (spell-out, digit-by-digit, punctuation handling). It has zero visual rendering — th

## Category 6 — Pipeline-fixable — not captured now, but the screenshot automation could be customised (fixable)  (47)

- **`animation-range`**  — _pipeline_: Rebuild the demo as a scroll-driven timeline: give the sample element animation-timeline: view() (or scroll() over a scroll container), then have the screenshot automation create an actual overflow/scroll container and capture frames at sev
- **`animation-range-end`**  — _pipeline_: Same as animation-range: bind animation-timeline: view()/scroll() to the sample element, have the automation build a real scroll/overflow container and capture frames at multiple scroll offsets so different range-end values make the animati
- **`animation-range-start`**  — _pipeline_: Rebuild the demo so range-start actually matters: give the effect panel a tall scrollable inner container, attach the box's animation to a view()/scroll() timeline (animation-timeline) bound to that scroller, then have the screenshot automa
- **`animation-timeline`**  — _pipeline_: Add a tall scrollable container to the effect panel, bind the box's keyframe animation to it, and have the screenshot automation drive scroll position. Then scroll(root block) and view(block auto auto) will show scroll-linked progress that 
- **`background-attachment`**  — _pipeline_: Give the sample element a tall scrollable inner content (overflow:auto with content taller than the box) and have the screenshot automation scroll the element a fixed amount before capturing a second frame; compare/snap that scrolled frame 
- **`caret-animation`**  — _pipeline_: Focus the contenteditable BEFORE the temporal capture (or at frame-00) and capture two frames spaced ~500ms apart so they land on opposite blink phases. Then auto shows caret on->off (or off->on) while manual stays on->on, making the two va
- **`column-fill`**  — _pipeline_: Give the multicol specimen an explicit fixed height (e.g. height:120px) with enough text to overflow, OR render it under print/paged emulation. With a constrained height, auto fills column 1 completely before column 2 (lopsided), while bala
- **`content-visibility`**  — _pipeline_: Render the content-visibility:auto specimen inside an overflow:auto scroll container with the specimen scrolled below the visible region (or place it far past the fold and do not scroll it into view), then screenshot. The auto element will 
- **`font-family`**  — _pipeline_: Configure the headless Chrome screenshot environment so the CSS system-font keywords resolve to actual fonts: run Chrome with the macOS system fonts available / system-ui mapping enabled (or bundle representative fonts and a fontconfig/loca
- **`font-language-override`**  — _pipeline_: Change the sample element for font-language-override: use a font that ships language-specific 'locl' glyph substitutions and sample text in the relevant script (e.g. Serbian/Macedonian Cyrillic characters that differ from Russian, or Turkis
- **`forced-color-adjust`**  — _pipeline_: Emulate forced-colors mode before capturing this property's screenshots. Use Chrome DevTools Protocol Emulation.setEmulatedMedia with features [{name:'forced-colors', value:'active'}] (and optionally a prefers-color-scheme), or launch the h
- **`interpolate-size`**  — _pipeline_: Make the demo element transition width (or height) between a fixed value and an intrinsic keyword such as auto/max-content, trigger the transition, and capture a temporal 3-frame filmstrip. With allow-keywords the mid frame shows a partway-
- **`justify-content`**  — _pipeline_: Force main-axis overflow in the justify-content demo: use more/wider items or a much narrower flex container so total item width exceeds the container. Then unsafe center/start/end will visibly overflow past the container edge (items clippe
- **`math-depth`**  — _ui_: Build the sample so math-depth actually scales something: put a nested element inside the math element that uses 'font-size: math' (i.e. font-size: math) and set the math-depth on it, or use real <math> with nested <mfrac>/<msup> where the 
- **`overflow-anchor`**  — _pipeline_: Add automation that scrolls a tall scrollable container to a middle position, then inserts a tall block ABOVE the viewport via DOM mutation, and captures the scroll offset / visible-content position before vs after. With auto the visible co
- **`overlay`**  — _pipeline_: Use a real top-layer element: a popover (or <dialog>) with a transition on opacity/transform plus 'transition-behavior: allow-discrete' and an 'overlay' transition. Open it, trigger its close, and capture a mid-exit-transition frame (a temp
- **`pointer-events`**  — _pipeline_: Replace the 'click' capture recipe's programmatic 't.click()' with a real CDP coordinate click (Input.dispatchMousePressed/Released at the trigger button's bounding-box center) so the browser hit-tests; pointer-events:none will then yield '
- **`position-anchor`**  — _ui_: Redesign the demo so the chip is a real anchor-positioned element: give the anchor box 'anchor-name: --my-var', make the chip 'position: fixed; position-area: bottom center' (or top: anchor(...)) and set 'position-anchor' to the value under
- **`position-try`**  — _ui_: Build a real anchor-positioned scenario in the effect panel: an anchor box near a container edge, the chip as 'position: fixed; position-area: top' (so its preferred position overflows the top of a clipped container), and set 'position-try'
- **`position-try-fallbacks`**  — _ui_: Same fix as position-try: place the chip as a fixed anchor-positioned element whose preferred position-area overflows a clipped container, then set position-try-fallbacks to the tactic/position list under test. The engine will apply the fir
- **`position-try-order`**  — _ui_: Construct a scenario with an anchor-positioned chip that overflows and has at least two position-try fallbacks that yield different available widths/heights, then set position-try-order to the value under test. most-width vs most-height vs 
- **`position-visibility`**  — _pipeline_: Add a scroll/overflow automation step to the position-visibility demo: place the anchor inside a tall scroll container and capture a second frame after scrolling the anchor off-screen (so anchors-visible / no-overflow / anchors-valid values
- **`print-color-adjust`**  — _pipeline_: Add print-media emulation to the screenshot automation for this property: call DevTools Emulation.setEmulatedMedia with media='print' before capture, and give the demo elements heavy backgrounds/colors that 'economy' would strip and 'exact'
- **`resize`**  — _pipeline_: Add a 'resize' capture mode that performs a synthetic drag of the box's bottom-right corner handle (e.g. CDP mouse press at the corner, move +120,+120, release) and captures a before/after pair. After the drag, horizontal will have widened 
- **`scroll-initial-target`**  — _pipeline_: Rebuild the demo as a tall/overflowing scroll container whose content has a target element placed well below the fold, with scroll-initial-target:nearest set on that target. On load, capture the container's initial scroll position: with 'ne
- **`scroll-target-group`**  — _pipeline_: Replace the static box with a scrollable container that defines ::scroll-marker pseudo-elements on its items; with scroll-target-group:auto the host generates a scroll-marker group (visible marker dots/strip) while 'none' shows none. Captur
- **`scroll-timeline`**  — _pipeline_: Add a scrollable container that sets scroll-timeline:--my-var <axis> and an animated child whose animation-timeline:--my-var drives a visible transform (e.g. translate/scale tied to scroll progress). Drive the scroll in the demo (scroll the
- **`scroll-timeline-axis`**  — _pipeline_: Use a container that scrolls in BOTH axes, define scroll-timeline-name on it and animation-timeline on a child that maps scroll progress to a visible transform, then scroll the relevant axis. block/y will animate when scrolling vertically a
- **`scroll-timeline-name`**  — _pipeline_: Make the host a scrollable container with scroll-timeline-name:--my-var and add a child with animation-timeline:--my-var driving a visible scroll-linked transform; scroll the container in the demo. '--my-var' then animates the box as you sc
- **`scrollbar-color`**  — _pipeline_: Force a classic (non-overlay) scrollbar in the scrollable sample so scrollbar-color actually paints: e.g. apply a webkit/standard styled scrollbar context, render the demo under a Linux-style classic-scrollbar configuration, or launch Chrom
- **`scrollbar-gutter`**  — _pipeline_: Add a second sample container that does NOT overflow (content shorter than the box) alongside the overflowing one, applying the same scrollbar-gutter value to both. With no scrollbar present, 'auto' reserves no space while 'stable' still re
- **`shape-image-threshold`**  — _ui_: Change the floated specimen to use shape-outside referencing a same-origin raster image WITH a soft alpha gradient (e.g. a radial-fade PNG mask), so different thresholds clip the wrap contour at different alpha levels. Automate by adding su
- **`shape-rendering`**  — _ui_: Replace the sample element with an inline SVG containing a diagonal/curved <path> or thin-stroked geometry, zoomed in (large viewport / scaled), and apply shape-rendering to it. crispEdges will show aliased, hard pixel edges (no anti-aliasi
- **`text-box-edge`**  — _pipeline_: Redesign the demo element: apply text-box-trim: trim-both to the same sample element that carries text-box-edge, give the text a contrasting background/border (e.g. a padded inline-block with bg) so the trimmed over/under leading is visible
- **`text-box-trim`**  — _pipeline_: Give the sample text a visible padded box (background-color + border) and set text-box-edge: cap alphabetic (or text text) on the same element so the leading is non-trivial, then trim-start/trim-end/trim-both visibly shrink the box at top/b
- **`text-size-adjust`**  — _pipeline_: Add a mobile-emulation capture path for this property: set chromerpc to a small mobile device metric (e.g. 360px-wide viewport, device-scale 2, mobile=true) and render the sample inside a wide block of body text WITHOUT a <meta name=viewpor
- **`timeline-scope`**  — _pipeline_: Build a demo where a parent declares a named view/scroll timeline and a descendant has animation-timeline referencing that name with a keyframe animation that visibly moves/recolors a box. Set timeline-scope on the parent. Drive the scrolle
- **`touch-action`**  — _pipeline_: Extend the 'touch' capture to run several synthetic gestures per value and stack the results: a downward vertical pan, an upward vertical pan, a leftward and rightward horizontal pan, and a two-finger pinch. Capture which gestures move the 
- **`user-modify`**  — _pipeline_: Extend the temporal automation to focus the specimen element and emulate keystrokes (Input.dispatchKeyEvent via chromerpc), then capture frames: read-write accepts typed characters and shows a caret, read-only rejects input (content unchang
- **`user-select`**  — _pipeline_: In the 'select' capture, replace the full-node Range with (a) a partial drag-selection over only a few words so text/auto show a partial highlight, (b) a single collapsed click inside the element for all (which should expand to select the w
- **`view-timeline`**  — _pipeline_: Bind a keyframe animation (e.g. translateX/opacity 0->100%) to the named view-timeline via animation-timeline, place the subject inside a scrollable container, and have the screenshot automation scroll the container to several progress posi
- **`view-timeline-axis`**  — _pipeline_: Create a scrollable subject with a keyframe animation bound to the view timeline via animation-timeline. Make the container scrollable in BOTH axes (or place horizontal vs vertical scrollers) and scroll along the selected axis in automation
- **`view-timeline-inset`**  — _pipeline_: Bind a keyframe animation to a named view timeline and scroll the subject through its scrollport in automation, capturing the progress at multiple scroll offsets. Different inset values then shift the scroll position at which the animation 
- **`view-timeline-name`**  — _pipeline_: Add a subject element with animation-timeline: --my-var and a keyframe animation, declare view-timeline-name on the scroll subject, and scroll the container in automation across several offsets capturing filmstrips. With name=--my-var the a
- **`view-transition-class`**  — _pipeline_: Extend the screenshot automation to capture a live view transition: assign view-transition-name to the demo element, call document.startViewTransition() while mutating the element's position/size/color, and capture mid-transition frames (e.
- **`view-transition-name`**  — _pipeline_: Extend the automation to fire a real same-document view transition: set the demo element's view-transition-name, call document.startViewTransition() while mutating the DOM (move/resize/recolor the element or swap it for a sibling), and capt
- **`widows`**  — _pipeline_: Redesign the widows demo so the page/column height and the split paragraph's line count are tuned such that the NATURAL break would leave only 1 line at the top of the next page. Then widows:2 pushes 1 extra line down, widows:3 pushes 2, wi

## Category 7 — Broken / other — a value renders wrong or the demo is broken (fixable)  (13)

- **`border-image-source`**  — _grammar_: Stop emitting Chrome-unsupported image sources for <image>/border-image-source: drop image() (CSS Images L4 image()), the unprefixed cross-fade() form, and element(). In the border-image-source / <image> generation rule, restrict to url(), 
- **`caret`**  — _ui_: In live.jsx InteractiveDemo caret branch, guard the forced longhands on the EXACT longhand name only, never on the 'caret' shorthand: change `if (n !== 'caret-color') styleObj.caretColor = 'var(--accent)'` so it does NOT fire when n === 'ca
- **`corner-top-right-shape`**  — _none_: Engine-support gap: this Chrome build does not implement the superellipse() function form of corner-shape, so the value is dropped and falls back to round. No UI/automation/grammar change can make Chrome render it. Either drop superellipse(
- **`corner-top-shape`**  — _none_: Engine-support gap: this Chrome build does not implement the superellipse() function form of corner-shape, so the value is dropped and falls back to round. No UI/automation/grammar change can make Chrome render it. Either drop superellipse(
- **`font-synthesis`**  — _pipeline_: Fix the demo's value-application logic so the 'position' token only toggles font-variant-position synthesis and NEVER enables small-caps — small-caps must depend solely on the 'small-caps' token being present. Additionally, dedupe the gener
- **`initial-letter`**  — _ui_: Apply initial-letter to the ::first-letter pseudo-element of a real block paragraph (e.g. p.specimen::first-letter { initial-letter: <value>; }) with enough following text to wrap several lines, so the drop/raised cap and its sink depth bec
- **`line-clamp`**  — _ui_: Give the clamped specimen element a proper clamp context so line-clamp takes effect. For the legacy/webkit forms apply display:-webkit-box; -webkit-box-orient:vertical; overflow:hidden; and for the standardized form set overflow:hidden (and
- **`margin-trim`**  — _none_: No UI or automation change can fix this: Chrome does not support margin-trim, so the effect cannot be rendered in the headless pipeline. Consider dropping margin-trim from the gallery (or flagging it as unsupported) until/unless the renderi
- **`mask-border`**  — _ui_: Emit the prefixed longhands/shorthand that Blink actually supports: set -webkit-mask-box-image (and the -webkit-mask-box-image-source/-slice/-width/-outset/-repeat longhands) in the demo template alongside mask-border. Once -webkit-mask-box
- **`mask-border-width`**  — _ui_: Rebuild the template to set a mask-border-source (e.g. a 9-slice SVG/gradient border image) plus mask-border-slice, then vary mask-border-width per value. Use a solid colored box (not a photo) as the masked element with a thick, contrasting
- **`scroll-marker-group`**  — _pipeline_: Rebuild the demo as a real CSS carousel: a horizontal/vertical scroll container with scroll-snap-type and several child slides, give the children 'scroll-marker-group: <value>' on the container plus 'li::scroll-marker { content: '' }' styli
- **`text-autospace`**  — _none_: 
- **`text-decoration-inset`**  — _pipeline_: text-decoration-inset is not a CSS property Chrome implements; it should be removed from properties.txt (it is a non-existent property), or the template should target the real property text-underline-offset, which DOES move the underline by

## Category 8 — Grammar fix — collisions from spec-invalid generated values (fix lang/*.ebnf)  (191)

- **`accent-color`**  — _grammar_: Fix the color-value grammar used to generate accent-color samples so it never emits the non-standard / unimplemented color functions ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and color() with an undefined custom colorspace (--my-va
- **`animation-duration`**  — _grammar_: Fix the value generator/grammar so animation-duration only receives <time>-typed values. The math-function rule that feeds <time> properties is substituting <length> sample args (24px / 0.5rem); change it to emit <time> args (e.g. 1s / 0.5s
- **`animation-timing-function`**  — _grammar_: Constrain the cubic-bezier() generator so the x-coordinates (1st and 3rd arguments) are emitted only in the [0,1] range (the y-coordinates may be any number). The rule producing <cubic-bezier-easing-function> / cubic-bezier(<number>, <numbe
- **`background-clip`**  — _grammar_: Fix the background-clip value rule so it emits exactly ONE keyword from the single-keyword set (content-box | padding-box | border-box | border-area | text) and never a space-separated pair like 'border-area text' or 'text border-area'. The
- **`background-color`**  — _grammar_: Remove the unsupported color-function productions from the background-color <color> grammar: drop ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and the color() variant that uses an undefined custom colorspace token (--my-var). Restrict
- **`background-image`**  — _grammar_: In the image-set grammar, emit a real MIME type for type(): use type("image/jpeg") (or the actual asset's type) instead of the placeholder type("Specimen"). Also drop element(#id) from the background-image <image> production (Chrome does no
- **`background-position`**  — _grammar_: In the value generator for <position>/<bg-position> (the <length-percentage> production feeding background-position), stop emitting type-mismatched math functions and out-of-context anchor functions: drop sign()/acos()/asin()/atan()/atan2()
- **`background-size`**  — _grammar_: In the value generator for <bg-size>/<length-percentage> feeding background-size, stop emitting type-mismatched math functions and out-of-context anchor functions: drop sign()/acos()/asin()/atan()/atan2()/sin()/cos()/tan()/exp()/log()/pow()
- **`baseline-shift`**  — _grammar_: In the value generator for the baseline-shift <length-percentage> slot: (1) do not pass <length> arguments to angle/number math functions (sin, cos, tan, asin, acos, atan, atan2, exp, log, pow) or to sign() whose result is a <number> not a 
- **`block-size`**  — _pipeline_: Fix the value GENERATOR so math functions are only emitted with type-correct arguments: sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign must NOT be generated over a <length> in a <length> context -- restrict these to the <number>/<angle> 
- **`border`**  — _grammar_: In lang/functions.ebnf remove the non-standard color-function alternatives from the color production (ColorType / function-color group): IctcpFn (ictcp), JzazbzFn (jzazbz), JzczhzFn (jzczhz), AlphaFn (alpha), and HdrColorFn (color-hdr). Als
- **`border-block`**  — _grammar_: Same as 'border': in lang/functions.ebnf drop IctcpFn, JzazbzFn, JzczhzFn, AlphaFn, HdrColorFn from the color production and constrain ColorFn's ColorspaceParamsType so it cannot emit a custom-ident colorspace ('--my-var'). These produce de
- **`border-block-color`**  — _grammar_: Same grammar fix: in lang/functions.ebnf remove IctcpFn, JzazbzFn, JzczhzFn, AlphaFn, HdrColorFn from the color production and prevent ColorFn from emitting a custom-ident colorspace ('--my-var') via ColorspaceParamsType.
- **`border-block-end`**  — _grammar_: Same grammar fix in lang/functions.ebnf: drop IctcpFn, JzazbzFn, JzczhzFn, AlphaFn, HdrColorFn from the color production and constrain ColorFn's ColorspaceParamsType against custom-ident colorspaces ('--my-var').
- **`border-block-end-color`**  — _grammar_: Same grammar fix in lang/functions.ebnf: remove IctcpFn, JzazbzFn, JzczhzFn, AlphaFn, HdrColorFn from the color production and prevent ColorFn (ColorspaceParamsType) from emitting a custom-ident colorspace ('--my-var').
- **`border-block-end-width`**  — _grammar_: Remove 'hairline' from the border-width / line-width keyword set in the EBNF (the <line-width> / border-*-width production). Only thin | medium | thick | <length> are valid; 'hairline' is not a CSS keyword and is dropped by Chrome.
- **`border-block-start`**  — _grammar_: In the <color> grammar production used for border color values, remove the non-CSS color functions: ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and the invalid color(from <color> --my-var ...) form (color()'s first token must be a re
- **`border-block-start-color`**  — _grammar_: In the <color> grammar production, remove the non-CSS color functions ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and the invalid color(from <color> --my-var ...) form (color()'s first token must be a real colorspace identifier). Kee
- **`border-block-start-width`**  — _grammar_: Remove 'hairline' from the LineWidthType rule in lang/property.ebnf:1897 (LineWidthType = NonNegativeLengthType | hairline | thin | medium | thick) and from lang/keyword.ebnf:1173. Only thin|medium|thick are valid <line-width> keywords. Opt
- **`border-block-width`**  — _grammar_: Remove 'hairline' from the LineWidthType rule in lang/property.ebnf:1897 and lang/keyword.ebnf:1173; valid <line-width> keywords are only thin|medium|thick. Optionally remove the box base border so thin/medium/thick differ visibly.
- **`border-bottom`**  — _grammar_: Remove the non-standard color functions from ColorFunctionType in lang/datatype.ebnf:147 (drop IctcpFn | JzazbzFn | JzczhzFn | AlphaFn | HdrColorFn, defined in lang/functions.ebnf:54-66, and remove the --custom-ident colorspace branch from 
- **`border-bottom-color`**  — _grammar_: Remove the non-standard color functions from ColorFunctionType in lang/datatype.ebnf:147 (drop IctcpFn | JzazbzFn | JzczhzFn | AlphaFn | HdrColorFn defined at lang/functions.ebnf:54-66) and remove the --custom-ident colorspace branch from C
- **`border-bottom-left-radius`**  — _grammar_: In the math-function grammar feeding <length-percentage> for border-radius, restrict generated arguments to length-typed expressions only. sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign must NOT be offered where a <length> is required (t
- **`border-bottom-right-radius`**  — _grammar_: Same grammar fix as border-bottom-left-radius: in the math-function producer for <length-percentage>, only emit calc/clamp/min/max/round/abs and the anchor/env/calc-size forms that resolve to <length>; never emit sin/cos/tan/asin/acos/atan/
- **`border-color`**  — _grammar_: Remove the non-standard color-function productions from the <color> value generator used for border-color: ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and the color(from <color> --custom-ident ...) relative form. These are not real C
- **`border-end-end-radius`**  — _grammar_: In the <length-percentage> generator feeding border-*-radius, stop passing a <length> to trig/exponential functions that require a <number> or produce an <angle>: drop sin()/cos()/tan()/asin()/acos()/atan()/atan2()/exp()/log()/pow()/sign() 
- **`border-end-start-radius`**  — _grammar_: Same fix as border-end-end-radius: in the <length-percentage> generator for border-*-radius, do not feed a <length> into sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign, and drop calc-size() for radius. Keep abs()/round() over a <length>.
- **`border-image-width`**  — _grammar_: In the <length-percentage>/<number> generator that feeds border-image-width, never pass a <length> argument to angle/number-only math functions. Specifically remove sin/cos/tan/asin/acos/atan over a length, atan2/log/pow with length args, a
- **`border-inline`**  — _grammar_: In the <color> generator: (1) remove the non-standard color functions ictcp(), jzazbz(), jzczhz(), alpha(), and color-hdr() entirely. (2) For color(), only emit valid predefined colorspaces (srgb, srgb-linear, display-p3, a98-rgb, prophoto-
- **`border-inline-color`**  — _grammar_: In the color grammar feeding this property, stop emitting non-CSS color functions: remove ictcp(), jzazbz(), jzczhz(), alpha(), and color-hdr() from the <color>/color-function production, and constrain color()'s first argument to valid pred
- **`border-inline-end`**  — _grammar_: Same fix as border-inline-color: in the <color> production used by this border shorthand, remove ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr() and restrict color()'s colorspace argument to real colorspaces instead of an arbitrary custom
- **`border-inline-end-color`**  — _grammar_: Same fix as border-inline-color: remove ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr() from the <color> production and restrict color()'s colorspace argument to real colorspaces rather than a custom property dashed-ident.
- **`border-inline-end-width`**  — _grammar_: In the border-width / line-width grammar production, remove the 'hairline' keyword - it is not a valid <line-width> value (only thin | medium | thick | <length>). Emitting only thin/medium/thick plus real lengths removes the hairline-vs-med
- **`border-inline-start`**  — _grammar_: In the color grammar used by the value generator, remove the non-CSS color functions ictcp(), jzazbz(), jzczhz(), alpha(), and color-hdr() from the <color> production, and fix the color(from ...) rule so it only emits real predefined color-
- **`border-inline-start-color`**  — _grammar_: Same as border-inline-start: remove ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr() from the <color> production in the value-generator grammar, and constrain color(from ...) to emit real predefined color-space idents instead of --my-var. 
- **`border-inline-width`**  — _grammar_: In lang/property.ebnf line 1897, rule LineWidthType = NonNegativeLengthType | hairline | thin | medium | thick ; remove the 'hairline' alternative so it reads LineWidthType = NonNegativeLengthType | thin | medium | thick ;. (Optionally drop
- **`border-left`**  — _grammar_: In lang/datatype.ebnf line 147, rule ColorFunctionType, remove the unsupported alternatives IctcpFn | JzazbzFn | JzczhzFn | AlphaFn | HdrColorFn so only RgbFn | RgbaFn | HslFn | HslaFn | HwbFn | LabFn | LchFn | OklabFn | OklchFn | ColorFn r
- **`border-left-color`**  — _grammar_: In lang/datatype.ebnf line 147, rule ColorFunctionType, remove IctcpFn | JzazbzFn | JzczhzFn | AlphaFn | HdrColorFn leaving only RgbFn | RgbaFn | HslFn | HslaFn | HwbFn | LabFn | LchFn | OklabFn | OklchFn | ColorFn. Also constrain ColorFn s
- **`border-left-width`**  — _grammar_: In lang/property.ebnf line 1897, rule LineWidthType = NonNegativeLengthType | hairline | thin | medium | thick ; remove the 'hairline' alternative so it reads LineWidthType = NonNegativeLengthType | thin | medium | thick ;. (Optionally drop
- **`border-radius`**  — _grammar_: In the EBNF math-function rules (lang/*.ebnf, the <calc-value>/<math-function> productions feeding <length-percentage>), do not emit trig (sin/cos/tan) or inverse-trig (asin/acos/atan/atan2) or exponential (exp/log/pow) or sign() functions 
- **`border-right`**  — _grammar_: In the <color> grammar (lang/*.ebnf color productions), remove the non-standard color-function alternatives ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and constrain color(<colorspace> ...) so <colorspace> is one of the predefined-rg
- **`border-right-color`**  — _grammar_: Same color-grammar fix as border-right: in the <color> productions remove the non-standard ictcp()/jzazbz()/jzczhz()/alpha()/color-hdr() alternatives and constrain color(<colorspace> ...) so <colorspace> is a predefined-rgb/xyz keyword rath
- **`border-start-end-radius`**  — _grammar_: In the <length-percentage> value generator for border-radius longhands, do not emit math functions whose result is not a <length-percentage>: drop sign/sin/cos/tan/asin/acos/atan/atan2/exp/log/pow (they yield <number>/<angle>) and only keep
- **`border-start-start-radius`**  — _grammar_: Same fix as border-start-end-radius: in the border-radius <length-percentage> generator, exclude math functions that do not resolve to a length (sign/sin/cos/tan/asin/acos/atan/atan2/exp/log/pow) and exclude anchor()/anchor-size()/calc-size
- **`border-top`**  — _grammar_: In the <color> generator, remove the non-standard color functions that Chrome does not support: ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(). For color(), only emit valid predefined colorspace idents (srgb, srgb-linear, display-p3, a98
- **`border-top-color`**  — _grammar_: In lang/datatype.ebnf:147 ColorFunctionType, remove the unsupported color functions IctcpFn, JzazbzFn, JzczhzFn, AlphaFn and HdrColorFn (defined at lang/functions.ebnf:54/57/60/66/72). Also fix/remove the ColorFn branch path that generates 
- **`border-top-left-radius`**  — _grammar_: Fix the <length-percentage> generation so number/angle-producing math functions are never emitted for a <length-percentage> radius. In lang/datatype.ebnf:346-348 MathFunctionType (referenced by LengthPercentageType at lang/datatype.ebnf:349
- **`border-top-right-radius`**  — _grammar_: Same fix as border-top-left-radius: in lang/datatype.ebnf restrict LengthPercentageType (line 349) / MathFunctionType (lines 346-348) so the number/angle-producing functions (SignFn, AcosFn, AsinFn, AtanFn, Atan2Fn, SinFn, CosFn, TanFn, Exp
- **`border-top-width`**  — _grammar_: In lang/property.ebnf:1897 remove the `hairline` token from LineWidthType (currently `LineWidthType = NonNegativeLengthType | hairline | thin | medium | thick ;`). The valid <line-width> set is only <length> | thin | medium | thick. Also dr
- **`border-width`**  — _grammar_: Remove 'hairline' from the set of named-width keywords generated for border-width (and its longhands border-top/right/bottom/left-width). The <line-width> grammar rule should only emit the spec-valid keywords thin | medium | thick (plus <le
- **`bottom`**  — _grammar_: In the EBNF / value generator for <length-percentage> properties (e.g. the math-function production feeding `top/right/bottom/left` insets), restrict trig and exponential functions to type-correct arguments: sin/cos/tan must take <number>|<
- **`caret-color`**  — _grammar_: Fix the <color> grammar used by the value generator (lang/ color production) so it never emits non-existent CSS color functions or invalid colorspace idents: remove ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr() from the function alterna
- **`clear`**  — _grammar_: Restrict the clear value grammar so it only generates the spec keywords: none | left | right | both | inline-start | inline-end. Remove the block-start/block-end/top/bottom/both-inline/both-block keywords from whatever shared logical/physic
- **`clip`**  — _grammar_: Fix the value generator/grammar rule that produces clip's <rect>: for the 'clip' property emit only the comma-separated 4-arg form rect(<top>, <right>, <bottom>, <left>) with NO 'round'/slash extension, and ensure left<right and top<bottom 
- **`color`**  — _grammar_: Remove the non-standard / unsupported color functions from the <color> value grammar: ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and the color() variant whose colorspace token is a generic <custom-ident> (--my-var). The color() rule
- **`column-gap`**  — _grammar_: In the value generator for <length-percentage>/gap contexts, stop emitting trig/exp/log/pow/sign math functions applied to a <length> (they require <number>/<angle>), and do not emit anchor()/anchor-size()/calc-size() or the border-width id
- **`column-rule`**  — _grammar_: In the color-function grammar (the <color>/<color-function> rule feeding column-rule's <color> component), remove the non-standard color functions ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and disallow color(from <color> --custom-i
- **`column-rule-color`**  — _grammar_: In the <color> grammar feeding column-rule-color, remove the non-standard functions ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and forbid color(from <color> --dashed-ident ...) (colorspace must be a predefined ident such as srgb/dis
- **`column-rule-style`**  — _grammar_: Change ColumnRuleStyleProp (lang/property.ebnf:2986) from 'LineStyleListType | AutoLineStyleListType' to just 'LineStyleType' so only a single <line-style> keyword is generated. Chrome does not support the repeat()/comma-list column-rule-st
- **`column-rule-width`**  — _grammar_: Remove 'hairline' from LineWidthType at lang/property.ebnf:1897 (LineWidthType = NonNegativeLengthType | thin | medium | thick). 'hairline' is not a CSS-standard line-width keyword and is dropped by Chrome.
- **`columns`**  — _grammar_: Fix the columns / column-count value generator so it never emits a non-integer for the count component. The grammar rule producing column-count (the <integer> slot of the columns shorthand) is substituting var(--my-var, 0.5) whose fallback 
- **`content`**  — _grammar_: Fix the content/<image> value generator so it stops emitting Chrome-unsupported or type-invalid image functions: drop the image() function entirely; for image-set() emit a real MIME string (type("image/jpeg")) instead of type("Specimen"); u
- **`corner-end-end-shape`**  — _grammar_: In lang/functions.ebnf:75 SuperellipseFn (consumed by CornerShapeValueType at lang/datatype.ebnf:238), the generator picks the argument 1 which aliases bevel and is dropped by Chrome. Either drop the SuperellipseFn branch from CornerShapeVa
- **`corner-end-start-shape`**  — _grammar_: Same as corner-end-end-shape: in lang/functions.ebnf:75 SuperellipseFn (via CornerShapeValueType at lang/datatype.ebnf:238) avoid emitting superellipse(1) (aliases bevel, dropped by Chrome). Drop the SuperellipseFn branch or constrain its N
- **`corner-inline-end-shape`**  — _grammar_: Same as the other corner-shape properties: in lang/functions.ebnf:75 SuperellipseFn (via CornerShapeValueType at lang/datatype.ebnf:238) avoid emitting superellipse(1). Drop the SuperellipseFn branch or constrain its NumberType argument to 
- **`corner-inline-start-shape`**  — _grammar_: Same as the other corner-shape properties: in lang/functions.ebnf:75 SuperellipseFn (via CornerShapeValueType at lang/datatype.ebnf:238) avoid emitting superellipse(1). Drop the SuperellipseFn branch or constrain its NumberType argument to 
- **`corner-left-shape`**  — _grammar_: Same as the other corner-shape properties: in lang/functions.ebnf:75 SuperellipseFn (via CornerShapeValueType at lang/datatype.ebnf:238) avoid emitting superellipse(1). Drop the SuperellipseFn branch or constrain its NumberType argument to 
- **`cx`**  — _grammar_: In the value generator, the <length-percentage> production for cx/cy must NOT wrap a <length> argument in number-only math functions. Specifically: stop emitting sin()/cos()/tan()/asin()/acos()/atan()/atan2()/exp()/log()/pow()/sign() with <
- **`cy`**  — _grammar_: Same fix as cx: in the <length-percentage> production used by cx/cy, do not emit number-returning math functions (sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign) wrapping a <length>. Restrict math-function generation for length-typed slo
- **`fill`**  — _grammar_: In the color-value grammar (lang/ color/<color-function> rules feeding chrome-testing/cmd/generate), remove or gate the non-standard color functions ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and the color(from ... --my-var ...) cus
- **`flex`**  — _grammar_: Fix the <flex-basis>/<width> generator used inside the flex shorthand so trig/exp/log/pow/sign are not applied to a <length> argument, and so anchor()/anchor-size()/calc-size() are not emitted for the flex-basis component. Only type-valid <
- **`flex-basis`**  — _grammar_: In the <flex-basis>/width-keyword generator, stop emitting trig/exp/log/pow/sign math functions over a <length>, and do not emit anchor()/anchor-size()/calc-size() for flex-basis. Keep only type-valid length-percentage math functions and th
- **`flex-flow`**  — _grammar_: Remove the 'balance' keyword from the <flex-wrap> production in the grammar (flex-wrap must only generate nowrap | wrap | wrap-reverse). This eliminates all 'balance' permutations, leaving the 12 valid direction x wrap combinations (and the
- **`flex-wrap`**  — _grammar_: Fix the flex-wrap grammar production to emit exactly one of the three keywords nowrap|wrap|wrap-reverse. Remove the 'balance' keyword (not part of flex-wrap) and forbid multi-keyword sequences.
- **`float`**  — _grammar_: Restrict the float value grammar to the keywords the engine actually supports: none | left | right | inline-start | inline-end. Drop the CSS Page Floats production (block-start, block-end, snap-block(), snap-inline(), footnote) and the inva
- **`flood-color`**  — _grammar_: Same fix as fill: in the color-value grammar (lang/ <color-function> rules feeding chrome-testing/cmd/generate), drop the non-standard color functions ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(), and the color(from ... --my-var ...) c
- **`font-size`**  — _grammar_: In the font-size value generator, restrict the math/function rule to functions whose return type is a <length>/<length-percentage>: keep calc(), clamp(), min(), max(), round(), abs() (length args), and env() with a length fallback. Drop sig
- **`font-smooth`**  — _grammar_: Fix the font-smooth value grammar so it only emits its own keyword set (auto | never | always); the font-size keyword/<length> alternatives (xx-small..xxx-large and the px lengths) are leaking in from a shared/incorrect production and must 
- **`font-style`**  — _grammar_: Fix the font-style value grammar so it emits only 'normal | italic | oblique [<angle [-90deg,90deg]>]'. Remove 'left'/'right' (these come from a generic direction/keyword rep-value leaking into the font-style production) and constrain the o
- **`gap`**  — _grammar_: In the <length-percentage> value generator for gap/row-gap/column-gap: only emit math functions whose result type is a <length>/<percentage>. Drop sign() (returns <number>), the trig functions sin/cos/tan/asin/acos/atan/atan2 and exp/log/po
- **`grid`**  — _grammar_: Fix lang/datatype.ebnf LengthPercentageType (line 349): it pulls in the full MathFunctionType (line 346-348) which includes SinFn/CosFn/TanFn/AsinFn/AcosFn/AtanFn/Atan2Fn/ExpFn/LogFn/PowFn/SignFn. Those return angles or unitless numbers and
- **`grid-auto-columns`**  — _grammar_: Fix lang/datatype.ebnf LengthPercentageType (line 349) which references the full MathFunctionType (lines 346-348). The trig (SinFn/CosFn/TanFn/AsinFn/AcosFn/AtanFn/Atan2Fn), exponential (ExpFn/LogFn/PowFn) and SignFn return angles/unitless 
- **`grid-auto-rows`**  — _grammar_: Same root cause as grid-auto-columns: fix lang/datatype.ebnf LengthPercentageType (line 349) which references the full MathFunctionType (lines 346-348). Restrict the math functions allowed in length contexts to CalcFn/ClampFn/MinFn/MaxFn/Ro
- **`grid-column-gap`**  — _grammar_: In the math-function grammar rule that fills <length-percentage> for gap properties, do not emit angle/number-typed functions (sin/cos/tan/asin/acos/atan/atan2 and exp/log/pow/sign) with <length> arguments. Restrict the <length-percentage> 
- **`grid-gap`**  — _grammar_: Same <length-percentage> grammar rule used for gap: stop emitting angle/number-typed math functions (sin/cos/tan/asin/acos/atan/atan2, exp/log/pow/sign) with <length> arguments into either component of the grid-gap shorthand, and stop emitt
- **`grid-row-gap`**  — _grammar_: In the EBNF/value generator for <length-percentage> (the grid-row-gap / gap track-size rule), stop feeding a <length> argument to angle/number math functions. sin/cos/tan/asin/acos/atan/atan2 return <angle> and require <angle>|<number> inpu
- **`grid-template`**  — _grammar_: In the grid-template <track-size>/<track-list> generation rule, stop emitting angle/number math functions where a <track-breadth>/<length-percentage> is required. sin/cos/tan/asin/acos/atan/atan2 return <angle> and sign returns a <number> -
- **`grid-template-columns`**  — _grammar_: In the value generator, restrict the math-function expansion used for <track-breadth>/<length-percentage> track sizes (grid-template-columns/rows) to functions whose computed type is <length>/<length-percentage>: calc(), min(), max(), clamp
- **`grid-template-rows`**  — _grammar_: Same grammar fix as grid-template-columns: restrict the math-function expansion for <track-breadth>/<length-percentage> track sizes to length-returning functions (calc, min, max, clamp, round, abs). Remove the trig/sign branch (sin, cos, ta
- **`height`**  — _grammar_: In lang/functions.ebnf constrain the trig/inverse-trig/exponential function rules so they do not feed a <length> as the argument for length-valued properties. Affected rules: SinFn, CosFn, TanFn (require <angle>|<number>), AcosFn, AsinFn, A
- **`image-orientation`**  — _grammar_: Edit lang/property.ebnf line 3383 ImageOrientationProp to `from_image | none ;` (drop the `AngleType , [ flip ] | flip , [ AngleType ]` alternatives). The obsolete <angle>? flip? syntax was removed from the spec and is unsupported by Chrome
- **`inline-size`**  — _grammar_: Same fix as height. In lang/functions.ebnf restrict the argument of SinFn, CosFn, TanFn (require <angle>|<number>) and AcosFn, AsinFn, AtanFn, Atan2Fn, ExpFn, LogFn, PowFn, SignFn (require dimensionless <number>) so their CalcSumType argume
- **`inset`**  — _grammar_: In the EBNF value generator for <length-percentage> / inset-family properties, stop emitting type-mismatched math functions: do not feed a <length> argument to sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign (these require <number>/<angle
- **`inset-block`**  — _grammar_: Same as inset: in the <length-percentage> value generator, stop feeding <length> arguments to sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign, drop sign() (unitless result) and any var() fallback that is a bare number, in length contexts.
- **`inset-block-end`**  — _grammar_: Same as inset: constrain the <length-percentage> generator so sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign are never emitted with <length> arguments (they need <number>/<angle>), and so var() fallbacks are valid <length-percentage> tok
- **`inset-block-start`**  — _grammar_: Same as inset: in the <length-percentage> generator, never emit sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign with <length> arguments (they require <number>/<angle>), and never emit a var() fallback that is a bare number in a length con
- **`inset-inline`**  — _grammar_: Same as inset: constrain the <length-percentage> generator so sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign are never emitted with <length> arguments, and var() fallbacks are valid <length-percentage> tokens rather than bare numbers.
- **`inset-inline-end`**  — _grammar_: In the EBNF/value generator for the <length-percentage> production used by inset/offset properties, stop feeding type-mismatched arguments to math functions: trig (sin/cos/tan) and inverse-trig (asin/acos/atan/atan2) must take <angle>/<numb
- **`inset-inline-start`**  — _grammar_: Same grammar fix as inset-inline-end: in the <length-percentage> production for inset/offset properties, stop generating math functions with type-mismatched args (trig/inverse-trig must take <angle>/<number>; exp/log/pow must take <number>;
- **`left`**  — _grammar_: Same grammar fix as the inset properties: in the <length-percentage> production used by left/top/right/bottom and inset properties, stop emitting math functions with type-mismatched args (trig/inverse-trig need <angle>/<number>; exp/log/pow
- **`letter-spacing`**  — _grammar_: In the value generator for letter-spacing (a property whose grammar accepts <length-percentage>|normal), restrict the math-function expansion so trig/inverse-trig/exponential/sign functions are NOT emitted into a <length> slot: sin/cos/tan/
- **`lighting-color`**  — _grammar_: In the color-value grammar feeding lighting-color, remove the non-standard / unsupported <color> function productions: ictcp(), jzazbz(), jzczhz(), alpha(), and color-hdr(); and constrain color(from <color> <ident> ...) so its colorspace to
- **`line-height`**  — _grammar_: In lang/datatype.ebnf, the rule 'MathFunctionType' (lines 346-348) wrongly lists AcosFn|AsinFn|AtanFn|Atan2Fn|SinFn|CosFn|TanFn|ExpFn|LogFn|PowFn as <length-percentage>-producing functions; MathFunctionType feeds NonNegativeLengthPercentage
- **`margin`**  — _grammar_: Fix the grammar/value generator so dimensionless math functions are never given <length> args for margin: sin/cos/tan must take <angle>|<number>, asin/acos/atan/atan2 return <angle> (not a <length>) and must be excluded, exp/log/pow/sign ta
- **`margin-block`**  — _grammar_: Same grammar fix as margin: in the <length-percentage> math production used for margin-block, forbid sin/cos/tan/exp/log/pow/sign from receiving <length> args (they need <number>/<angle>) and exclude angle-returning asin/acos/atan/atan2 ent
- **`margin-block-end`**  — _grammar_: Same grammar fix as margin: restrict trig/exp/log/pow/sign to <number> args and <length>-typed results, exclude angle-returning asin/acos/atan/atan2 from the margin <length-percentage> math production, and make var() fallback a <length>.
- **`margin-block-start`**  — _grammar_: Same grammar fix as margin: restrict trig/exp/log/pow/sign to <number> args and <length>-typed results, exclude angle-returning asin/acos/atan/atan2 from the margin <length-percentage> math production, and make var() fallback a <length>.
- **`margin-bottom`**  — _grammar_: Same grammar fix as margin: restrict trig/exp/log/pow/sign to <number> args and <length>-typed results, exclude angle-returning asin/acos/atan/atan2 from the margin <length-percentage> math production, and make var() fallback a <length>.
- **`margin-inline`**  — _grammar_: Fix the math-function value generator so trig/exp/log/pow/sign are only emitted with spec-valid argument types for a <length-percentage> context. In the EBNF/grammar rule that produces <calc-value>/<math-function> arguments for length prope
- **`margin-inline-end`**  — _grammar_: Same grammar fix as margin-inline: restrict sin/cos/tan/asin/acos/atan/atan2 args to <angle>/<number> and never use their result as a length, restrict exp/log/pow/sign args to <number>, drop or wrap sign() as a multiplier, and make the var(
- **`margin-inline-start`**  — _grammar_: Same grammar fix as margin-inline: restrict sin/cos/tan/asin/acos/atan/atan2 args to <angle>/<number> and never use their result as a length, restrict exp/log/pow/sign args to <number>, drop or wrap sign() as a multiplier, and make the var(
- **`margin-left`**  — _grammar_: Same grammar fix as margin-inline: restrict sin/cos/tan/asin/acos/atan/atan2 args to <angle>/<number> and never use their result as a length, restrict exp/log/pow/sign args to <number>, drop or wrap sign() as a multiplier, and make the var(
- **`margin-right`**  — _grammar_: Same grammar fix as margin-inline: restrict sin/cos/tan/asin/acos/atan/atan2 args to <angle>/<number> and never use their result as a length, restrict exp/log/pow/sign args to <number>, drop or wrap sign() as a multiplier, and make the var(
- **`margin-top`**  — _grammar_: Fix the margin-top length-value generator so it never feeds a <length> argument to the trigonometric/exponential functions (sin, cos, tan, asin, acos, atan, atan2, exp, log, pow) and never emits sign(<length>) into a <length> slot — these r
- **`marker`**  — _grammar_: Fix the MarkerRefType production (and the UrlType assist feeding it) so it emits a same-document fragment marker reference such as url(#marker-dot) that points to a real <marker> element, instead of url(assets/photo.jpg). The demo should re
- **`marker-end`**  — _grammar_: Fix MarkerRefType / the UrlType assist to emit a fragment marker reference (url(#marker-dot)) referencing a real <marker> element rather than url(assets/photo.jpg).
- **`marker-mid`**  — _grammar_: Fix MarkerRefType / the UrlType assist to emit a fragment marker reference (url(#marker-dot)) referencing a real <marker> element rather than url(assets/photo.jpg).
- **`marker-start`**  — _grammar_: Fix MarkerRefType / the UrlType assist to emit a fragment marker reference (url(#marker-dot)) referencing a real <marker> element rather than url(assets/photo.jpg).
- **`mask-position`**  — _grammar_: Fix the value generator so a <length-percentage>/<position> context (PositionType feeding MaskPositionProp at property.ebnf:3575, ultimately CalcSumType in lang/functions.ebnf) never emits trig/exp/log/pow over a bare <length>: sin/cos/tan/
- **`mask-repeat`**  — _grammar_: Remove repeat_block and repeat_inline from the RepeatStyleType production at lang/property.ebnf:1851 (RepeatStyleType = repeat_x | repeat_y | repeat_block | repeat_inline | RepetitionType , [ RepetitionType ]) for the mask-repeat / backgrou
- **`mask-size`**  — _grammar_: In the value generator/grammar for the <length-percentage>/<bg-size> slot used by mask-size, forbid the trig/exponential functions (sin/cos/tan/asin/acos/atan/atan2/exp/log/pow) and sign() from receiving <length> arguments and from being us
- **`max-block-size`**  — _grammar_: Fix the value generator so trig/exp/log/pow/sign and atan2 are never emitted with <length> arguments for <length-percentage> sizing properties. In the EBNF math-function rules (lang/ calc/math-function grammar), restrict sin/cos/tan/asin/ac
- **`max-height`**  — _grammar_: Same grammar fix as max-block-size: restrict trig/exp/log/pow/sign/atan2 so they are not generated with <length> arguments and not sampled where a <length-percentage> is required. Only length-returning math functions (abs/min/max/clamp/calc
- **`max-inline-size`**  — _grammar_: Same grammar fix as the rest of the sizing family: stop emitting trig/exp/log/pow/sign/atan2 with <length> arguments and exclude angle/number-returning math functions from <length-percentage> sizing value sets; keep only abs/min/max/clamp/c
- **`max-width`**  — _grammar_: Same grammar fix as the rest of the sizing family: do not emit trig/exp/log/pow/sign/atan2 with <length> arguments and exclude angle/number-returning math functions from <length-percentage> sizing value sets; keep only abs/min/max/clamp/cal
- **`min-block-size`**  — _grammar_: Same grammar fix as the rest of the sizing family: stop emitting trig/exp/log/pow/sign/atan2 with <length> arguments and exclude angle/number-returning math functions from <length-percentage> sizing value sets; keep only abs/min/max/clamp/c
- **`min-height`**  — _grammar_: Fix the value generator/grammar for the <length-percentage> sizing context so it never feeds <length> arguments into trig/log/exp/pow/sign functions and never emits a non-length keyword like 'contain'. In lang/, the math-function rule (the 
- **`min-inline-size`**  — _grammar_: Same grammar fix as min-height/min-width: in lang/, the math-function production allowing sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign must require <number>/<angle> typed arguments (CSS Values 4) instead of substituting a <length> like
- **`min-width`**  — _grammar_: Same grammar fix as min-height/min-inline-size: in lang/, the math-function production allowing sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign must require <number>/<angle> typed arguments (CSS Values 4) rather than substituting a <lengt
- **`object-fit`**  — _grammar_: Fix the object-fit value generator so it emits exactly ONE keyword from the set { fill | contain | cover | none | scale-down } and never juxtaposes two of them. The grammar rule producing object-fit values is concatenating/combining keyword
- **`object-position`**  — _grammar_: In the value generator for object-position (a <position> / <length-percentage> grammar rule), restrict generation to the actually-valid grammar: physical keywords (left|right|top|bottom|center), <length-percentage>, and math functions that 
- **`offset-anchor`**  — _grammar_: Fix the value generator so the <length-percentage> / <position> slot for offset-anchor never receives trig (sin/cos/tan/asin/acos/atan/atan2), exp/log/pow, or sign() applied to a length, and never receives a bare number from var()/calc-size
- **`offset-distance`**  — _grammar_: Same grammar fix as offset-anchor: in the <length-percentage> slot, exclude trig/exp/log/pow/sign applied to lengths and bare-number var()/calc-size() fallbacks; keep only calc/clamp/min/max/round/abs over lengths-percentages. After the fix
- **`outline`**  — _grammar_: Fix the color value generator so it never emits non-CSS color functions for <color> contexts: drop the ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr() function productions, and make color() only emit valid <colorspace-params> (predefined 
- **`outline-color`**  — _grammar_: Same fix as outline: in the <color> value generator, remove the ictcp()/jzazbz()/jzczhz()/alpha()/color-hdr() productions and constrain color() to emit only valid predefined or @color-profile colorspaces instead of '--my-var'. Keep the rela
- **`overscroll-behavior`**  — _grammar_: Remove 'chain' from the overscroll-behavior grammar productions in lang/property.ebnf: OverscrollBehaviorProp / OverscrollBehaviorPropItem (line 3729-3730), and also the sibling rules OverscrollBehaviorBlockProp (3717), OverscrollBehaviorIn
- **`overscroll-behavior-block`**  — _grammar_: In lang/property.ebnf change OverscrollBehaviorBlockProp (line 3717) from '= contain | none | auto | chain' to '= contain | none | auto'. (Fix the sibling overscroll productions at lines 3720/3723/3726/3729-3730 too.) Then improve the demo 
- **`overscroll-behavior-inline`**  — _grammar_: In lang/property.ebnf change OverscrollBehaviorInlineProp (line 3720) from '= contain | none | auto | chain' to '= contain | none | auto'. (Fix the sibling overscroll productions at 3717/3723/3726/3729-3730 too.) Then improve the demo to ov
- **`padding`**  — _grammar_: In the length-percentage value grammar feeding padding, restrict math-function generation to functions that return a <length>/<length-percentage>: keep calc(), clamp(), min(), max(), round(), abs() (with length args) and env(); do NOT emit 
- **`padding-block`**  — _grammar_: Same grammar fix as padding: restrict the math-function rule feeding the <length-percentage> for padding-block to length-returning functions (calc/clamp/min/max/round/abs/env) and stop emitting angle/number-typed trig/exp/log/pow/sign over 
- **`padding-block-end`**  — _grammar_: Same grammar fix as padding: restrict the math-function rule feeding the <length-percentage> for padding-block-end to length-returning functions and drop the angle/number-typed trig/exp/log/pow/sign over a <length> plus anchor()/anchor-size
- **`padding-block-start`**  — _grammar_: Same grammar fix as padding: restrict the math-function rule feeding the <length-percentage> for padding-block-start to length-returning functions and drop the angle/number-typed trig/exp/log/pow/sign over a <length> plus anchor()/anchor-si
- **`padding-bottom`**  — _grammar_: Same grammar fix as padding: restrict the math-function rule feeding the <length-percentage> for padding-bottom to length-returning functions (calc/clamp/min/max/round/abs/env) and drop the angle/number-typed trig/exp/log/pow/sign over a <l
- **`padding-inline`**  — _grammar_: In the <length-percentage> value generator for padding properties, stop feeding <length> arguments into math functions that require <number>/<angle> (sin/cos/tan/exp/log/pow and sign), and stop emitting angle-returning (asin/acos/atan/atan2
- **`padding-inline-end`**  — _grammar_: In the <length-percentage> value generator for padding properties, stop feeding <length> arguments into math functions that require <number>/<angle> (sin/cos/tan/exp/log/pow and sign), and stop emitting angle-returning (asin/acos/atan/atan2
- **`padding-inline-start`**  — _grammar_: In the <length-percentage> value generator for padding properties, stop feeding <length> arguments into math functions that require <number>/<angle> (sin/cos/tan/exp/log/pow and sign), and stop emitting angle-returning (asin/acos/atan/atan2
- **`padding-left`**  — _grammar_: In the <length-percentage> value generator for padding properties, stop feeding <length> arguments into math functions that require <number>/<angle> (sin/cos/tan/exp/log/pow and sign), and stop emitting angle-returning (asin/acos/atan/atan2
- **`padding-right`**  — _grammar_: In the <length-percentage> value generator for padding properties, stop feeding <length> arguments into math functions that require <number>/<angle> (sin/cos/tan/exp/log/pow and sign), and stop emitting angle-returning (asin/acos/atan/atan2
- **`padding-top`**  — _grammar_: In the EBNF math-function / value grammar, restrict which functions may be generated for the <length-percentage> padding context. Do not emit sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign with <length> arguments where a length result is
- **`perspective-origin`**  — _grammar_: In the value generator for <length-percentage>/<position> args, stop emitting trig/exp/log/pow/sign/atan2 with <length> arguments. sin/cos/tan/asin/acos/atan/exp/log/pow require unitless <number> (or sin/cos/tan an <angle>); sign() returns 
- **`place-self`**  — _grammar_: Fix the place-self / self-position grammar so the safe|unsafe overflow modifier is only generated in front of a <self-position> keyword, NOT in front of 'normal' or 'auto'. Remove the 'unsafe normal' / 'safe normal' productions (the [ safe 
- **`quotes`**  — _grammar_: The EBNF rule QuotesProp/QuotesPropItem (lang/property.ebnf:3832-3833, QuotesPropItem = string_type , string_type) correctly requires a string PAIR, but the generator collapses it to a single StringType rep. Fix the generator so each Quotes
- **`right`**  — _grammar_: In the value generator for <length-percentage> / right (and the shared offset grammar), stop feeding raw <length> arguments into functions that require angles or unitless numbers. Trig (sin/cos/tan) should receive an <angle> or <number>; in
- **`rotate`**  — _grammar_: Fix the rotate value rule so a numeric axis is emitted only as a full <number>{3} vector (e.g. '1 0.5 2 320deg'), never as a single number + angle. Remove the production that pairs one <number> with an <angle>. Also fix the attr() generator
- **`row-gap`**  — _grammar_: In the row-gap / <length-percentage> value generator, restrict math-function arguments to length-producing functions only: keep calc/clamp/min/max/round/abs (length-typed) but DROP sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign when thei
- **`rx`**  — _grammar_: In the rx value grammar, stop feeding raw <length> arguments to the math/trig functions. sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign require <number>/<angle> arguments (and trig/exp/log/pow/sign do not yield a <length>), so they must 
- **`ry`**  — _grammar_: Same fix as rx: in the ry (shared SVG geometry <length-percentage>) grammar, remove sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign as <length> producers and stop passing <length> args to math functions that require <number>/<angle>. Keep
- **`scroll-padding`**  — _grammar_: In lang/datatype.ebnf, LengthPercentageType (line 349) references MathFunctionType (line 346) which bundles number/angle-returning functions (SignFn, SinFn, CosFn, TanFn, AsinFn, AcosFn, AtanFn, Atan2Fn, ExpFn, LogFn, PowFn) alongside the l
- **`scroll-padding-block`**  — _grammar_: Same as scroll-padding: in lang/datatype.ebnf restrict LengthPercentageType (line 349) to use a length-valued math-function subset (CalcFn/ClampFn/MinFn/MaxFn/RoundFn/AbsFn) instead of the full MathFunctionType (line 346) so SignFn/SinFn/Co
- **`scroll-padding-block-end`**  — _grammar_: Same as scroll-padding: restrict LengthPercentageType (lang/datatype.ebnf line 349) to a length-valued math-function subset (CalcFn/ClampFn/MinFn/MaxFn/RoundFn/AbsFn) instead of the full MathFunctionType (line 346), so trig/exp/log/pow/sign
- **`scroll-padding-block-start`**  — _grammar_: In the value generator for <length-percentage> properties (scroll-padding family), stop emitting trig/exponential math functions with <length> arguments. The math-function rule must type-check argument/return kinds: sin/cos/tan/asin/acos/at
- **`scroll-padding-bottom`**  — _grammar_: Same grammar fix as the whole scroll-padding family: exclude trig (sin/cos/tan/asin/acos/atan/atan2) and exponential/sign (exp/log/pow/sign) math functions from the <length-percentage> value pool because they take/return non-length kinds, a
- **`scroll-padding-inline`**  — _grammar_: Same grammar fix as the whole scroll-padding family: exclude trig (sin/cos/tan/asin/acos/atan/atan2) and exponential/sign (exp/log/pow/sign) math functions from the <length-percentage> value pool because they take/return non-length kinds, a
- **`scroll-padding-inline-end`**  — _grammar_: Same grammar fix as the whole scroll-padding family: exclude trig (sin/cos/tan/asin/acos/atan/atan2) and exponential/sign (exp/log/pow/sign) math functions from the <length-percentage> value pool because they take/return non-length kinds, a
- **`scroll-padding-inline-start`**  — _grammar_: Same grammar fix as the whole scroll-padding family: exclude trig (sin/cos/tan/asin/acos/atan/atan2) and exponential/sign (exp/log/pow/sign) math functions from the <length-percentage> value pool because they take/return non-length kinds, a
- **`scroll-padding-left`**  — _grammar_: In the value generator for <length-percentage> slots, stop feeding <length> arguments into trig/exp/log/pow/sign functions. The math-function rule must only emit sin/cos/tan/asin/acos/atan/atan2/exp/log/pow over <number> args (and the resul
- **`scroll-padding-right`**  — _grammar_: Same fix as scroll-padding-left: exclude trig/exp/log/pow/sign math functions from the <length-percentage> value generator (they return number/angle, never a length), and make the var() fallback a <length-percentage> (e.g. 0.5rem) instead o
- **`scroll-padding-top`**  — _grammar_: Same shared fix: the <length-percentage> generator rule must not pass <length> arguments to trig/exp/log/pow/sign (these return number/angle, invalid as a length) and the var() fallback must be a <length-percentage> (e.g. 0.5rem) not the ba
- **`shape-margin`**  — _grammar_: Fix the value generator so <length-percentage> properties like shape-margin never receive math/trig functions that take or return non-length types. Restrict the function-emitting rule for length-percentage contexts to calc/min/max/clamp/rou
- **`stop-color`**  — _grammar_: In the <color> grammar used by stop-color (and other paint/color properties), drop the non-standard color functions ictcp(), jzazbz(), jzczhz(), alpha(), and color-hdr(), and restrict color()'s first argument to real colorspace idents (srgb
- **`stroke`**  — _grammar_: Same <color> grammar fix as stop-color: remove the non-standard color functions ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr() from the <color> production used by stroke, and restrict color()'s colorspace argument to real predefined colo
- **`stroke-dasharray`**  — _grammar_: Fix the value generator so trig/exp/log/pow functions are never emitted with <length> arguments for stroke-dasharray. In the EBNF math-function rules (lang/ calc/trig grammar), restrict sin()/cos()/tan() to <number>|<angle>, asin()/acos()/a
- **`stroke-dashoffset`**  — _grammar_: Same grammar fix as stroke-dasharray: in the EBNF math-function rules restrict sin()/cos()/tan() to <number>|<angle>, asin()/acos()/atan()/exp()/log()/sign() to <number>, atan2()/pow() to <number>, and never feed a bare <length> into a func
- **`stroke-miterlimit`**  — _grammar_: Constrain the stroke-miterlimit value generator to emit only numbers >= 1 (the spec minimum). In the grammar rule that produces <number> for stroke-miterlimit, drop the 0.5/0.75/0.25 (and any <1) samples and substitute distinct >=1 values s
- **`stroke-width`**  — _grammar_: In the value generator/grammar for stroke-width, stop emitting type/unit-mismatched math functions and out-of-context functions/keywords. Specifically: (1) do not feed <length> into sin/cos/tan/asin/acos/atan/atan2/exp/log/pow (these requir
- **`text-align`**  — _grammar_: Stop the text-align value generator from emitting bare <string> values ("Specimen", "Aa") and the non-standard justify-all keyword for a generic block specimen. In the text-align grammar rule, drop justify-all (it is not a valid text-align 
- **`text-decoration`**  — _grammar_: Fix the <color> / color-function production in lang/ EBNF: (1) for legacy comma rgb()/rgba(), only emit integer channel values (or switch to the modern space-separated syntax) so floats like 0.5/2 are never generated; (2) remove the non-sta
- **`text-decoration-color`**  — _grammar_: Same <color> / color-function grammar fix as text-decoration: emit only integer channels for legacy comma rgb()/rgba() (or use modern syntax), remove ictcp()/jzazbz()/jzczhz()/alpha()/color-hdr(), and restrict color() to valid predefined co
- **`text-decoration-thickness`**  — _grammar_: Constrain the value generator for text-decoration-thickness to only emit valid <length-percentage> | auto | from-font. (1) In the math-function rule, do not feed <length> arguments to sin/cos/tan/asin/acos/atan/atan2/exp/log/pow or to sign(
- **`text-emphasis`**  — _grammar_: Fix the <color> grammar feeding text-emphasis values: drop legacy comma rgb()/rgba() forms that emit float channel values (rgb(1,0.5,2,0.75)); remove the unsupported color functions ictcp(), jzazbz(), jzczhz(), alpha(), and color-hdr(); and
- **`text-emphasis-color`**  — _grammar_: Same <color> grammar fix as text-emphasis: drop legacy comma rgb()/rgba() with float channels; remove unsupported ictcp(), jzazbz(), jzczhz(), alpha(), color-hdr(); and make color(from ...) emit a valid colorspace ident instead of '--my-var
- **`text-justify`**  — _grammar_: Fix the text-justify value generator/grammar rule so it never emits `no-compress` (alone or compounded). text-justify takes a single keyword from `auto | none | inter-word | inter-character | distribute`; `no-compress` belongs to a differen
- **`text-underline-offset`**  — _grammar_: In the value generator for <length-percentage> properties (text-underline-offset), restrict the math-function production so trig/exp/log/pow/sign are only emitted with <number>/<angle>-typed arguments, never a bare <length>. Specifically: s
- **`text-wrap-style`**  — _grammar_: Remove the avoid_orphans alternative from rule TextWrapStyleProp (lang/property.ebnf line 4170) and from TextWrapStyleType (lang/property.ebnf line 2498) so 'avoid-orphans' is never generated for text-wrap-style (or the text-wrap shorthand)
- **`top`**  — _grammar_: Same fix as right: in the shared <length-percentage> offset grammar, stop emitting trig functions (sin/cos/tan) with <length> arguments and stop emitting asin/acos/atan/atan2/exp/log/pow/sign (which yield <angle> or unitless <number>, not <
- **`transform-origin`**  — _grammar_: In the value generator, restrict math/trig functions used for the <length-percentage> production so they only emit functions whose result type is <length>/<length-percentage>. Disallow sin/cos/tan/asin/acos/atan/atan2 (return <number>/<angl
- **`transition`**  — _grammar_: In the transition shorthand grammar/generator (the rule that fills <single-transition-property> inside the 'transition' shorthand, lang/ EBNF for the transition shorthand), stop emitting 'none' for the property slot. Emit 'all' (or a concre
- **`vertical-align`**  — _grammar_: Fix the value generator for <length-percentage> properties so it never emits type/unit-mismatched math functions. In the EBNF math-function rule (the <calc-value>/<math-function> production used to fill <length-percentage>), restrict to fun
- **`visibility`**  — _grammar_: In lang/property.ebnf line 4258, change the rule 'VisibilityProp = visible | hidden | force_hidden | collapse ;' to 'VisibilityProp = visible | hidden | collapse ;' (remove the force_hidden alternative). 'force-hidden' is not a valid CSS vi
- **`white-space`**  — _grammar_: Fix the value generator/EBNF for the white-space property so it only emits Chrome-supported white-space values: normal | pre | pre-wrap | pre-line | nowrap | break-spaces. Remove the CSS Text Level 4 long-form expansion (white-space-collaps
- **`width`**  — _grammar_: Same grammar fix as the min-* sizing properties: in lang/, the math-function production allowing sin/cos/tan/asin/acos/atan/atan2/exp/log/pow/sign must require <number>/<angle> typed arguments (CSS Values 4) rather than substituting a <leng
